# Packages

Shared TypeScript/JavaScript packages live here.

Cross-process or cross-language protocol types must ultimately come from the canonical schema source defined in P01 rather than independently drifting package interfaces.
