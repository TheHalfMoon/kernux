[package]
name = "kernux-policy"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license = "Apache-2.0"
publish = false

[dependencies]
kernux-contracts = { path = "../kernux-contracts" }

[lib]
path = "src/lib.rs"
