[package]
name = "kernux-secret"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license = "Apache-2.0"
publish = false

[dependencies]
kernux-policy = { path = "../kernux-policy" }
zeroize = { version = "=1.9.0", default-features = false, features = ["alloc"] }

[lib]
path = "src/lib.rs"

