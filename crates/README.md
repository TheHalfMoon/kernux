# Crates

Rust crates live here.

The privileged `kernuxd` implementation and other native components are created only when their canonical phases become dependency-eligible. The workspace is intentionally empty during the repository-baseline task.
