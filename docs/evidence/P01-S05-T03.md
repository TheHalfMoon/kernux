# Evidence — KX-P01-S05-T03

## Task

`KX-P01-S05-T03 — Build canonical protocol fixture corpus including disconnect, duplicate operation, malformed payload and version mismatch.`

SpecGrain:

`SG-000016 — KX-P01-S05-T03 — Build the canonical KRP adversarial protocol corpus`

Risk: `R2`.

## Outcome

KRP v1 now has one tracked provider-neutral adversarial corpus at:

`protocol/fixtures/v1/adversarial.json`

The corpus contains:

- twenty stable policy cases, `ADV-01` through `ADV-20`, matching the minimum adversarial invariants in `docs/canonical/RUNTIME_OPERATION_MODEL.md` section 28;
- thirteen wire cases, `WIRE-01` through `WIRE-13`, covering malformed payloads and protocol/capability version mismatch;
- an explicit evidence boundary separating contract/policy-fixture conformance from live runtime behavior.

The normal Node protocol gate executes:

`tools/protocol/adversarial.mjs`

Rust independently consumes the same tracked adversarial fixture bytes through:

`crates/kernux-contracts/tests/adversarial.rs`

The normal `pnpm check` baseline includes the Node adversarial gate, and the Cargo workspace test baseline includes the Rust mirror.

This evidence does not claim exactly-once runtime execution, durable replay protection, real cancellation delivery, reconnect persistence, or live runtime transport behavior.

## Authority chain

PR #74 — `docs: refine KRP adversarial corpus grain`

- exact head `49616994c9b4df926e07705aab2d7ffc791cd637`;
- exact-head CI `35420433382` — SUCCESS;
- merge `901852fb886dd000888fc5b7cae6bb8422b1e0f7`;
- post-merge CI `35420477060` — five protected jobs PASS.

The native WorkPacket generated from the refined Grain is bound to:

- spec revision: `sha256:68ae041723ef54fcda29ce50feca25f8a9c2f93c0e3950f679c73d158f1e6e67`;
- WorkPacket: `sha256:0b29333440bb49c2fc99579198b31b941fcd79c8ca0b8695d776fd897f499b87`;
- context-plan digest: `sha256:c5a8cfc0ab4417184f6cee260b6d52e626a98631f30df56ab0f4960c642ecc07`.

## Implementation chain

PR #75 — `test: add canonical KRP adversarial corpus`

- exact head `f5ab4f5b5a6be36dac88f7edf7c274f5d700aadc`;
- exact-head CI `35420629836` — five protected jobs PASS;
- merge `324d21592c2ab8a64e68d53081606cfd881419d4`;
- post-merge CI `35421424515` — five protected jobs PASS.

PR #76 — `test: enforce KRP adversarial policy`

- exact head `5d96d64d22c44768515ee6d7f2c9378a95533228`;
- exact-head CI `35421605163` — five protected jobs PASS;
- merge `14ce280b59e199533eb3e9187d9e921fcc38c6dd`;
- post-merge CI `35421645005` — five protected jobs PASS.

PR #77 — `test: mirror KRP adversarial corpus in Rust`

- exact head `605d2581f39d969c4009bfad3630dc5d0f63948c`;
- exact-head CI `35421806819` — five protected jobs PASS;
- merge `45cf26be030bc5713566734e09be5fa872eafbd9`;
- post-merge CI `35421854493` — five protected jobs PASS.

PR #78 — `docs: bind KRP adversarial evidence`

- exact head `37bdb54383a8d81e46074569531f24a785d5c344`;
- exact-head CI `35422006682` — five protected jobs PASS;
- merge `986b0fd6754411885f88f544c63718a9467b3382`;
- post-merge CI `35422072516` — five protected jobs PASS.

Final qualified implementation revision:

`986b0fd6754411885f88f544c63718a9467b3382`

## Executable adversarial proof

The Node gate reports:

`KRP adversarial conformance: PASS (20 policy, 13 wire cases)`

It fails closed for:

- missing or duplicate required invariant IDs;
- wrong policy-rule binding;
- missing required wire scenarios;
- unsupported mutation operations;
- malformed or unsupported schema meanings;
- unsupported Rust expectation metadata;
- version-coverage gaps;
- evidence-boundary drift.

Deliberate negative checks performed during implementation included:

- changing an expected policy outcome — rejected;
- binding `ADV-01` to the wrong policy rule — rejected;
- removing a required wire case — rejected.

The Rust mirror has three passing tests:

- shared twenty-invariant policy fixture;
- shared wire rejection/schema-boundary behavior;
- runtime-evidence claim boundary.

The Rust mirror proves ten malformed/version cases are rejected by generated serde for the applicable generated types.

Three string-pattern cases are intentionally JSON-Schema-only:

- malformed operation UUID;
- malformed payload digest;
- malformed resource URI.

A deliberate false Rust expectation that malformed UUID text must be rejected by generated serde caused the Rust mirror to fail, preserving the schema-vs-serde boundary honestly.

## Adversarial semantic coverage

The corpus proves at the contract/policy-fixture layer:

- disconnect, heartbeat timeout, request timeout, and controller cache loss do not synthesize exit;
- cancellation acceptance does not synthesize exit;
- cancellation transport loss remains unverifiable;
- stale observation generation cannot replace newer truth;
- equal-generation contradictory states are invariant conflicts;
- same OperationId + same canonical fingerprint reconciles without a second side effect;
- same OperationId + different fingerprint conflicts before execution;
- ambiguous or may-have-started paths preserve the original OperationId and require reconciliation;
- runtime advertisement alone does not authorize;
- Grant without negotiated capability does not authorize;
- runtime/provider type does not imply action support;
- reconnect does not revive expired or revoked Grant authority;
- changed enrolled trust identity is not the same Runtime;
- provider diagnostics cannot lower canonical side-effect uncertainty;
- unknown OperationId remains uncertain absent an explicit persistence guarantee;
- RuntimeDescriptor, RuntimeCapability, OperationStart, and Event version mismatches reject without downgrade.

## Provider-neutrality

The canonical adversarial fixture contains no provider names.

The Rust mirror contains no provider names.

The Node checker contains provider-name strings only in its explicit denylist used to fail if provider-specific content leaks into the fixture. Those denylist strings are checker implementation, not corpus or public contract semantics.

## Independent local verification

On final qualified main `986b0fd6754411885f88f544c63718a9467b3382`:

- `node tools/protocol/adversarial.mjs` — PASS, 20 policy + 13 wire cases;
- `cargo test -p kernux-contracts --test adversarial` — 3/3 PASS;
- `pnpm check` — PASS;
- `git diff --check` over the complete S05-T03 implementation range — PASS;
- provider-neutral corpus/public-semantics scan — PASS;
- same tracked adversarial fixture bytes are referenced by Node and consumed by Rust through `include_bytes!`;
- native SG-000014 and SG-000015 prerequisite records remain `verified=true`, `issues=[]`.

Diffcipline R2 passed for the final bounded ranges of PRs #75, #76, #77, and #78.

An earlier local PR #75 R2 attempt failed because the filesystem ran out of space during Cargo execution. That failed attempt is preserved as a failed attempt and was not relabeled. After safe cleanup, the exact candidate was rerun and R2 passed before merge.

## Review discipline

OpenCodeReview was used as the review framework for implementation slices.

- PR #75: OCR default path policy excluded the fixture JSON; its JSON rule was resolved, and the complete 20-policy/13-wire corpus received an independent semantic review.
- PR #76: OpenCodeReview covered 2/2 changed files; two checker-coverage gaps were found and fixed before the final commit.
- PR #77: OpenCodeReview covered 1/1 Rust file; Rust ownership, panic-boundary, unsafe/concurrency, performance, API, and security rules were applied.
- PR #78: OCR excluded Markdown as `unsupported_ext`; all 3/3 documentation files were manually reviewed and one typo was fixed before the final commit.

CodeRabbit was not used as review evidence.

## Native SpecGrain verification

Native verification was executed only after the final implementation merge and its post-merge five-job qualification succeeded.

- spec revision: `sha256:68ae041723ef54fcda29ce50feca25f8a9c2f93c0e3950f679c73d158f1e6e67`;
- WorkPacket: `sha256:0b29333440bb49c2fc99579198b31b941fcd79c8ca0b8695d776fd897f499b87`;
- context-plan digest: `sha256:c5a8cfc0ab4417184f6cee260b6d52e626a98631f30df56ab0f4960c642ecc07`;
- implementation revision: `git:986b0fd6754411885f88f544c63718a9467b3382`;
- verification: `verified=true`;
- issues: `[]`;
- result digest: `sha256:493b73cb4aae08e49c98d09479e0bc76280c9a75520128d9541af32a3baa7746`;
- evidence record: `sha256:d0f0200aec1f62a725add036579ce70808ac54cfb83a31df998f69f75a6c0e74`.

The immutable evidence record was appended through the pinned native SpecGrain API. It was not hand-authored.

Observed implementation paths are exactly:

- `crates/kernux-contracts/tests/adversarial.rs`;
- `docs/canonical/KRP_COMPATIBILITY.md`;
- `docs/canonical/PROTOCOL_STRATEGY.md`;
- `docs/canonical/RUNTIME_OPERATION_MODEL.md`;
- `package.json`;
- `protocol/fixtures/v1/adversarial.json`;
- `tools/protocol/adversarial.mjs`.

## Closure sequencing

The native report verifies the implementation while preserving the closeout sequencing rule.

Before this closeout merge:

- `KX-P01-S05-T03` remains `NEXT`;
- P01 remains `6 / 7 macro tasks PROVEN`.

This protected closeout is the authorized transition point to:

- `KX-P01-S05-T03 = PROVEN`;
- P01 macro-task count = `7 / 7 PROVEN`.

P01 exit is **not** inferred from task count and remains a separate gate assessment.

## Governance verification

The protected branch metadata continues to require:

- JavaScript / TypeScript;
- Rust;
- SpecGrain;
- Diffcipline R1;
- Provenance.

The GitHub App could not re-read the administrative branch-protection endpoint during this closeout because that endpoint returned HTTP 403. Therefore this evidence does not claim a fresh administrative-endpoint read of `enforce_admins`; the last successful live verification remains the authoritative prior observation.

No required check was removed or weakened. No force-push, rebase, or shared-history rewrite was used.

## Acceptance conclusion

After this protected closeout is merged and its post-merge qualification succeeds:

**PROVEN** for `KX-P01-S05-T03`.

P01 then has all seven macro tasks PROVEN.

The next frontier is the explicit P01 exit-gate assessment:

`cross-language contracts executable; provider names absent from core; disconnect cannot imply exit; grants structurally bounded.`

P02 remains blocked until that gate is separately proven.
