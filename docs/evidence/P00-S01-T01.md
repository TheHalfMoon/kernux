# Evidence — KX-P00-S01-T01

## Task

`KX-P00-S01-T01 — Establish pnpm + Cargo monorepo skeleton, canonical folders, root commands, formatting/editor baseline.`

Risk: `R1`

## Qualified implementation

Implementation commit:

`f91245bf1e032dd078619cf3c07a582abbb4520a`

Merged to `main` through PR #3 at:

`a921ad864077b6465de62427524136e11a576245`

## Observed change

The implementation branch was exactly one commit ahead of the pre-task `main` and zero behind. The diff contained only the 17 intended repository-baseline files:

- pnpm/Cargo workspace roots;
- pinned Rust/pnpm baseline configuration;
- editor/line-ending/ignore configuration;
- canonical top-level workspace boundary READMEs;
- deterministic zero-dependency workspace checker;
- development-baseline documentation.

No CI workflow, product application, privileged daemon, dependency, donor source, or release behavior was introduced.

## Verification performed

Before the implementation commit was created, the exact new-file contents were exercised locally with:

```text
node --check tools/workspace/check.mjs
python: JSON/TOML parsing of package.json, Cargo.toml, rust-toolchain.toml
node tools/workspace/check.mjs
git diff --cached --check
```

Observed results:

```text
JSON/TOML parse: PASS
Kernux workspace baseline: PASS
git diff --check: PASS
```

After commit creation, GitHub commit inspection confirmed the expected exact contents at `f91245bf...`, and the branch comparison confirmed exactly 17 intended added files, one commit ahead, zero behind.

PR #3 was mergeable and had no review threads at merge time. Repository CI did not yet exist; creating it is the next canonical task (`KX-P00-S01-T02`), so no CI success is claimed for T01.

## Toolchain provenance

At task execution time:

- pnpm `12.4.2` was the current npm `latest` release;
- Rust `1.98.1` was the current stable point release published on 2026-09-03 and fixed the Rust 1.98.0 vtable-generation miscompilation.

The task pins those versions explicitly rather than relying on floating `latest`.

## Acceptance conclusion

**PROVEN** for the bounded T01 outcome.

The evidence establishes the repository skeleton, root workspace contract, toolchain pins, and formatting/editor baseline. It deliberately does not establish CI, lint, static-analysis, unit-test, SpecGrain, Diffcipline, provenance-enforcement, or product-runtime claims.

## Recovery

The task is additive scaffolding only. Recovery is a normal revert of PR #3 / merge commit `a921ad864077b6465de62427524136e11a576245`; no user data or runtime state was created.
