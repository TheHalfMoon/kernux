# Evidence — KX-P00-S01-T04

## Task

`KX-P00-S01-T04 — Decide/apply project license and validate compatibility strategy for planned donor imports/dependencies.`

SpecGrain:

`SG-000004 — KX-P00-S01-T04 — Establish Apache-2.0 project license and compatibility policy`

Canonical task risk: `R1`.

The refined Grain required `R2` exact-change proof for the implementation slices.

## Outcome

Kernux-owned code is now explicitly licensed under Apache-2.0 with a canonical repository-root license, a bounded project-license policy, deterministic fail-closed validation, conformance coverage, and dedicated CI enforcement.

The policy preserves third-party license boundaries and records the current compatibility posture for the three pinned public founding-donor references, the installed JavaScript/TypeScript toolchain/support packages, and the current dependency-free Rust workspace.

No donor product source was imported by this task.

No CLA, DCO, contributor-governance policy, release SBOM, release NOTICE bundle, installer attribution package, or universal future-license compatibility claim was created by this task.

## Grain identity and WorkPacket

Refinement PR #21:

`docs: refine Apache-2.0 project license grain`

Refinement head:

`e99be3472184250eead3c5ddadde10a789886d1a`

Refinement merge:

`4709ad3c68d82dc54ae3de5ab9c338dd3aa013a3`

Spec revision:

`sha256:02e9b8aea298821ced7b836ab9558453846b594d069fb6cab1d500d2192d6958`

WorkPacket digest:

`sha256:4910da4459a27027a793c5a3daf6307cb7d5d4235c155d0c7cf627504f90084f`

Context-plan digest:

`sha256:68950b80f3a3759458a1aac9965094120c7691f27b82ef6230d7345ff60d32ce`

The exported WorkPacket used 8 explicit context sources and 7,334 / 8,500 estimated context tokens.

The already-proven T02 prerequisite was bound through exact canonical repository evidence rather than by fabricating unsupported SpecGrain lifecycle state.

Refinement exact-head GitHub Actions run:

`35305103712`

Post-merge refinement run on `main@4709ad3c68d82dc54ae3de5ab9c338dd3aa013a3`:

`35308853329`

Observed on both runs:

- JavaScript / TypeScript: SUCCESS;
- Rust: SUCCESS;
- SpecGrain: SUCCESS;
- Diffcipline R1: SUCCESS;
- Provenance: SUCCESS.

## Canonical project-license identities

Project-owned code SPDX identity:

`Apache-2.0`

Canonical Apache License 2.0 byte digest:

`sha256:cfc7749b96f63bd31c3c42b5c471bf756814053e847c10f3eb003417bc523d30`

Canonical project-license policy byte digest:

`sha256:52036ff4149918166c6feb843798e2cdd023ddd6a7d6c0d3ef98d342709ceaf3`

The repository-root npm manifest remains private and intentionally carries no publish-license field.

The repository-root Cargo manifest remains a virtual workspace and intentionally carries no inherited publish-license field.

Every future distributable npm package or Rust crate must declare explicit SPDX metadata before release.

## Current compatibility posture

The bounded current posture records:

- `stablyai/orca@0d23ea6e688410c878096dab8b1779857354b7d4` — MIT;
- `tinyfish-io/agentql@418ba8ad1c69dfac134a6833369a01dfba5a24a7` — MIT;
- `wonderwhy-er/DesktopCommanderMCP@08ff76192919a6e8fe2557b39c3f99e7b54c3b92` — MIT;
- `oxfmt@0.68.0` — MIT;
- `oxlint@1.83.0` — MIT;
- `tinypool@2.1.2` — MIT;
- `typescript@7.0.2` — Apache-2.0;
- current installed Oxc platform binding — MIT;
- current installed TypeScript platform package — Apache-2.0;
- current Cargo workspace — no external package dependency.

MIT and Apache-2.0 inputs are admitted under the current policy subject to applicable provenance and notice obligations.

Any other SPDX expression, custom/proprietary term, copyleft class, weak-copyleft class, network-copyleft class, ambiguous license, or non-code asset/model/data/service term requires explicit review before admission to a distributed Kernux artifact.

This is an engineering source-admission policy, not a universal legal compatibility opinion.

## Core license/policy slice

PR #22:

`build: establish Apache-2.0 project license core`

Canonical base:

`4709ad3c68d82dc54ae3de5ab9c338dd3aa013a3`

Final implementation head:

`7ff052f44d47e3673c86a75d09ad8e97de6eeb93`

Merged to `main` at:

`e7c864e82507a7c7e168226190ee4ec26278a9c9`

The slice established:

- the canonical repository-root `LICENSE`;
- `docs/canonical/LICENSE_POLICY.md`;
- explicit README and third-party boundary language;
- the fail-closed `tools/provenance/validate_project_license.py` validator.

The validator rejects:

- missing, tampered, or symlinked canonical license/policy paths;
- parent-directory symlink traversal;
- a non-private npm root;
- publish-license metadata on the current private npm root;
- a non-virtual Cargo root;
- inherited publish-license metadata on the current virtual Cargo root;
- missing required project/third-party boundary markers;
- malformed duplicate-key package JSON;
- non-UTF8 Cargo input;
- nondeterministic/unsorted diagnostics.

Local proof recorded on the exact implementation head included:

- project-license validator: PASS;
- notice validator: PASS;
- provenance validator: PASS;
- parent-directory symlink adversarial probe: PASS;
- `pnpm check`: PASS;
- `git diff --check`: PASS;
- Diffcipline R2: PASS.

OpenCodeReview identified a real parent-directory symlink gap before the final candidate. The validator was hardened to reuse the repository path guard, and no actionable finding remained on the final core head.

Exact-head GitHub Actions run:

`35308937388`

Post-merge run on `main@e7c864e82507a7c7e168226190ee4ec26278a9c9`:

`35309005434`

Observed on both runs: all five required jobs SUCCESS.

## Conformance and CI enforcement slice

PR #23:

`test: enforce Apache-2.0 project license policy`

Canonical base:

`e7c864e82507a7c7e168226190ee4ec26278a9c9`

Final enforcement head:

`a307675953fb5db0989d96d71dc1f48a6bcca149`

Merged to `main` at:

`d19122c9217e2ad1d9c133de3e64f34ff984f7d5`

The slice added 12 project-license conformance/adversarial tests and integrated project-license compile/test/check steps into the existing least-privilege Provenance job.

The combined provenance enforcement corpus now contains:

- 21 donor-provenance tests;
- 18 notice-inventory tests;
- 12 project-license tests;
- 51 total tests.

The Provenance CI job continues to use `contents: read`, pinned checkout, full Git history for provenance proof, and no new secret/token expansion.

Local proof recorded on the exact enforcement head included:

- combined conformance: 51/51 PASS;
- project-license validator: PASS;
- notice validator: PASS;
- provenance validator: PASS;
- `pnpm check`: PASS;
- `git diff --check`: PASS;
- Diffcipline R2: PASS.

OpenCodeReview on the final enforcement range covered both reviewable changed files. A stale missing-file diagnostic assertion exposed by the stronger parent-symlink guard was corrected before the final candidate. No actionable finding remained.

Exact-head GitHub Actions run:

`35309085749`

Observed:

- JavaScript / TypeScript: SUCCESS;
- Rust: SUCCESS;
- SpecGrain: SUCCESS;
- Diffcipline R1: SUCCESS;
- Provenance: SUCCESS.

Final post-merge run on `main@d19122c9217e2ad1d9c133de3e64f34ff984f7d5`:

`35309205416`

Observed:

- JavaScript / TypeScript: SUCCESS;
- Rust: SUCCESS;
- SpecGrain: SUCCESS;
- Diffcipline R1: SUCCESS;
- Provenance: SUCCESS.

## Acceptance conclusion

**PROVEN** for `KX-P00-S01-T04`.

All required SG-000004 evidence classes are established:

- `t02-prerequisite-proof-bound`: PROVEN;
- `apache-license-canonical-digest-pass`: PROVEN;
- `private-virtual-root-boundary-pass`: PROVEN;
- `current-dependency-license-inventory-pass`: PROVEN;
- `founding-donor-compatibility-review-pass`: PROVEN;
- `third-party-license-boundary-review-pass`: PROVEN;
- `project-license-validator-tests-pass`: PROVEN;
- `provenance-ci-pass`: PROVEN;
- `diffcipline-r2-proof-pass`: PROVEN;
- `baseline-ci-pass`: PROVEN;
- `post-merge-ci-pass`: PROVEN.

This proves the bounded P00 project-license and compatibility-policy task.

It does **not** prove:

- any donor product-source import;
- characterization or security review of a future donor import;
- contributor/community governance;
- P00 phase completion;
- P01 contract completion;
- product implementation;
- release qualification;
- release readiness.

## Remaining P00 dependency truth

The only remaining P00 macro task in the registry is:

`KX-P00-S01-T03 — Add CONTRIBUTING, SECURITY, SUPPORT, CODE_OF_CONDUCT/issue/PR templates and repository governance docs.`

T03 depends only on T01, which is already PROVEN.

T03 therefore becomes the active next macro task after this closeout.

P00 itself remains incomplete until T03 is PROVEN and the P00 exit gate is explicitly reconciled against fresh-clone checks, SpecGrain/Diffcipline usability, and provenance enforcement.

No P01 implementation is authorized by this closeout.

## Recovery

To remove the complete T04 implementation, revert the enforcement merge `d19122c9217e2ad1d9c133de3e64f34ff984f7d5`, then revert the core merge `e7c864e82507a7c7e168226190ee4ec26278a9c9` in reverse dependency order.

The refinement merge `4709ad3c68d82dc54ae3de5ab9c338dd3aa013a3` may then be reverted separately if the Grain itself must be removed.

This task created no Kernux product/user runtime state and imported no donor product source.
