# Evidence — KX-P00-S01-T03

## Task

`KX-P00-S01-T03 — Add CONTRIBUTING, SECURITY, SUPPORT, CODE_OF_CONDUCT/issue/PR templates and repository governance docs.`

Primary Grain:

`SG-000005 — KX-P00-S01-T03 — Establish contributor and repository governance baseline`

Corrective Grain:

`SG-000006 — KX-P00-S01-T03 — Reconcile GitHub issue-template scope under explicit paths`

Canonical task risk: `R0`.

Both implementation slices were qualified at the stronger repository `R1` proof floor.

## Outcome

Kernux now has repository-owned contribution, security, support, conduct, issue, pull-request, and repository-governance surfaces before product implementation begins.

The community/governance baseline is discoverable from the README and is enforced by the deterministic workspace checker. A regression test proves that removal of any required governance surface fails closed.

No product code, donor product source, repository setting, CLA/DCO, private maintainer contact, SLA, bounty promise, or P01 contract work was introduced.

## Primary Grain identity

Refinement PR #25:

`docs: refine repository governance grain`

Refinement head:

`bdc5f2e1955d4ae6110bd27cb666564dec87ba67`

Refinement merge:

`c6f67895e62a1f5a5d5dbca43a3dd01cc6504279`

SG-000005 revision:

`sha256:c66f3c0a5616e7d98c852fa44d7a0093561da9a989cf0b142003abeaf6d71616`

Original WorkPacket:

`sha256:4e34348c64f777d7caea8f9c93e32caebfa5676d48eb30b2be6a7981cb7fcd0b`

Context-plan digest:

`sha256:e06648fc335bc0e42ae99193da0e39920d54e1bf83b65c5094dc22dfa23fee07`

Context accounting:

- 8 required sources;
- 26,570 bytes;
- 6,646 / 8,000 estimated tokens.

The exact original WorkPacket was later reconstructed from the context metadata preserved in PR #25 and matched the original digest byte-for-byte.

Refinement PR CI:

`35313392522` — all five required jobs SUCCESS.

Post-merge refinement CI:

`35313457990` — all five required jobs SUCCESS.

## Core governance slice

PR #26:

`docs: establish repository governance baseline`

Final core head:

`49b2cb04c7a7b3ffbb00c88c50996cb71e15a385`

Merged to `main` at:

`e159848ee6dbb820d98d0590d2c200e6d31d1a49`

The slice added:

- `CONTRIBUTING.md`;
- `SECURITY.md`;
- `SUPPORT.md`;
- `CODE_OF_CONDUCT.md`;
- `docs/governance/REPOSITORY_GOVERNANCE.md`;
- bug, feature, and support issue forms;
- issue chooser configuration;
- pull-request template.

All policy prose is original Kernux-owned text. No third-party governance template text was copied.

Security guidance does not falsely claim private vulnerability reporting is enabled. At refinement time, live GitHub API truth reported private vulnerability reporting disabled, so the policy instructs reporters to use GitHub private reporting only when the repository exposes it and otherwise request a private channel without posting vulnerability details.

Local validation included:

- four YAML files parsed successfully;
- three issue forms passed required structural checks;
- security/privacy/provenance boundary assertions PASS;
- `pnpm check` PASS;
- `git diff --check` PASS;
- Diffcipline R1 PASS.

OCR delegate accounting:

- total files: 10;
- reviewable YAML files: 4;
- Markdown excluded as `unsupported_ext`;
- resolved GitHub YAML rules manually checked;
- no actionable finding remained.

PR-head CI:

`35313889730` — all five required jobs SUCCESS.

Post-merge CI:

`35313954696` — all five required jobs SUCCESS.

## Enforcement slice

PR #27:

`test: enforce repository governance surfaces`

Final enforcement head:

`0603b44378b8f803c082e46c2275ebc1c4fd2b54`

Merged to `main` at:

`7c87797421d698e1e08de34d623a0ff4158fbc8c`

The slice:

- exposes contributor/security/support/conduct/governance/development entry points from README;
- makes all required governance/community/template files part of the deterministic workspace contract;
- makes the workspace checker importable without changing CLI behavior;
- adds a portable temporary-fixture regression test.

The regression test removes every required governance surface one at a time, requires an `ENOENT` failure, restores the file, and proves the reconstructed fixture passes again.

Focused test result:

`2 passed / 0 failed`

Exact enforcement Diffcipline R1:

- changed files: 3;
- +100 / -36;
- scope violations: 0;
- verification: PASS.

PR-head CI:

`35325060210` — all five required jobs SUCCESS.

Post-merge CI:

`35325148975` — all five required jobs SUCCESS.

## SG-000005 native proof gap

Independent reconstruction of the exact SG-000005 WorkPacket succeeded and matched the original digest.

Independent verification then observed all 13 implementation paths from the primary refinement merge through the enforcement merge.

The pinned SpecGrain verifier returned:

`verified=false`

with four `UNSCOPED_CHANGE` issues:

- `.github/ISSUE_TEMPLATE/bug_report.yml`;
- `.github/ISSUE_TEMPLATE/config.yml`;
- `.github/ISSUE_TEMPLATE/feature_request.yml`;
- `.github/ISSUE_TEMPLATE/support_question.yml`.

Cause:

SG-000005 used the change-surface entry:

`.github/ISSUE_TEMPLATE/**`

The pinned verifier at:

`TheHalfMoon/SpecGrain@5de7d6499bb0a9e3a191fc0934399cf099d1980a`

authorizes only exact equality or literal directory-prefix matching. It does not expand glob syntax.

No SG-000005 evidence record was written.

The Grain was not edited retroactively and the failed result was not relabeled as PASS.

## Explicit-path corrective Grain

Corrective refinement PR #28:

`docs: refine issue-template scope reconciliation`

Refinement head:

`df7ca48492d300414eac559822b385e7dd539ce9`

Refinement merge:

`e4f1ed03b18543efb71b26bb18627aa4e67fb7d3`

SG-000006 revision:

`sha256:2ef3ad4c244ee5c6c599ea12926175fdb546a80bfb4e1f03ec4e153bdb3b735f`

WorkPacket:

`sha256:b795105a86fdb070191b341be86f8023451c11809f761f1d7b7d5f49e88dc1a8`

Context plan:

`sha256:c7af63e40aecd204cb2002f2f6ca567ef924f70596a774133204dabdae65d956`

The first corrective packet attempt correctly failed because required context was 5,436 tokens against a 5,000-token policy budget.

Context was reduced by replacing the full CURRENT file with only its active frontier/dependency/delivery excerpt.

The successful packet used:

- 7 required sources;
- 16,277 bytes;
- 4,072 / 5,000 estimated tokens.

The corrective Grain uses four explicit issue-template/config paths and no wildcard change surface.

Refinement PR CI:

`35325592969` — all five required jobs SUCCESS.

Post-merge refinement CI:

`35325664507` — all five required jobs SUCCESS.

## Corrective implementation

PR #29:

`chore: reconcile issue-template scope explicitly`

Final corrective head:

`5a113d8b04b26ee0b017239438e1a1ebe965c88e`

Merged to `main` at:

`f483762e1afdff04686801c6ac097d555e749259`

The exact corrective diff touches all four explicitly authorized YAML paths with one non-functional governance comment per file.

Semantic YAML digests computed from parsed YAML before and after the change were identical for all four files.

Validation:

- four YAML files parse: PASS;
- three issue-form structural contracts: PASS;
- `pnpm check`: PASS;
- `git diff --check`: PASS;
- Diffcipline R1: PASS;
- OCR reviewable coverage: 4/4.

PR-head CI:

`35325800570` — all five required jobs SUCCESS.

Post-merge CI:

`35325891272` — all five required jobs SUCCESS.

## SG-000006 native proof

Independent verification was executed against:

`git:f483762e1afdff04686801c6ac097d555e749259`

Observed changed paths: exactly the four explicit issue-template/config paths.

Verification result:

`verified=true`

Issues:

`[]`

Result digest:

`sha256:6e37fb4f0744622eac1cca5b8b6d58f7b301682dc606cc9276b256373102fcf5`

Evidence record:

`sha256:5096e6059a53f6c1db6c2d00317b61137f03fc2c8ca9945795838b9d8f3fc6bb`

`specgrain prove SG-000006 .` deterministically reports `verified=true`.

## Acceptance conclusion

**PROVEN** for `KX-P00-S01-T03`.

The macro outcome is proven by the combined primary implementation evidence plus the explicit-path corrective evidence:

- T01 prerequisite bound: PROVEN;
- community files/structure: PROVEN;
- security reporting boundary: PROVEN;
- issue/PR template validation: PROVEN;
- repository governance review: PROVEN;
- workspace governance regression: PROVEN;
- Diffcipline R1 proof: PROVEN;
- baseline CI: PROVEN;
- final post-merge CI: PROVEN.

The historical SG-000005 native proof gap remains visible and is not converted into PASS. The four affected current surfaces are instead explicitly re-adopted and natively verified by SG-000006.

This proves the T03 repository-governance macro outcome.

It does **not** prove:

- P00 phase exit;
- P01 contract implementation;
- product implementation;
- donor source import;
- release readiness.

## Remaining P00 truth

All eight P00 macro tasks are now PROVEN.

The P00 phase itself remains in progress until its explicit exit gate is reconciled against live repository truth:

- fresh-clone documented checks;
- Rust and JavaScript CI baselines;
- SpecGrain/Diffcipline workflow reproducibility;
- donor provenance enforcement before imports;
- absence of product dependence on ungoverned donor internals.

No P01 implementation is authorized by this closeout alone.

## Recovery

To remove the T03 implementation, revert the corrective implementation merge `f483762e1afdff04686801c6ac097d555e749259`, the enforcement merge `7c87797421d698e1e08de34d623a0ff4158fbc8c`, and the core governance merge `e159848ee6dbb820d98d0590d2c200e6d31d1a49` in reverse dependency order.

The refinement merges `e4f1ed03b18543efb71b26bb18627aa4e67fb7d3` and `c6f67895e62a1f5a5d5dbca43a3dd01cc6504279` may then be reverted separately if their Grains must also be removed.

This work created no product runtime state or user data.
