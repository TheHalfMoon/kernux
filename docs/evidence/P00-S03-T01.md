# Evidence — KX-P00-S03-T01

## Task

`KX-P00-S03-T01 — Initialize Diffcipline policy with R0-R3 verification profiles and CI gate.`

SpecGrain identity:

`SG-000001`

Canonical risk:

`R1`

## Qualified implementation

Canonical base used by the Grain:

`c9f5dc4b2c2f71975d8a8a6cbbcd4ba78de99823`

Final implementation head:

`9d01b6a20cf4ed8cb1db9329a1c547eabb09213d`

Merged to `main` through PR #9 at:

`da096259e1bb7d669206f2d097e56731004d7c41`

## WorkPacket

The canonical SpecGrain packet used for execution was:

- spec revision: `sha256:f86ede65c949dda5053ea103e3bd1d46da19af9e945e84c328e8b036d4b7e167`;
- packet digest: `sha256:da38d065c723cdeb73047fa0ede72025b37892dd619d1c7bc7b7f3ecc34327d1`;
- context-plan digest: `sha256:2f3ab625d8154801f712cb8d1ccb4c45b5ccf5b9fdd3bcc2dc762c5ee12b0eba`.

The packet selected only revision-bound required context:

- canonical Kernux `specs/CURRENT.md`;
- Diffcipline v1.0.0 `action.yml`;
- Diffcipline v1.0.0 `docs/PROOF-CONTRACT.md`;
- Diffcipline v1.0.0 `docs/RISK-MODEL.md`.

## Immutable Diffcipline authority

Kernux now uses:

- repository: `TheHalfMoon/Diffcipline`;
- release: `v1.0.0`;
- exact commit: `5cb1c77340b75649f6168e0e8f66479ea047ea96`;
- license: MIT.

The CI workflow pins the full commit rather than a moving branch or tag.

## Repository policy established

`.diffcipline.toml` defines:

- maximum changed files: 12;
- maximum added lines: 600;
- dependency-manifest changes: REVIEW;
- lockfile changes: REVIEW;
- untracked files: FAIL;
- explicit R0, R1, R2, and R3 verification profiles.

R0 executes formatting plus SpecGrain-state verification.

R1, R2, and R3 each execute the complete `pnpm check` repository baseline. Higher-risk task-specific security, compatibility, recovery, platform, or review evidence remains additive and cannot be replaced by the generic profile PASS.

## Negative-path qualification

Before canonical implementation, the immutable v1.0.0 CLI was exercised with configured verification but without `--run`.

Observed:

- scope: PASS;
- verification: NOT RUN;
- verdict: REVIEW;
- exit code: 1.

This demonstrated the required proof-contract invariant that NOT RUN is never PASS.

## Exact-candidate local qualification

The real candidate commit `9d01b6a20cf4ed8cb1db9329a1c547eabb09213d` was evaluated against canonical base `c9f5dc4b2c2f71975d8a8a6cbbcd4ba78de99823`.

Observed diff:

- changed files: 3;
- additions: 188;
- deletions: 0;
- dependency manifests: unchanged;
- lockfiles: unchanged;
- untracked files: 0.

Executed immutable v1.0.0 profiles:

- R0: PASS / exit 0;
- R1: PASS / exit 0;
- R2: PASS / exit 0;
- R3: PASS / exit 0.

R1-R3 each executed `pnpm check` successfully.

## Pull-request CI qualification

PR #9 exact-head workflow run:

`35293226806`

Observed on `9d01b6a20cf4ed8cb1db9329a1c547eabb09213d`:

- JavaScript / TypeScript: SUCCESS;
- Rust: SUCCESS;
- SpecGrain: SUCCESS;
- Diffcipline R1: SUCCESS.

The Diffcipline Action reported:

- verdict: PASS;
- risk: R1;
- comparison: `c9f5dc4b2c2f71975d8a8a6cbbcd4ba78de99823...HEAD`;
- changed files: 3;
- diff: +188 / -0;
- dependencies: unchanged;
- lockfiles: unchanged;
- untracked: 0;
- verification: PASS — `pnpm check`.

At final PR qualification:

- PR #9 was mergeable;
- CodeRabbit status: SUCCESS;
- open review threads: 0.

## Post-merge main qualification

Post-merge workflow run:

`35293292415`

Observed on `main@da096259e1bb7d669206f2d097e56731004d7c41`:

- JavaScript / TypeScript: SUCCESS;
- Rust: SUCCESS;
- SpecGrain: SUCCESS;
- Diffcipline R1: SUCCESS.

For the push event:

- the pull-request proof step was correctly skipped;
- the `Prove main push at R1` step executed and succeeded.

This proves both event paths of the new gate: pull-request comparison by base SHA and main-push comparison by the previous main SHA.

## Acceptance conclusion

**PROVEN** for the bounded Diffcipline-adoption outcome.

This evidence establishes a repository-owned exact-diff proof policy and working pinned CI gate. It does not establish donor provenance enforcement, third-party notice closure, product implementation, release qualification, or project completion.

## Recovery

Recovery is a normal revert of PR #9 / merge commit `da096259e1bb7d669206f2d097e56731004d7c41`. The task creates no Kernux product runtime state or user data.
