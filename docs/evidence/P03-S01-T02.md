# Evidence - KX-P03-S01-T02 (implementation, UNPROVEN)

Task: `KX-P03-S01-T02 - Implement daemon connection/reconnect projection and honest degraded states.`
Grain: `SG-000032`, state `GRAIN`, risk `R2` (medium grain risk; slices qualify at R1 floor plus R2 proof).
Status: `IMPLEMENTED_UNPROVEN`. No advancement claimed. P03 entry stays
`KX-P03-S01-T02 ONLY`; later P03 tasks stay PLANNED.
Ledgers (`specs/CURRENT.md`, `specs/tasks.md`) are intentionally untouched by this record.

## Canonical implementation range

- Pre-implementation base (canonical main): `9469a346c9ed2c63f2d50f5e999071d9fdb1bbc9`
  (T01 proof merge, PR #220, post-merge CI `35986307130` SUCCESS).
- Grain shaping PR #221 (`34658f4`, merge `61be9b6`): SG-000032 Grain with
  frozen nine-state vocabulary, frozen reconnect bounds, stale-session rules,
  and projection authority.
- Packet persistence PR #222 (`a2f8145`, merge `2f2c834`,
  post-merge CI `35990891134` SUCCESS): tracked ContextSources and WorkPacket.
- PR-A #223 head: `92d9f61d63038eabfa0dab1fde7abedcabbf78da`
  (base `2f2c834cc15f16dea76b8270f7a7e172651796e7`)
- PR-A merge: `be6cf1fb96c8d451e8d944e875cc69d05fd621c8`
  (2026-09-24T11:30:55Z, ordinary merge; post-merge CI `35993465076` SUCCESS).
- PR-B #224 head: `4b18a862c10040834a0014e024f20b7e9f6c764c`
  (base `be6cf1fb96c8d451e8d944e875cc69d05fd621c8`)
- PR-B merge: `653d9427594e11b7f3ea084934833adbb2c5c7dd`
  (2026-09-24T11:34:08Z, ordinary merge; post-merge CI `35993779871` SUCCESS).
- PR-C #225 head: `3f7994206eaf65387c2b4c933de6f5f827b8c7db`
  (base `653d9427594e11b7f3ea084934833adbb2c5c7dd`)
- PR-C merge: `1c516ea673dbca7ed41bc76b19bed4daf379e8ac`
  (2026-09-24T11:36:48Z, ordinary merge; post-merge CI `35994034679` SUCCESS).
- PR-D #226 head: `19f42e7ac6136907f4955aa63822627ae9799c5c`
  (base `1c516ea673dbca7ed41bc76b19bed4daf379e8ac`)
- PR-D merge: `bdce872d535f858f11121273b00cf926e5b0fc09`
  (2026-09-24T11:39:11Z, ordinary merge; post-merge CI `35994268276` SUCCESS).
- Combined range `9469a34...bdce872`: exactly 12 files,
  `+1877/-1`, zero npm dependencies, zero manifest drift, zero lockfile drift:
  `.specgrain/specs/SG-000032.json`, nine `apps/desktop/` paths
  (`connection-state.ts`, `reconnect-controller.ts`,
  `connection-projection.ts`, `renderer.tsx` wiring, `connection-harness.ts`,
  four test suites), and the two tracked SG-000032 packet files.
  Every path sits inside the frozen SG-000032 authorized change surface.
  The single deletion is one re-wrapped comment line in `renderer.tsx`.
- `docs/evidence/P03-S01-T02.md` is this file (IMPLEMENTED_UNPROVEN; advances nothing).

## Packet identity (frozen)

- WorkPacket digest: `sha256:a8f29a55050ca0ec14a13e539daeb3784f8bfb4c49058fefee50f2ce0969c9dc`
- Context-plan digest: `sha256:904ebb16a0fc14d860e69c620185e69c6337e7acd31069ebce50641ae29aba2e`
- Spec revision: `sha256:b6fb682050e7f433f196c9b744232b824b4c7d8fbcfec83f4ae868363bfe0031`
- SpecGrain pin: `TheHalfMoon/SpecGrain` commit `5de7d6499bb0a9e3a191fc0934399cf099d1980a`
  (archive SHA-256 `da6a8c006c3a0154e76e2bf08bcae58bb86e04f06dd7a03115275b466489eef9`)
- WorkPacket file SHA-256: `C5D35911836354D913FB4AED577B12BD5A93DCE018156F8C71140478F1EBFA67`
  (10544 bytes; context sources 6455 bytes, 17 sources).
- `prove SG-000032`: `verified=false`, `record_count=0` (correct: native
  verification is a later dependent step, no record hand-authored).
- `prove SG-000031` and earlier: preserved (prior proof untouched).
- `check .` reports valid (32 specs, 0 refining, 0 blocked).
- WorkPacket replay on Windows (`packet SG-000032`, Python 3.11.15):
  JSON-semantically identical to the tracked file; byte comparison differs by
  exactly one carriage return because the SpecGrain CLI emits CRLF line endings
  on Windows while the tracked file carries LF. Linux/macOS byte-identical
  replay (`cmp`) is a probe-gate item, not claimed here.

## IMPLEMENTATION_EVIDENCE

PR-A (#223) closed nine-state reducer (`connection-state.ts`, 264 lines;
`connection-state.test.ts`, 213 lines; `+477/-0`):
nine frozen states, ten closed events, pure total reducer (forbidden pairs
return the current state, never throw), `terminal` absorbing with
dispose-from-any-state, first-attempt degraded, degraded recovery only through
a fresh handshake, unavailable retrigger only via explicit retry or dispose,
handshake completeness/dispatch/schedule/recoverability tables, user-visible
meanings with no false serving claim.

PR-B (#224) bounded reconnect controller (`reconnect-controller.ts`, 337 lines;
shared harness `connection-harness.ts`, 106 lines; `reconnect-controller.test.ts`;
`+543/-0`): frozen 200 ms debounce, 200 ms base doubling backoff capped at
5000 ms, 10-attempt per-episode budget with exhaustion into `unavailable`,
episode reset on fresh serving handshake, disposal cancelling all timers and
suppressing late callbacks, duplicate-trigger coalescing to a single timer and
single in-flight attempt, fresh socket plus fresh handshake per attempt with
connection/attempt/handshake generation guards. Zero Electron/Node imports;
clock and attempt injected.

PR-C (#225) single-authority projection (`connection-projection.ts`, 115 lines;
`renderer.tsx` additive `authorityStatusText`, `+13/-1`;
`connection-projection.test.ts`, 117 lines): five stable error categories
(`none`, `daemon-unreachable`, `daemon-unavailable`, `daemon-incompatible`,
`connection-closed`), mechanical derivation from the controller snapshot,
dispatch permission mirroring the frozen reducer table exactly, explicit-retry
flags, constant strings only. The renderer keeps the prior `statusText` path
and adds no second boolean authority.

PR-D (#226) adversarial corpus (`connection-adversarial.test.ts`, 333 lines;
`+333/-0`): 18 scenarios over the real controller plus projection covering
recovery journeys, attempt-budget exhaustion, disposal races, stale
generations, repeated events, concurrent triggers, and race-sensitive
transitions. Tests only; zero production drift.

## LOCAL_TEST_EVIDENCE (this run, Windows, exact main `bdce872`)

- Strict `tsc` over `apps/desktop/test/tsconfig.tests.json`: PASS, exit 0
  (TypeScript 7.0.2). Full-project `tsc` over `apps/desktop/tsconfig.json`
  reports only the expected dependency-bound diagnostics (`electron`,
  `react`, Node types absent by design: zero npm dependencies); the same
  allowlisted gate pattern as T01 applies and the probe syntax gate binds it.
- Desktop tests compiled to an isolated temp dir and run with `node --test`:
  79/79 PASS (26 suites, 0 fail). Connection suites alone: 48/48 PASS
  (reducer vocabulary/determinism/corrections, controller bounds/episodes/
  coalescing, projection authority, adversarial corpus).
- `node ./tools/workspace/check.mjs`: PASS. Workspace unit tests: 2/2 PASS.
- Root `tsc --project tsconfig.json --noEmit`: PASS.
- `oxfmt --check` over the governed set: PASS (13 files).
- `oxlint` over the governed set: PASS, zero findings.
- KRP: `generate.mjs --check` PASS, `conformance.mjs` PASS (10 cases),
  `compatibility.mjs` PASS, `adversarial.mjs` PASS (20 policy, 13 wire).
- SpecGrain `check .`: PASS (valid, 32 specs).
- Rust `cargo build -p kernuxd` / `cargo test`: BLOCKED locally (MSVC
  `link.exe` absent; only the `x86_64-pc-windows-msvc` target installed, no
  Visual Studio toolchain). Authority for Rust gates is CI plus the
  Linux/macOS probe runners. Not waived; recorded below.

## CI_EVIDENCE (live GitHub truth, verified 2026-09-24)

- PR-A exact-head CI `35993214830`: 6/6 SUCCESS (Rust, JavaScript/TypeScript,
  Windows kernuxd, SpecGrain, Provenance, Diffcipline R1). Post-merge CI on
  `be6cf1f`: `35993465076` SUCCESS.
- PR-B exact-head CI `35993559120`: 6/6 SUCCESS. Post-merge CI on `653d942`:
  `35993779871` SUCCESS.
- PR-C exact-head CI `35993848453`: 6/6 SUCCESS. Post-merge CI on `1c516ea`:
  `35994034679` SUCCESS.
- PR-D exact-head CI `35994100987`: 6/6 SUCCESS. Post-merge CI on `bdce872`:
  `35994268276` SUCCESS.
- Every slice therefore carries the always-on Diffcipline R1 floor plus the
  full six-job gate at its exact head. R2 proof for the T02 risk profile runs
  in the PR-E probe (pending, not claimed).
- PR-E exact-head CI: PENDING (this PR not yet opened at time of writing).

## REAL_DAEMON_EVIDENCE

Status: NOT VERIFIED (no mock renamed as real; mock/harness suites stay
labeled as deterministic fixtures).
Local real-daemon qualification is blocked by the missing MSVC linker (same
environmental fact as T01: `link.exe` absent, `cargo build -p kernuxd` fails
at build-script linking). The qualification vehicle is the PR-E probe on
Linux and macOS runners, which will drive the real compiled `kernuxd` over
UDS with real nonce auth through the real controller path:
initial connect, real handshake success, serving state, daemon kill,
disconnect detection, degraded/transient projection, bounded reconnect,
daemon restart on the same socket path, fresh handshake, serving
restoration, episode reset, repeated disappear/return cycles, budget
exhaustion into unavailable, explicit-retry recovery, dispose during the
reconnect lifecycle, and stale-callback rejection. Results will be recorded
in the closeout section, not backfilled here.

## NATIVE_WINDOWS_EVIDENCE

Status: NOT VERIFIED beyond exact-head CI.
The `Windows kernuxd` job passed at all four slice heads and both post-merge
tips listed above. Desktop-shell and live-reconnect behavior on native
Windows is not claimed from CI alone. Native Windows qualification beyond CI
is a closeout-gate item: either a Windows probe leg or an explicit
governance disposition with founder-visible rationale. Not waived here.

## NATIVE_MACOS_EVIDENCE

Status: NOT VERIFIED (pending PR-E probe macOS-14 leg with live reconnect,
SpecGrain replay `cmp`, R2, and OCR accounting). Not claimed here.

## JEV_REVIEW_EVIDENCE (genuine, this run)

- CLI: `jev 0.3.2` (skill-bundled stdlib script) via Python 3.11.15.
- Provider: TypeSafe (`api.typesafe.ai`), model `jev-1.13.0`, key check PASS.
- Scope: exact merged slice diffs (`git show <head>`), two yes/no questions
  per slice (8/8). Exit code 1 = answer `no` (the safe direction); every
  probability below 0.25. No blocking findings. Disposition clean.

| Slice | Question | P(yes) | Answer |
| --- | --- | --- | --- |
| PR-A `92d9f61` | renderer/plugin/agent gains availability authority, dispatch, or self-enforcement | 0.06 | no |
| PR-A `92d9f61` | serving without fresh handshake, terminal resurrection, vocabulary erosion | 0.04 | no |
| PR-B `4b18a86` | parallel loops, post-disposal activity, stale socket/handshake reuse | 0.04 | no |
| PR-B `4b18a86` | bound excess (debounce/backoff/ceiling/budget) or missing exhaustion path | 0.20 | no |
| PR-C `3f79942` | renderer second truth outside the single projection | 0.05 | no |
| PR-C `3f79942` | nonce/secret/path/request-id/socket leakage in strings or logs | 0.04 | no |
| PR-D `19f42e7` | flaky/weak corpus, missing stale/disposal/budget races | 0.12 | no |
| PR-D `19f42e7` | production change, bridge widening, new dependency | 0.05 | no |

- `JEV_VERSION = jev 0.3.2 / jev-1.13.0 (TypeSafe)`
- `JEV_TARGET_REVISION = bdce872 (slice diffs 92d9f61, 4b18a86, 3f79942, 19f42e7)`
- `JEV_SCOPE = state machine, reconnect controller, projection boundary, async ownership, cancellation, cleanup, stale generations, races, terminal invariants, test adequacy, secret hygiene`
- `JEV_RESULT = 8/8 no, zero blocking findings`
- `JEV_FINDINGS = none requiring FIXED; no REJECTED_WITH_EVIDENCE; no OUT_OF_SCOPE`
- Real-daemon qualification was reviewed for adequacy as a plan (corpus
  question); the live probe itself is future evidence, not Jev input.

## ALIBABA_OCR_EVIDENCE

Status: NOT VERIFIED (pending PR-E probe legs).
Local OCR execution was deliberately deferred: running the OCR CLI over the
diff on this workstation was declined at policy review, so no local model or
accounting run is claimed. The probe workflow runs the pinned
`@alibaba-group/open-code-review@1.12.7` in Delegation Mode accounting
(`ocr delegate preview --format json --from <base> --to <target>`) on both
Linux and macOS runners over the public repository, exactly as the T01
probes did. No CodeRabbit/Qodo/Cubic/Copilot output is cited as OCR
evidence. Findings and accounting will be recorded in the closeout section.

## SEMANTIC_REVIEW (exact heads, this run)

- Reducer totality holds over the 9x10 product; every unlisted pair returns
  the current state; `terminal` absorbs all events including `dispose`.
- `serving` ignores unsolicited success/retry/timer events without leaving;
  loss and failure downgrade synchronously to `transient-disconnect`
  (no stale serving retention).
- Controller `attempt` counter spans degraded-plus-reconnecting inside one
  episode and resets only on serving success, matching the frozen
  per-episode budget text; `failures` resets on any terminal attempt outcome
  so backoff restarts cleanly per outcome class.
- `dispose` clears timers and listeners and projects `terminal`; the
  `disposed` guard plus captured episode/operation ids suppress every late
  callback; no resurrection path exists.
- Projection maps each state to exactly one status line, one error category,
  and mirrored dispatch permission; auto-retrying states do not demand
  explicit retry; only serving dispatches.
- Renderer change is purely additive (`authorityStatusText`); no existing
  projection path altered; bridge allowlist untouched; zero dependencies.
- No defects found. No repairs required; Jev independently concurs (8/8 no).

## NEGATIVE_EVIDENCE (preserved, not waived)

- Local `cargo` unusable: `link.exe` absent, no Visual Studio toolchain;
  `cargo build -p kernuxd` fails linking build scripts. Rust authority rests
  on CI plus probes (same limitation class as T01, still open).
- `pnpm` on this workstation is 11.25.0 versus the required `>=12.4.2 <13`;
  no `pnpm` command was used locally; installs and `pnpm check` run on CI
  and probe runners with pinned pnpm 12.4.2.
- GitHub API access from the default sandbox is denied (socket policy); all
  GitHub reads/writes in this run used explicit escalated execution and are
  logged as such. No state was inferred without a live response.
- WorkPacket replay on Windows is JSON-equal but not byte-identical (CLI
  CRLF vs tracked LF); Linux/macOS `cmp` remains the binding replay gate.
- Local OCR run declined at policy review (third-party code over the diff
  without explicit founder approval); evidence comes from CI probe runners.
- Open PRs at time of writing (#186, #187, #188, #200, #202) are unrelated
  DRAFT planning docs; no concurrent worker touches SG-000032.

## KNOWN_LIMITATIONS

1. Real-daemon reconnect round trip: NOT VERIFIED (probe-gated).
2. Native macOS qualification: NOT VERIFIED (probe-gated).
3. Native Windows beyond CI: NOT VERIFIED (closeout-gate item).
4. Alibaba Open Code Review: NOT VERIFIED (probe-gated).
5. Diffcipline R2 proof: NOT VERIFIED (probe-gated; R1 floor proven per slice).
6. Native SpecGrain verification record: correctly absent (`verified=false`,
   0 records); produced only after closeout merges with green post-merge CI.
7. PR-E exact-head CI and post-merge CI: PENDING (PR not yet opened).

## FINAL_QUALIFICATION

`IMPLEMENTED_UNPROVEN`. The four implementation slices are merged with exact-head
6/6 CI, deterministic suites pass 79/79 locally, Jev review is clean (8/8),
and semantic review finds no defects. Real-daemon, native-platform, OCR, R2,
exact-head-CI, and closeout gates remain open and are explicitly not claimed.
`KX-P03-S01-T02` stays NEXT; later P03 tasks stay PLANNED.

## Outstanding before PROVEN (not claimed here)

1. PR-E probe on Linux and macOS-14 (surface, replay `cmp`, full baseline,
   desktop tests 79/79, live real-daemon reconnect round trip, Diffcipline R2,
   OCR accounting, cleanliness) green at the exact PR-E head.
2. PR-E exact-head CI 6/6 SUCCESS plus post-merge CI 6/6 SUCCESS after lawful
   ordinary merge.
3. Native `verify_execution` with the pinned SpecGrain verifier binding the
   WorkPacket to observed evidence (11/11 acceptance, 26/26 required evidence,
   zero issues), producing the immutable record.
4. Protected closeout PR (`.specgrain/evidence/SG-000032/`, this record
   finalized, `specs/CURRENT.md`, `specs/tasks.md`) with its own exact-head
   CI plus zero-thread qualification, lawful merge, and post-merge CI. Only
   then may `KX-P03-S01-T02` transition NEXT to PROVEN; later P03 tasks stay
   PLANNED.

Status: `IMPLEMENTED_UNPROVEN`. `KX-P03-S01-T02` stays NEXT.
