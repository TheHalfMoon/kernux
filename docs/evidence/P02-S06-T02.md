# Evidence — KX-P02-S06-T02 (implementation, UNPROVEN)

Task: `KX-P02-S06-T02 — Add redaction-safe security audit events and secret-leak tests.`
Grain: `SG-000028`, state `GRAIN`, risk `R3` (high).
Status: `IMPLEMENTED_UNPROVEN`. No advancement claimed. P02 stays `8/10`, exit unproven.
Ledgers (`specs/CURRENT.md`, `specs/tasks.md`) are intentionally untouched by this record.

This document replaces the pre-PR implementation draft in full. That draft is
preserved in history only as an untracked working note; nothing in it is cited
as qualification.

## Canonical implementation range

- Pre-implementation base (canonical main): `fb63a4fb89b62f9e5600d62216eb3cc8e65366c4`
- PR-A (#193) head: `954f6250aba98892eae76ddba8f868dca548c810`
- PR-A merge: `f4729adcb21246505a53a86f0af1a82137390864` (2026-09-23T16:34:47Z, ordinary merge)
- PR-B (#194) head: `0826bc059098758a63d1fa4344e8b15063b29c16`
  (first pushed head `09312f0` plus one additive clippy-fix commit; never force-pushed)
- PR-B merge: `92f3929e26a18de25f9f3dbb2db8e68eec984bb9` (ordinary merge)
- Combined range `fb63a4f...92f3929`: exactly 2 files, `+757/-1`
  (`crates/kernux-secret/src/audit.rs` +749/-0, `crates/kernux-secret/src/lib.rs` +8/-1)
- No Cargo manifest drift, no lockfile drift, no `protocol/` drift, no ledger
  change across the whole range (verified empty by probe surface steps on both platforms).
- `Cargo.lock` SHA-256 at `92f3929`: `f74117077faceefba65c497b5eef7f63500e761d0833e48626b0ded80a93a7c8`

## Packet identity (frozen)

- WorkPacket digest: `sha256:453cffa03b66c5b47813a343056fbbad7ae8bd9c71743436c0475fcf420d6005`
- Context-plan digest: `sha256:10225431599a370dce89c1d6a4355802cdeb34e98860118ef202860d4cb23130`
- Spec revision: `sha256:1afaca0e329fd1b2742db53737d25f4c8de2a9f9285f36912b83139706420335`
- SpecGrain pin: `TheHalfMoon/SpecGrain` commit `5de7d6499bb0a9e3a191fc0934399cf099d1980a`
  (archive SHA-256 `da6a8c006c3a0154e76e2bf08bcae58bb86e04f06dd7a03115275b466489eef9`)
- Byte-identical WorkPacket replay (`cmp` PASS) on probe Linux and macOS-14 for
  both PR-A and PR-B heads. Local Windows replay reproduces identical content;
  the only byte difference is shell-redirect CRLF vs LF, a platform artifact of
  PowerShell `>`, not a packet mismatch. The Grain and packet were not modified.
- `prove SG-000028`: `verified=false`, `record_count=0` (correct: native
  verification is a later dependent step, no record hand-authored).
- `prove SG-000027`: `verified=true`, `record_count=1` (prior proof preserved).

## PR-A qualification (head 954f625)

- Exact-head CI run `35884729510`: 6/6 SUCCESS (Rust, JS/TS, SpecGrain,
  Diffcipline R1, Provenance, Windows kernuxd). Preserved, never re-run on an unchanged head.
- Probe run `35889111829` (workflow `pr193-audit-core-qualification`):
  Linux job `107276793935` SUCCESS, macOS job `107276793393` SUCCESS, every step green.
- Diffcipline R3 with `run-verification=true`, action
  `TheHalfMoon/Diffcipline@5cb1c77340b75649f6168e0e8f66479ea047ea96` (v1.0.0):
  base `fb63a4f`, comparison `fb63a4f...HEAD` (HEAD pinned to TARGET by the
  prior step), changed 2 files, `+526/-1`, dependencies unchanged, lockfiles
  unchanged, untracked 0, scope PASS, verification PASS (`pnpm check`),
  final verdict PASS — on Linux and macOS-14 independently.
- Full baseline `pnpm check` PASS both platforms (workspace, KRP generate /
  conformance 10 cases / compatibility / adversarial 20+13, JS 2/2, Rust suites).
- Focused `kernux-secret` regression PASS; focused
  `kernux-policy`/`kernux-store`/`kernuxd`/`kernux-identity` regression PASS, both platforms.
- Genuine Jev (`jev 0.3.2`, provider TypeSafe, model `jev-1.13.0`,
  base `fb63a4f`, head `954f625`): 8/8 answer no — plaintext-leak 0.05,
  bearer-handle 0.02, value-duplication 0.04, error-echo 0.08,
  privilege-widening 0.06, side-effecting-IO 0.02, shell-construction 0.02,
  reveal-implied 0.02. No blocking findings. Disposition clean.
- Alibaba OpenCodeReview `1.12.7` (`85cecfe`, linux/amd64 and darwin/arm64),
  `delegate preview` (Delegation Mode accounting only): merge_base `fb63a4f`,
  total 2 / reviewable 2 / excluded 0, `+526/-1`, `excluded_files: []`.
  No model-backed review claimed; no blocking findings exist in accounting output.
- Native macOS: `macos-14-arm64`, ProductVersion `14.8.9`, BuildVersion `23J631`, arm64.
- Windows: exact-head CI `Windows kernuxd` SUCCESS (local `cargo` blocked by
  missing `link.exe`; CI is the Rust authority per precedent).
- Semantic review of exact head: allow projection exact; denial mapping covers
  all 21 `BrokerError` variants with distinct kebab-case codes; grant-less
  denials invent no grant id; constructors pure (`core::fmt` +
  `kernux_policy::CanonicalUtcSecond` only); no plaintext/value in events;
  no new authority; KRP frozen; no provider execution; no daemon widening;
  no dependency drift. Three non-blocking observations recorded and kept:
  (1) canonical string renders egress with `{:?}`; (2) `corresponds_to_allow`
  does not compare egress; (3) Debug is a safe subset omitting egress and
  envelope labels. Private fields with no mutation API make (2) defense-in-depth
  only; `authorizes_use` is unconditionally false.
- Reviews: 0 formal. Threads: 0 blocking (only bot billing/skip notices).
- Post-merge CI run `35889733273` on `f4729ad`: 6/6 SUCCESS.

## PR-B qualification (head 0826bc0)

- Scope: 1 file (`crates/kernux-secret/src/audit.rs`), `+232/-1`, test-only
  additions plus the `SecretValue` test import. Under the R3 600-line budget.
- Restored from the preserved full-audit reference (`sg28-audit-full-747.rs`,
  747 lines, LF) with two documented deviations: (a) synthetic Slack corpus
  entry de-patterned after push-protection trip (see negative evidence);
  (b) `DenialCase` alias after the clippy failure (see negative evidence).
- Exact-head CI run `35890939606`: 6/6 SUCCESS.
- Probe run `35891315591` (workflow `pr194-audit-corpus-qualification`):
  Linux job `107284193855` SUCCESS, macOS job `107284194336` SUCCESS, every step green.
- Diffcipline R3, same action pin: base `f4729ad`, 1 file, `+232/-1`,
  dependencies/lockfiles unchanged, untracked 0, verification PASS, verdict
  PASS — on Linux and macOS-14 independently.
- WorkPacket replay `cmp` PASS both platforms; proof states unchanged
  (SG-000028 false/0, SG-000027 true/1).
- `kernux-secret` suites: 60/60 PASS on Linux and macOS-14, including all 10
  audit tests (3 PR-A + 7 PR-B): allow projection, denial codes, grant-less
  denial, real-admission denial correspondence (6 cases), consumption without
  plaintext (no `expose()` on the audit path), deterministic renderings with
  stable key order, frozen KRP vocabulary binding, never-authorize (incl.
  op-id correspondence sensitivity), timestamp rejection, 14-entry leak corpus
  over all 3 events x 3 surfaces plus value-Debug and error renderings.
- Genuine Jev (same CLI/provider/model, base `f4729ad`, head `0826bc0`):
  8/8 answer no — plaintext-leak 0.35 (elevated by the synthetic corpus
  strings themselves, still no), authorize/reveal 0.03, value-duplication
  0.05, error-echo 0.08, privilege-widening 0.08, side-effecting-IO 0.31
  (elevated by test vocabulary such as provider/persistence/HostCommand,
  still no), shell-construction 0.02, credential/side-effect 0.03.
  No blocking findings. Disposition clean.
- Alibaba OpenCodeReview `1.12.7` (`85cecfe`) both platforms, Delegation Mode
  accounting only: merge_base `f4729ad`, total 1 / reviewable 1 / excluded 0,
  `+232/-1`, `excluded_files: []`.
- Native macOS-14 arm64 PASS; Windows via exact-head CI PASS.
- Semantic review: the 7 restored tests match the authorized PR-B surface;
  `timestamp_failure_blocks_before_admission` holds at the type level
  (constructors require parsed `CanonicalUtcSecond`; sequencing stays with the
  daemon slice by packet design); the corpus de-patterning keeps identical
  matcher coverage (raw + hex substring); the `DenialCase` alias is semantics-preserving.
- Reviews: 0 formal. Threads: 0 (0 PR comments).
- Post-merge CI run `35891797823` on `92f3929`: 6/6 SUCCESS.

## Security invariants

- `audit-before-side-effect` and `audit-failure-never-admits` hold at the type
  level for this layer (event-first construction; no timestamp means no event).
  Full daemon-path proof stays with the daemon-execution slice by packet design.
- Redaction, handle-based use, no plaintext persistence, no reveal authority,
  no silent copying, and no secret-bearing Debug/Display are preserved and
  covered by the leak corpus on both platforms.

## Negative evidence (preserved, not waived)

- Clippy `type_complexity` denial on the first PR-B head (`-D warnings`):
  exact-head CI `35890302357` failed (Rust + Diffcipline R1 jobs) and probe
  `35890482058` failed at Full baseline on `let cases:
  [(fn(&mut BrokerRequest), BrokerError); 6]`. Fixed structurally with a
  `DenialCase` alias (2 added lines); no `#[allow]` was added and no gate was
  weakened. Re-qualified heads green everywhere.
- GitHub push protection (GH013, Slack API Token pattern) rejected the first
  PR-B push at the synthetic corpus entry `xoxb-123456789012-...` (line 714).
  Nothing was pushed. The entry was replaced with
  `slack-synthetic-token-0123456789-...` (same matcher coverage, clearly
  synthetic). One line, no scope change.
- Probe pin staleness: probe `35891002014` failed at the surface step (exit 2)
  because TARGET moved to the clippy-fix head while the workflow still asserted
  230 insertions (actual 232). Fixed the pin (230 to 232, 2 lines), re-ran green.
- Local Windows limitations: `link.exe` absent (no local cargo test/clippy);
  C: hit 0 bytes free during the session (npx OCR 1.12.7 install failed ENOSPC;
  space later recovered); PowerShell `>` redirect appends CRLF (packet-replay
  comparison done byte-safe instead); `ocr` not on local PATH and only 1.12.9
  present globally, so OCR ran version-pinned on probes only. All affected gates
  were moved to authorized runners per policy, never waived.
- Jev foreground calls intermittently exceeded the 10s exec cap and detached
  launches initially split the question argv (fixed with embedded quotes);
  all recorded scores come from complete, well-formed runs.
- Ambient qodo billing-blocked, CodeRabbit auto-skip, and cubic reviewer-only
  states are limitations, not approvals.

## Outstanding before PROVEN (not claimed here)

1. Native `verify_execution` with the pinned SpecGrain verifier binding the
   WorkPacket to this observed evidence (7/7 acceptance, 21/21 required
   evidence, zero issues), producing the immutable record.
2. Separate protected closeout PR (`.specgrain/evidence/SG-000028/`, this
   record finalized, `specs/CURRENT.md`, `specs/tasks.md`) with its own
   exact-head CI + R3 + Jev + OpenCodeReview + platform + semantic + zero-thread
   qualification, lawful merge, and post-merge CI. Only then may
   `KX-P02-S06-T02` transition NEXT to PROVEN; P02 exit stays unproven.

Status: `IMPLEMENTED_UNPROVEN`. `KX-P02-S06-T02` stays NEXT. P02 stays 8/10.
