# Evidence — KX-P02-S05-T02

## Task and closure state

`KX-P02-S05-T02 — Implement user-friendly Safe/Standard/Developer/Autonomous/Custom profiles compiling to granular grants.`

SpecGrain: `SG-000025 — KX-P02-S05-T02 — Compile permission profiles to bounded granular Grants`.

Risk: `R3`.

The bounded implementation chain is merged and its final implementation revision is post-merge qualified on `main`.

This document records the macro-task implementation evidence and the packet-recovery boundary. It does not itself claim native proof. No later P02 task is authorized by this document, and P02 phase exit remains unproven.

Final implementation merge:

`d6e42a3ecccf9a23f5acb11afd97cf34a9caa6c9`

Final implementation post-merge workflow:

`35627158007` — six jobs SUCCESS.

## Frozen SG-000025 identities

- semantic Spec revision: `sha256:033563155d6258f9ceaf7bc0defd1cfc0ceddbf613d1f249f8c56e958b16edfb`;
- original WorkPacket identity: `sha256:b022c2f177a3ce59586f05603427659e2c41a6443947061eb59f8de2b153679e`;
- context-plan identity: `sha256:62ac40ed2cfea821d47ae7caeb3bbe07d839a09f7d6e718fe280c0ef465cac00`;
- original context accounting: 11 required sources, 48,517 source bytes, `12,133 / 18,000` tokens;
- tracked ContextSource records: 4,491 bytes, file SHA-256 `sha256:ec5e40b710251c0b76546f71ab8120f19f97ed2cc13b76c05170e1be8ce427d7`;
- tracked WorkPacket bytes: 13,285 bytes, file SHA-256 `sha256:2fedcd3ed6591de8bf68f0cdad96422fb22c8af4f7eff2a99f7bce34dfac8486`;
- Git blobs: ContextSource `8930ff8a48122c54d28454f7401455b95918965b`, WorkPacket `08dc8da4299e81ada95eb2feaf8a914d0c262350`;
- acceptance checks: 14;
- required evidence checks: 19.

The original ContextSource records and WorkPacket bytes were generated on clean post-refinement canonical main before any permission-profile implementation mutation, are tracked, and replay byte-identically through the repository-pinned SpecGrain CLI. The non-imported, non-invented recovery state that blocked SG-000023 does not apply to this Grain.

## Qualified implementation chain

| Slice | Exact head | Exact-head CI | Review | Merge | Post-merge CI |
| --- | --- | --- | --- | --- | --- |
| PR #154 — SG-000025 refinement | `96709d2047c0a53d8ba7d2a37f5e2212fb1094b2` | — | — | `1fa8c39d608c918c5ef327bc77e1aca41a6166fc` | `35590784297` |
| PR #155 — packet persistence | `c1da584cbb37664e837063f8752b35bc14406c18` | `35622293692` | `5268932765` | `9177faa18a415099549bdb63cc78faca77fd2dc3` | `35623546558` |
| PR #161 — profile vocabulary and preset ceilings | `68d4edb9ec081f13672a27bb01e5bae83006d6ef` | `35623895107` | `5269086553` | `7f71124328f846383b76e5823ec0e4ab8c931e8c` | `35624966977` |
| PR #162 — profile compiler | `fc9c448d04f0ebed2c9a49a1b119b53570183151` | `35625323534` | `5269204828` | `8b1e0b35195a17d0ecde716dc70ed222dcd3d180` | `35626090366` |
| PR #163 — adversarial corpus | `b7038e7b5dc59c26b43f0fef66c26cd53b1f0b9d` | `35626652203` | `5269296040` | `d6e42a3ecccf9a23f5acb11afd97cf34a9caa6c9` | `35627158007` |

Every dependent slice preserved its own exact failure history and did not relabel negative, cancelled, review-only, or obsolete-head results as PASS.

## Packet persistence

PR #155 persisted exactly the two authorized SG-000025 packet-evidence paths and nothing else. Both tracked files are byte-for-byte identical to the pinned production generation run, and replay through `tools/specgrain/run.py packet SG-000025` reproduces the tracked WorkPacket byte-for-byte at the merged head.

Qualification:

- linux probe job `106409144958` and native macOS probe job `106409144684` from run `35622620107` — SUCCESS;
- full `pnpm check` — PASS on linux and macOS;
- Diffcipline R1 and R3 on the exact two-file slice — PASS, `+113 / -0`, `Scope PASS`, `Untracked 0`;
- Alibaba OpenCodeReview v1.12.7 Delegation Mode accounting — PASS;
- exact-head six-job CI `35622293692` — 6/6 SUCCESS;
- exact-head review `5268932765` — no finding, zero unresolved threads;
- post-merge six-job CI `35623546558` — 6/6 SUCCESS.

## Profile vocabulary and preset ceilings

PR #161 established the closed profile vocabulary, the compile-error vocabulary, the trusted intent and rule records, the bounded `ProfileGrantCandidate` with its canonical wire rendering and `ValidatedGrant` round trip, the explicit `Safe`/`Standard`/`Developer` preset tables, the preset hard-deny list, and `preset_ceiling`.

Qualification:

- linux probe job `106414215376` and native macOS probe job `106414215685` from run `35624145133` — SUCCESS;
- full `pnpm check` and focused `cargo test --locked -p kernux-policy --all-targets` — PASS on linux and macOS;
- Diffcipline R1 and R3 on the exact two-file slice — PASS;
- Alibaba OpenCodeReview v1.12.7 Delegation Mode accounting — PASS;
- exact-head six-job CI `35623895107` — 6/6 SUCCESS;
- exact-head review `5269086553` — no finding, zero unresolved threads;
- post-merge six-job CI `35624966977` — 6/6 SUCCESS.

## Profile compiler

PR #162 added the pure compiler entry point. It fails closed on untrusted provenance, wildcards, non-canonical or missing identity, unbounded budgets, inverted or over-long windows, and constraint disagreement; it derives the authoritative consequence from the existing classifier rather than from the profile; and `Custom` derives its ceiling and constraint ceiling by intersecting every matching trusted rule.

Qualification:

- linux probe job `106419135208` and native macOS probe job `106419135604` from run `35625615854` — SUCCESS;
- full `pnpm check` and focused `cargo test --locked -p kernux-policy --all-targets` — PASS on linux and macOS;
- Diffcipline R1 and R3 on the exact slice — PASS;
- Alibaba OpenCodeReview v1.12.7 Delegation Mode accounting — PASS;
- exact-head six-job CI `35625323534` — 6/6 SUCCESS;
- exact-head review `5269204828` — no finding, zero unresolved threads;
- post-merge six-job CI `35626090366` — 6/6 SUCCESS.

## Adversarial corpus

PR #163 added the fail-closed corpus: the full core-action family set against every preset, untrusted provenance for every profile, trusted-consequence escalation, wildcard and provider-shaped vocabulary bypass attempts, resource-authority confusion, exact `Custom` rule matching with intersection, intent-bound failures, extension handling with and without a trusted floor, and preset hard-deny coverage.

Qualification:

- linux probe job `106423566072` and native macOS probe job `106423566480` from run `35626962427` — SUCCESS;
- full `pnpm check` and focused `cargo test --locked -p kernux-policy --all-targets` — PASS on linux and macOS;
- Diffcipline R1 and R3 on the exact slice — PASS;
- Alibaba OpenCodeReview v1.12.7 Delegation Mode accounting — PASS;
- exact-head six-job CI `35626652203` — 6/6 SUCCESS;
- exact-head review `5269296040` — no finding, zero unresolved threads;
- post-merge six-job CI `35627158007` — 6/6 SUCCESS.

## Preserved negative evidence

Two exact heads were qualified and failed. Neither was relabeled PASS and neither was rerun-to-green without a content change.

1. `ac7f1e1` (compiler slice, first candidate) failed Rust, Windows kernuxd, and Diffcipline R1 with `error[E0716]`: a new test bound a temporary resource URI to a value that outlived the statement. CI run `35625021988`. Corrected by binding the resource to a named local before building the intent.
2. `1086bfd` (adversarial slice, first candidate) failed Rust, Windows kernuxd, and Diffcipline R1 with two assertion mismatches: `UnknownConsequence` instead of `ActionNotPermittedByProfile` for an extension action without a trusted floor, and `ActionNotPermittedByProfile` instead of `ConsequenceExceeded` for escalated facts asserted against an action the profile does not cover. CI run `35626180274`. Corrected by aligning the expectations with the compiler's documented evaluation order and by asserting escalated facts against an action each profile actually covers.

## Full implementation range

The implementation-authority range for this Grain, after the qualified SG-000025 refinement merge `1fa8c39d608c918c5ef327bc77e1aca41a6166fc`, is:

`1fa8c39d608c918c5ef327bc77e1aca41a6166fc..d6e42a3ecccf9a23f5acb11afd97cf34a9caa6c9`

The paths changed inside that range are exactly:

1. `crates/kernux-policy/src/lib.rs`;
2. `crates/kernux-policy/src/profile.rs`;
3. `docs/evidence/SG-000025-context-sources.json`;
4. `docs/evidence/SG-000025-work-packet.json`.

All four are inside the frozen SG-000025 authorized change surface. The remaining authorized surface paths are governance, proof, and canonical-ledger surfaces:

- `.specgrain/specs/SG-000025.json` — CLI-emitted during refinement (PR #154);
- `docs/evidence/P02-S05-T02.md` — this packet;
- `.specgrain/evidence/SG-000025/*.json` — the native proof record;
- `specs/CURRENT.md` and `specs/tasks.md` — canonical transition.

## Native SpecGrain verification

Pinned native verification checked out the canonical repository and loaded the tracked original SG-000025 WorkPacket from `docs/evidence/SG-000025-work-packet.json`. It independently observed the implementation range:

`1fa8c39d608c918c5ef327bc77e1aca41a6166fc..d6e42a3ecccf9a23f5acb11afd97cf34a9caa6c9`

as exactly four implementation-authority paths:

1. `crates/kernux-policy/src/lib.rs`;
2. `crates/kernux-policy/src/profile.rs`;
3. `docs/evidence/SG-000025-context-sources.json`;
4. `docs/evidence/SG-000025-work-packet.json`.

Native `verify_execution` result:

- implementation revision: `git:d6e42a3ecccf9a23f5acb11afd97cf34a9caa6c9`;
- packet digest: `sha256:b022c2f177a3ce59586f05603427659e2c41a6443947061eb59f8de2b153679e`;
- execution result digest: `sha256:4b510819826c6825f2bcefa6cf5ff2eea90b51f6aab463b8436d8d87d88a2088`;
- immutable evidence record: `sha256:e0a77a108922a591f6934fb1fec1fde70cdfabae93f803449223d3ce6d0f91fa`;
- `verified=true`;
- `issues=[]`;
- acceptance checks: 14/14 PASS;
- required evidence checks: 19/19 PASS.

The generated evidence bytes are persisted at:

`.specgrain/evidence/SG-000025/e0a77a108922a591f6934fb1fec1fde70cdfabae93f803449223d3ce6d0f91fa.json`

The generated file is 18,763 bytes with file SHA-256:

`sha256:6c9f5aa637623a3ab3c4664d0c79afa3f34b8a73f11587ffe84b5057a9c27313`

## Protected closeout

The protected closeout-evidence PR #164 carried this packet and merged normally.

- exact head: `ad1ecac9de882ed8a49b4c11e63119346c423517`;
- exact-head CI: `35627524278` — 6/6 SUCCESS;
- linux qualification job `106427045000` and native macOS qualification job `106427044604` from run `35628011667` — SUCCESS;
- exact-head review: `5269389086` — no finding and zero unresolved threads;
- merge: `d480d5abf1ae5525bf38f2a2c15dda616d9640d1`;
- post-merge CI: `35628296835` — 6/6 SUCCESS.

The first probe for that slice, run `35627602583`, remains preserved as FAIL: it asserted `specgrain prove SG-000025` as a passing gate before any evidence record exists. It was corrected by asserting the deliberate pre-proof state instead of relabeling the failure as PASS.

## Canonical transition

This closeout advances exactly one macro task and one successor:

- `KX-P02-S05-T02` — `PROVEN`;
- `KX-P02-S06-T01` — `NEXT`;
- `KX-P02-S06-T02` — `PLANNED`;
- P02 progress — `7 / 9`;
- P02 phase exit — unproven.

Do not:

- mark `KX-P02-S05-T02` `PROVEN` before native SpecGrain verification returns `verified=true` with zero issues;
- advance `KX-P02-S06-T01` before the closeout and canonical transition gates complete;
- hand-author or edit the native evidence record;
- weaken local macOS, Diffcipline, OpenCodeReview, exact-head, post-merge, or native SpecGrain gates.
