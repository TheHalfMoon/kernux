# Evidence — KX-P02-S03-T01

## Task

`KX-P02-S03-T01 — Implement SQLite metadata store, schema versioning, WAL/migration and corruption/recovery fixtures.`

SpecGrain:

`SG-000021 — KX-P02-S03-T01 — Establish SQLite metadata store and migration/recovery boundary`

Risk: `R3`.

## Current closure state

The bounded SG-000021 implementation is merged and qualified on `main`. Native SG-000021 verification and the proof-bearing protected closeout are still pending.

`KX-P02-S03-T01` therefore remains canonically `NEXT`.

This closeout does **not** authorize `KX-P02-S04-T01` or any later P02 work until native verification, proof-bearing protected merge, post-merge qualification, and a separate canonical transition complete.

Final qualified implementation revision:

`3bf286402b70790beb997bd555cdf1b064f0492d`

Final implementation post-merge workflow:

`35452549806` — all five protected jobs plus additive native `Windows kernuxd` SUCCESS on the exact revision above.

## SpecGrain binding

The governing Grain remains exactly bound to:

- spec revision: `sha256:5134113af17675a96bcf16f2fb6b25ac0cdc05792cb0eb04e63fa460e5142268`;
- context-plan digest: `sha256:6ee186049f55d234cfc2083b1caf37f693c663bb4f58473d7f506b73c4be4421`;
- WorkPacket digest: `sha256:ef1e49bb4c083ecd84c63991276cd9bcf588fa49340587b6824134ed3d21a379`;
- context sources: 19 required sources / 17,224 tokens within the 20,000-token budget;
- acceptance criteria: 15;
- required evidence IDs: 26;
- authorized change surface: 10 paths.

The original WorkPacket remains available at `/tmp/kernux-sg21-packet.json` and its normalized digest matches the value above. No replacement WorkPacket is generated from post-implementation source bytes.

## Grain refinement

PR #107 — `docs: refine metadata store grain`

- exact head: `3eac71b5e241468f69934cd5f3ec1a18e2395b40`;
- exact-head workflow: `35443816802` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUS_pQ` — no findings;
- merge: `8cbb06b08657cf6e7e66d22ab5a65165168e00e6`;
- post-merge workflow: `35443897676` — six jobs SUCCESS.

The refinement froze the metadata-only SQLite boundary, dependency-before-code sequencing, exact WAL/migration/integrity requirements, full-u64 Event sequence representation, and explicit exclusions for Artifact CAS bytes, policy evaluation, secret plaintext, daemon DB-path wiring, and later P02 authority.

## Dependency admission

PR #108 — `build: admit metadata store dependencies`

- exact head: `19250c66c16d84dddf8e10d93deef5476f775642`;
- exact-head workflow: `35444238101` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUU7Cg` — no findings;
- merge: `3980a657ec74abc77a487a2d757fa9130ccca6d2`;
- post-merge workflow: `35444323549` — six jobs SUCCESS.

Exact direct dependencies:

- `rusqlite = 0.40.2`, `default-features = false`, direct feature `bundled` only;
- `uuid = 1.26.1`, `default-features = false`, direct feature `std` only.

The direct/transitive license and bundled SQLite observations are recorded in `docs/evidence/P02-S03-T01-DEPENDENCY-GATE.md`.

The repository dependency validator currently reports:

`Dependency admission: PASS (11 direct external dependencies)`.

The validator intentionally failed before the required UUID exception evidence existed. That earlier result remains FAIL and is not qualification evidence.

No store semantics landed before dependency admission merged and post-merge qualification succeeded.

## Schema, migration, and WAL core

PR #109 — `feat: add metadata store schema core`

- exact head: `6735a6faff367fec96156a47d30c5edbd5e252a7`;
- exact-head workflow: `35444547301` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUWF_g` — no findings;
- merge: `3c7b3706fe2a3250f58fe637711139dc681ceee7`;
- post-merge workflow: `35444651248` — six jobs SUCCESS.

The core establishes and verifies on filesystem databases:

- busy timeout = 5000 ms;
- `foreign_keys=ON`;
- `journal_mode=WAL`;
- `synchronous=FULL`;
- `trusted_schema=OFF`;
- `wal_autocheckpoint=1000`.

Schema version 1 uses both `PRAGMA user_version=1` and exact ledger row `(1, "metadata-v1")`.

Fresh `0 -> 1` migration executes atomically in an IMMEDIATE transaction. Existing version 1 validates exact ledger and authorized table surface. Partial version-0 schemas, unknown future versions, and migration failures reject fail-closed rather than being silently completed, downgraded, or recreated.

The v1 user-table set is exactly:

- `projects`;
- `tasks`;
- `work_units`;
- `runs`;
- `agent_sessions`;
- `runtimes`;
- `artifacts`;
- `evidence`;
- `events`;
- `grants`;
- `adapter_config_refs`;
- `schema_migrations`.

No Artifact blob table, generic secret-value table, policy-decision table, remote/cloud table, or daemon DB-path contract was introduced.

## Canonical metadata and revision semantics

PR #110 — `feat: add canonical metadata operations`

- exact head: `d66e5fec0e61530b53a07adf3e183fac47492322`;
- exact-head workflow: `35444938082` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUW11Q` — no findings;
- merge: `0b1383bc09a4c7e5cc17591e5111a2870f410004`;
- post-merge workflow: `35445035184` — six jobs SUCCESS.

Canonical IDs accept only exact lowercase hyphenated RFC 9562 UUIDv7 text with RFC variant. Uppercase, compact, braced, whitespace-bearing, nil, non-v7, and alternate representations reject rather than normalize.

Revision semantics are exact positive u32:

- initial revision = 1;
- zero rejects;
- accepted compare-and-swap advances exactly one;
- stale expected revision rejects without write;
- `u32::MAX` cannot advance and never wraps.

Revisioned metadata covers Project, Task, WorkUnit, and Runtime.

Run, AgentSession, Evidence, and issued Grant metadata are insert-once immutable revision-1 facts. Persistence of a Grant does not evaluate or grant authority.

## Artifact and adapter metadata

PR #112 — `feat: add artifact and adapter metadata`

- exact head: `754b20d14f757ad69f195d95ed3fea61f79b0de5`;
- exact-head workflow: `35445347306` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUXx-A` — no findings;
- merge: `8fb3a1b0e0b629b562a805b867391a3b8a7c3639`;
- post-merge workflow: `35445452378` — six jobs SUCCESS.

Artifact metadata enforces:

- canonical Artifact UUIDv7 identity;
- positive revision compare-and-swap;
- exact lowercase 64-hex SHA-256 when known;
- digest unknown-to-known transition at most once;
- digest invariance after known;
- nonnegative SQLite-bounded size;
- bounded media/retention metadata;
- redacted digest `Debug` output.

Adapter configuration persistence stores only bounded owner/type/reference metadata. No password, bearer-token, private-key, credential-value, or generic secret-value persistence API exists.

No Artifact bytes, CAS ingest/read, deduplication, GC, or filesystem content layout exists in this task.

## Artifact byte-identity corrective

PR #114 — `fix: preserve artifact size identity`

- exact head: `5324e306f72f7e17e71d5e37f3aa7699af8f1254`;
- exact-head workflow: `35445774048` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUYtwg` — no findings;
- merge: `a28c4e82cb87686f984c056762b924588171cebc`;
- post-merge workflow: `35445875592` — six jobs SUCCESS.

Artifact `size_bytes` now follows byte-identity semantics:

- unknown may become known once;
- known size may remain exactly the same;
- known size cannot change or be cleared for the same Artifact ID;
- rejected size mutation does not advance revision.

Stale overlapping PR #113 was closed without merge and is not qualification evidence.

## Transactional Event append core

PR #115 — `feat: add transactional event append core`

- exact head: `6317805d68fe1a7714c4bc16f514dc27a37861d8`;
- exact-head workflow: `35446921457` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUtlhw` — no findings;
- merge: `3fbd3cb4814010869aaa243a1e73a52a381525b9`;
- post-merge workflow: `35447047056` — six jobs SUCCESS.

Event append:

- executes under an IMMEDIATE SQLite transaction;
- accepts canonical Event ID, validated EventType, typed StreamRef, and expected predecessor;
- never accepts caller-controlled `stream_seq`;
- assigns sequence 1 with no predecessor for the first event;
- requires the exact current stream tip for every successor;
- increments exactly one;
- rejects overflow;
- inserts immutable Event rows only;
- validates the frozen lowercase dotted Event type grammar;
- stores Project/Task/Runtime exact positive owner revision metadata;
- treats Run as immutable revision 1;
- orders by owner kind + owner ID + explicit sequence, never UUID/time order.

`stream_seq` is stored as an exact fixed-width 8-byte big-endian BLOB, preserving the complete positive u64 domain and lexicographic numeric ordering without narrowing through SQLite signed INTEGER.

## Event adversarial expansion

PR #116 — `test: expand event append adversarial coverage`

- exact head: `69fe5ebc86871a66b19c547db0f045e45e4d353f`;
- exact-head workflow: `35447197605` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUdBsw` — no findings;
- merge: `94ec2d728ca34795cacc6687d2472a551a4cd116`;
- post-merge workflow: `35447292197` — six jobs SUCCESS.

The test-only expansion proves:

- owner revision changes do not reset one owner stream sequence;
- first-event predecessor misuse rejects, including cross-stream predecessor input;
- duplicate Event IDs reject without a new position;
- persisted sequence gaps reject future append;
- stored cross-stream predecessor corruption rejects future append;
- malformed persisted Event metadata rejects future append;
- UUID lexical/time order never controls canonical sequence.

Production Event code and public API bytes were unchanged in PR #116.

## Integrity and recovery

PR #117 — `feat: add metadata integrity and recovery fixtures`

- exact head: `0ca8f7846214968a6378f86e70067d22d24794d5`;
- exact-head workflow: `35452434061` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUvX3Q` — no findings;
- merge: `3bf286402b70790beb997bd555cdf1b064f0492d`;
- post-merge workflow: `35452549806` — six jobs SUCCESS.

Existing non-empty v1 databases are checked read-only before normal writable/WAL open:

1. exact user version;
2. exact migration ledger/schema surface;
3. `PRAGMA integrity_check` returning only `ok`;
4. `PRAGMA foreign_key_check` returning no violation rows.

Fresh databases receive the same integrity validation after migration and connection-policy/schema verification.

`Store::verify_integrity()` is explicit and non-destructive.

Executable recovery fixtures prove:

- valid databases pass integrity and foreign-key checks;
- committed WAL-backed metadata survives close/reopen;
- an uncommitted IMMEDIATE transaction does not survive close/reopen;
- a deliberately failing migration rolls back atomically;
- future schema rejects while preserving original database bytes;
- foreign-key corruption rejects during read-only preflight and preserves original database bytes;
- a deterministic corrupt-file fixture rejects without delete/recreate/replacement and preserves original bytes;
- integrity error text is typed, generic, and contains no row payload or SQL data values.

Production recovery code contains no automatic delete, recreate, repair, rename, copy, VACUUM, REINDEX, backup, or replacement behavior.

## Cross-platform executable evidence

Final-main local macOS qualification on `3bf286402b70790beb997bd555cdf1b064f0492d`:

`cargo test --locked -p kernux-store --all-targets` — 46/46 PASS.

Final-main Ubuntu `Rust` job in workflow `35452549806`:

- `kernux-store` compiled and executed natively on Ubuntu;
- 46/46 tests PASS.

Final-main native Windows `Windows kernuxd` job in workflow `35452549806`:

- `kernux-store` Clippy executed on `windows-latest`;
- `kernux-store` tests executed natively on Windows;
- 46/46 tests PASS.

Cross-compilation is not substituted for Windows runtime evidence.

## Implementation range and scope

The final SG-000021 implementation range is:

`8cbb06b08657cf6e7e66d22ab5a65165168e00e6..3bf286402b70790beb997bd555cdf1b064f0492d`

Exactly eleven paths changed:

- `.github/workflows/ci.yml`;
- `Cargo.lock`;
- `Cargo.toml`;
- `crates/kernux-store/Cargo.toml`;
- `crates/kernux-store/src/artifact.rs`;
- `crates/kernux-store/src/event.rs`;
- `crates/kernux-store/src/lib.rs`;
- `crates/kernux-store/src/metadata.rs`;
- `crates/kernux-store/src/recovery.rs`;
- `docs/evidence/P02-S03-T01-DEPENDENCY-GATE.md`;
- `third_party/dependencies/approved.json`.

All are inside SG-000021's authorized change surface.

No KRP schema/generated contract path, T02 authentication/local-transport path, or SG-000020 identity path changed in this implementation range.

## Protocol, auth, identity, and dependency regression

On final implementation main:

- dependency validator — PASS, 11 direct external dependencies;
- KRP generated contracts — PASS;
- KRP shared fixture conformance — PASS;
- KRP compatibility matrix — PASS;
- KRP adversarial conformance — PASS;
- T02 authentication/lifecycle/local-transport suites — PASS;
- SG-000020 identity tests — PASS;
- `kernux-store` final local corpus — 46/46 PASS;
- full `pnpm check` — PASS in final implementation qualification and CI;
- all five protected CI contexts — SUCCESS;
- additive `Windows kernuxd` — SUCCESS.

## Security and scope boundary

SG-000021 remains limited to repository-owned local SQLite metadata semantics and their exact dependencies/CI support.

It does not implement or claim:

- Artifact CAS bytes, streaming content storage, digest-addressed filesystem layout, deduplication, or GC;
- policy evaluation, Grant matching/admission, revocation/expiry/use accounting, delegation enforcement, or permission profiles;
- secret plaintext, private keys, bearer tokens, passwords, cookies, reusable browser credentials, provider API keys, credential stores, SQLCipher, TPM/HSM, or secure-enclave integration;
- daemon state-root selection, DB-path argv/environment/KRP commands, or renderer-direct database access;
- project export/import, general backup product workflows, remote/cloud replication, browser profiles, or later P02 operations;
- privileged filesystem/process/PTY operations, sandbox/browser control, or remote trust;
- P02 phase completion.

Ordinary SQLite metadata is not claimed to be encrypted at rest.

## Review discipline

Alibaba OpenCodeReview is the review/accounting framework for SG-000021. CodeRabbit, Qodo, and Cubic are not qualification evidence.

Bounded slice accounting:

- PR #107 refinement: 1/1 reviewable, 0 excluded;
- PR #108 dependency admission: 5 reviewable / 2 excluded; `Cargo.lock` and Markdown evidence manually reviewed completely;
- PR #109 schema core: 1/1 reviewable, 0 excluded;
- PR #110 canonical metadata: 2/2 reviewable, 0 excluded;
- PR #112 Artifact/adapter metadata: 3/3 reviewable, 0 excluded;
- PR #114 Artifact size corrective: 2/2 reviewable, 0 excluded;
- PR #115 Event core: 2/2 reviewable, 0 excluded;
- PR #116 Event adversarial expansion: 1/1 reviewable, 0 excluded;
- PR #117 recovery: 2/2 reviewable, 0 excluded.

Each merged slice received exact-head GitHub review with no findings and zero unresolved blocking threads before merge. No direct LLM OpenCodeReview success is claimed because no configured OCR LLM result is used as qualification evidence.

## Failure accounting

Failures remain failures and are not reused as PASS evidence:

- dependency validation failed before required UUID exception evidence existed;
- the first schema-core focused qualification had tests PASS but Clippy FAIL for dead-code/collapsible-if findings;
- an overbroad schema source assertion falsely matched forbidden names inside negative tests;
- intermediate local commit `c088ddcf8cfce180e52169f83b0fc115310b89cd` did not compile after concurrent working-tree capture and is not qualification evidence;
- duplicate/superseded PR #111 was closed without merge;
- the first Artifact negative test falsely treated authorized `size_bytes` as forbidden byte content and produced a 24/25 FAIL run;
- stale overlapping PR #113 was closed without merge;
- the original combined Event candidate `ad3ad5699a43a5f6c8ef5b93a2f2a002a4ee5ee0` passed tests but failed Diffcipline R3 because +794 exceeded the +600 limit;
- two Event development attempts failed Clippy on `manual_unwrap_or` / `manual_saturating_arithmetic`;
- the first split Event qualification had 33/33 tests PASS but Clippy FAIL due two deferred-test-only constants;
- one Event manual review regex falsely captured `stream_seq` from a later struct rather than the exact `EventAppend` block.

Only fresh corrected reruns are used as PASS evidence. Shared history was never rewritten to hide these failures.

## Independent final-main qualification

On `main@3bf286402b70790beb997bd555cdf1b064f0492d`:

- `git diff --check` over the implementation range — PASS;
- exact implementation range — 11 authorized paths;
- `cargo test --locked -p kernux-store --all-targets` — 46/46 PASS locally on macOS;
- dependency validator — PASS;
- full repository CI workflow `35452549806` — 6/6 SUCCESS;
- Ubuntu `kernux-store` runtime corpus — 46/46 PASS;
- native Windows `kernux-store` runtime corpus — 46/46 PASS;
- KRP protocol surface unchanged — PASS;
- T02 auth/local transport regression — PASS;
- SG-000020 identity regression — PASS.

## Pre-proof protected qualification

PR #118 pre-proof exact head:

`d165ccc2099f62ded33a3d73f58f414ec5910d7b`

Pre-proof protected workflow:

`35452855198` — six jobs SUCCESS: the five protected contexts plus native `Windows kernuxd`.

That exact-head qualification established the real `protected-closeout-pr-pass` prerequisite before native verification was created.

The pre-proof candidate itself passed:

- `git diff --check`;
- full `pnpm check`;
- Diffcipline R3: PASS, 1 file, +422/-0, zero scope violations;
- Alibaba OpenCodeReview Delegation Mode: 0 reviewable / 1 Markdown exclusion (`unsupported_ext`);
- complete manual semantic/security review of the excluded Markdown file.

## Native SpecGrain verification

Native SG-000021 verification was produced only after the pre-proof PR #118 exact head passed all six jobs. The pinned SpecGrain API independently verified the original WorkPacket and appended the immutable evidence record; the record was not hand-authored.

- spec revision: `sha256:5134113af17675a96bcf16f2fb6b25ac0cdc05792cb0eb04e63fa460e5142268`;
- WorkPacket: `sha256:ef1e49bb4c083ecd84c63991276cd9bcf588fa49340587b6824134ed3d21a379`;
- context plan: `sha256:6ee186049f55d234cfc2083b1caf37f693c663bb4f58473d7f506b73c4be4421`;
- implementation revision: `git:3bf286402b70790beb997bd555cdf1b064f0492d`;
- execution result digest: `sha256:55d8f02b0054a8ce16e90c0be8d1c75cb18cd86b4875e8a76d4668c5fd3004b8`;
- immutable evidence record: `sha256:e7136d88dd6ec7444054fb0a21103630c296975f0a68ad8f35c996802f9229ef`;
- `verified=true`;
- `issues=[]`;
- acceptance checks: 15/15 PASS;
- required evidence checks: 26/26 PASS;
- native `load_proof` / `specgrain prove` integrity check: PASS.

The immutable record is stored at:

`.specgrain/evidence/SG-000021/e7136d88dd6ec7444054fb0a21103630c296975f0a68ad8f35c996802f9229ef.json`

The verifier independently bound exactly the eleven implementation paths listed above to the SG-000021 authorized change surface. Executor self-report was not completion authority.

The native report also preserves the closeout state machine: `KX-P02-S03-T01` remains `NEXT`, `KX-P02-S04-T01` remains `PLANNED`, and no later task is authorized until the proof-bearing closeout merges, post-merge qualification succeeds, and a separate canonical transition is merged.

## Protected closeout

PENDING.

Until the proof-bearing closeout exact head passes fresh six-job qualification, exact-head review, zero unresolved threads, ordinary merge, and post-merge six-job qualification:

- `KX-P02-S03-T01` remains `NEXT`;
- `KX-P02-S04-T01` and all later P02 tasks remain `PLANNED`;
- P02 progress remains `3 / 9 macro tasks PROVEN`;
- P02 phase exit remains unproven.

## Next frontier

No later P02 implementation is authorized by this pre-proof closeout candidate.
