# Evidence — KX-P03-S01-T01 (implementation, UNPROVEN)

Task: `KX-P03-S01-T01 — Create Electron/React desktop shell with unprivileged renderer and typed preload bridge.`
Grain: `SG-000031`, state `GRAIN`, risk `R2` (medium grain risk; slices qualify at R3).
Status: `IMPLEMENTED_UNPROVEN`. No advancement claimed. P03 entry stays
`KX-P03-S01-T01 ONLY`; later P03 tasks stay PLANNED.
Ledgers (`specs/CURRENT.md`, `specs/tasks.md`) are intentionally untouched by this record.

## Canonical implementation range

- Pre-implementation base (canonical main): `0d9805e97f973955b5d9966dd73176d5b7d488fe`
- PR-A (#215) head: `1e5d08e4927577aca60dcad25f908d335b91d91c`
- PR-A merge: `ce1865606e4dc8d62c500516e63d527dc92123e7` (2026-09-24T09:31:23Z, ordinary merge)
- PR-B (#216) head: `9405b1a986c668b13c6b1aca8094e66db65d4463`
- PR-B merge: `88c5ea988fc6b36991763e5e3e807e8601489755` (2026-09-24T09:45:08Z, ordinary merge)
- PR-C (#217) head: `d7cdcd7c98e345bab8f2e3ffa3c07fd7692731ee`
- PR-C merge: `4a617661fab16961b70305334005dd5bd73d83fa` (2026-09-24T09:56:54Z, ordinary merge)
- Combined range `0d9805e...4a61766`: exactly 13 files under `apps/desktop/`,
  `+1322/-0`, zero npm dependencies, zero manifest drift, zero lockfile drift.
- `docs/evidence/P03-S01-T01.md` is this file (IMPLEMENTED_UNPROVEN; advances nothing).

## Packet identity (frozen)

- WorkPacket digest: `sha256:4b7dad47c9434c265cee589eca35a4f06723b99f21db0461ce677f380d78ee2d`
- Context-plan digest: `sha256:d196775b4a0ca522e651e173c5722cd5c95c95e69c385247d662000950e6909f`
- Spec revision: `sha256:6709656ac1f172bfcb3f3d5445ea5b76ac937ed75f1e7ec01901200f42a375d2`
- SpecGrain pin: `TheHalfMoon/SpecGrain` commit `5de7d6499bb0a9e3a191fc0934399cf099d1980a`
  (archive SHA-256 `da6a8c006c3a0154e76e2bf08bcae58bb86e04f06dd7a03115275b466489eef9`)
- WorkPacket file SHA-256: `e70b0737c525f3d258d03ceb7f4734a7815d28fedd9e45cf76966c438d1ce3fb`
  (6912 bytes; context sources 4545 bytes, 12 sources, 3468 tokens).
- Byte-identical WorkPacket replay (`cmp` PASS) on probe Linux and macOS-14 for
  all three slice heads. The Grain and packet were not modified.
- `prove SG-000031`: `verified=false`, `record_count=0` (correct: native
  verification is a later dependent step, no record hand-authored).
- `prove SG-000030`: `verified=true`, `record_count=1` (prior proof preserved).

## PR-A qualification (head 1e5d08e)

- Scope: 8 files under `apps/desktop/`, `+454/-0`, dependency-free. Closed
  2-channel invoke allowlist with exact parsing and constant errors, frozen
  unprivileged window policy, strict local-only CSP, single-window invariant,
  preload expose-only wiring, and 12 adversarial unit tests. No npm
  dependencies, no manifest changes, no lockfile changes.
- Exact-head CI run `35981415181`: 6/6 SUCCESS.
- Probe run `35981484739` (workflow `pr215-shell-a-qualification`): Linux and
  macOS jobs SUCCESS, every step green, including desktop shell tests 12/12
  on both platforms.
- Diffcipline R3 with `run-verification=true`, action
  `TheHalfMoon/Diffcipline@5cb1c77340b75649f6168e0e8f66479ea047ea96` (v1.0.0):
  base `0d9805e`, 8 files, `+454/-0`, manifests unchanged, lockfiles unchanged,
  untracked 0, scope PASS, verification PASS (`pnpm check`), final verdict
  PASS — on Linux and macOS-14 independently.
- Full baseline `pnpm check` PASS both platforms; desktop shell tests 12/12
  PASS both platforms (strict `tsc` + `node --test`).
- Genuine Jev (`jev 0.3.2`, provider TypeSafe, model `jev-1.13.0`,
  base `0d9805e`, head `1e5d08e`): 2/2 answer no — renderer-authority 0.04,
  secrets/dependencies 0.08. No blocking findings. Disposition clean.
- Alibaba OpenCodeReview `1.12.7` (`85cecfe`), Delegation Mode accounting only:
  merge_base `0d9805e`, total 8 / reviewable 6 / excluded 2 (`unsupported_ext`
  JSON), `+454/-0`, `excluded_files` are the two JSON configs. Same accounting
  on probe Linux and macOS-14. No model-backed review claimed.
- Native macOS: `macos-14-arm64`, ProductVersion `14.8.9`, BuildVersion `23J631`, arm64.
- Windows: exact-head CI `Windows kernuxd` SUCCESS (shell tests run on Linux
  and macOS probes; local `cargo` blocked by missing `link.exe`, CI is authority).
- Semantic review of exact head: two exact channels only; case-sensitive closed
  parsing; unknown channels rejected without echo; frozen flags exact;
  CSP local-only; single-window constant; preload exposes exactly one method;
  no secret capture; no dependencies. No defects.
- Reviews: 0 formal. Threads: 0 blocking (0 PR comments; only bot skip notices).
- Post-merge CI run `35981752210` on `ce18656`: 6/6 SUCCESS.

## PR-B qualification (head 9405b1a)

- Scope: 3 files, `+535/-1` (transport client, handshake tests, one-line shim
  widening for `rejects`/optional validators). Dependency-injected handshake
  client mirroring the kernuxd wire format exactly (`kxa/1` preamble,
  newline frames bounded at 4096 bytes, `health_version` probe shapes,
  canonical UUIDv7). Zero imports; socket injected. Nonce never escapes:
  constant errors, hard timeouts, premature-close and malformed handling.
- Exact-head CI run `35982484135`: 6/6 SUCCESS.
- Probe run `35982859309` (workflow `pr216-shell-b-qualification`): Linux
  (ubuntu-24.04) and macOS-14 jobs SUCCESS, every step green, including the
  LIVE daemon handshake against a real cargo-built kernuxd over UDS with real
  nonce auth on BOTH platforms:
  `{"connection":"connected","daemonVersion":"0.0.0","lifecycle":"serving"}`.
  Desktop shell tests 23/23 PASS both platforms; R3 Verdict PASS +
  Verification PASS both; OCR 3/2/1 both.
- Genuine Jev (same CLI/provider/model, base `ce18656`, head `9405b1a`):
  2/2 answer no — nonce-leak/framing 0.05, scope/dependencies 0.12.
  No blocking findings. Disposition clean.
- Alibaba OpenCodeReview `1.12.7` Delegation Mode accounting only:
  merge_base `ce18656`, total 3 / reviewable 2 / excluded 1, `+535/-1`.
- Native macOS-14 arm64 PASS with live handshake; Windows via exact-head CI PASS.
- Semantic review: preamble byte-exact (`kxa/1 ` + 64 lower-hex + newline);
  request/response shapes mirror the frozen KRP fixtures; requestId echo
  binding; contract/enum validation; degraded mapping exact; fake-socket tests
  cover chunking, timeout, error, close, and no-write-on-invalid-input. No defects.
- Reviews: 0 formal. Threads: 0 (0 PR comments).
- Post-merge CI run `35983143820` on `88c5ea9`: 6/6 SUCCESS.

## PR-C qualification (head d7cdcd7)

- Scope: 4 files, `+334/-0` (main process, renderer skeleton, adversarial
  corpus, bridge hardening). Single-window lifecycle with frozen web
  preferences, strict CSP headers, allowlisted handlers with a stdlib
  transport adapter; React skeleton with status projection and four-region
  shell; frozen allowlist plus JSON payload bound; 8-test adversarial corpus
  (relabeling, oversize/circular payloads, flag tampering, CSP lock, secret
  hygiene). Dependency-bound sources reviewed with syntax verification; full
  compilation executes under the future packaging grain with admission.
- Exact-head CI run `35983689502`: 6/6 SUCCESS.
- Probe run `35984051712` (workflow `pr217-shell-c-qualification`): Linux and
  macOS-14 jobs SUCCESS, every step green, including desktop shell tests 31/31
  both platforms, dependency-bound syntax gate PASS both (only expected
  unresolved-dependency diagnostics), R3, replay, and OCR 4/3/1 both.
- Genuine Jev (same CLI/provider/model, base `88c5ea9`, head `d7cdcd7`):
  2/2 answer no — wiring-authority 0.09, corpus-quality 0.19.
  No blocking findings. Disposition clean.
- Alibaba OpenCodeReview `1.12.7` Delegation Mode accounting only:
  merge_base `88c5ea9`, total 4 / reviewable 3 / excluded 1, `+334/-0`.
- Native macOS-14 arm64 PASS; Windows via exact-head CI PASS.
- Semantic review: single-instance lock with focus-restore; CSP applied per
  response; handlers re-validate the allowlist; launch-nonce gating is
  constant-error; status text exact; corpus locks rendered surfaces. The
  `main.ts` requestId extraction and `void session` cleanups were fixed
  pre-commit on review. No defects.
- Reviews: 0 formal. Threads: 0 (0 PR comments).
- Post-merge CI run `35984331591` on `4a61766`: 6/6 SUCCESS.

## Security invariants (held across all three slices)

- Renderer receives no ambient Node or host authority by construction
  (frozen flags, sandbox, context isolation, strict CSP).
- Bridge exposes exactly two typed invoke-only channels; unknown channels fail
  closed without echo; allowlist frozen at runtime; payloads bounded and
  JSON-guarded.
- Handshake mirrors the daemon wire format byte-exactly with nonce hygiene,
  hard timeouts, and request binding; proven live against real daemons.
- No secret or credential capture in shell, bridge, client, tests, or logs.
- Zero npm dependencies: no manifest drift, no lockfile drift, no admission
  debt. Electron and React land only with a future dependency grain.

## Negative evidence (preserved, not waived)

- Committed build artifacts (`bridge.js`, `window-config.js`) caught at commit
  time on the unpushed PR-A branch; removed via amend before push. Never
  reached any PR.
- Bridge truncation by a patch script that opened the file for writing before
  validating (write-then-crash); restored byte-identical from HEAD. Scripts
  now validate before opening or swap temp files.
- PR-B probe `35982585351` failed at the live-handshake `tsc` invocation
  (TS5112 files+project conflict); fixed with `--ignoreConfig` (verified
  locally first), re-running probe green. No PR head touched.
- PR-C probe `35983757281` failed at the syntax gate: the `error TS1`
  substring also matches TS18046-class diagnostics. Fixed with an explicit
  expected-diagnostic allowlist; re-running probe green. No PR head touched.
- Local Windows limitations (`link.exe` absent; local pnpm 11.x vs required
  12.x) persist as environmental facts; affected gates ran on authorized
  runners, never waived.
- Ambient qodo billing-blocked, CodeRabbit auto-skip, and cubic reviewer-only
  states are limitations, not approvals.

## Outstanding before PROVEN (not claimed here)

1. Native `verify_execution` with the pinned SpecGrain verifier binding the
   WorkPacket to this observed evidence (8/8 acceptance, 20/20 required
   evidence, zero issues), producing the immutable record.
2. Separate protected closeout PR (`.specgrain/evidence/SG-000031/`, this
   record finalized, `specs/CURRENT.md`, `specs/tasks.md`) with its own
   exact-head CI + R3 + Jev + OpenCodeReview + platform + semantic + zero-thread
   qualification, lawful merge, and post-merge CI. Only then may
   `KX-P03-S01-T01` transition NEXT to PROVEN; later P03 tasks stay PLANNED.

Status: `IMPLEMENTED_UNPROVEN`. `KX-P03-S01-T01` stays NEXT.

## Protected closeout (pre-proof, closeout PR)

Closeout claim: SG-000031 shell implementation is canonical (PR-A #215 merge
`ce18656`, PR-B #216 merge `88c5ea9`, PR-C #217 merge `4a61766`, evidence #218
merge `24df9d4`, each with post-merge CI 6/6 SUCCESS: `35981752210`,
`35983143820`, `35984331591`, `35984900773`). Combined
range `0d9805e...4a61766` holds exactly 13 files under `apps/desktop/`
(`+1322/-0`) with zero npm dependencies and zero manifest or lockfile drift,
inside the eleven-path authorized surface.

Pre-closeout native verification (pinned SpecGrain source
`5de7d6499bb0a9e3a191fc0934399cf099d1980a`, script
`.specgrain/tmp/sg31_prove.py`, NOT committed) reports verified=false with
exactly 5 issues and nothing else: `protected-closeout-pr-pass`,
`closeout-post-merge-ci-pass`, `native-specgrain-verification-pass`, and
acceptance checks 1 and 4. Acceptance is 6/8, evidence 17/20; packet, spec,
result, scope, and surface bindings are clean. Dry-run result digest:
`sha256:d958adbb79942163c5bce1e8fffad55d58d8f304842a8511b3cd56c4a3b780a3`.
No record was appended: a passing record cannot lawfully exist before this
closeout merges with green post-merge CI, so the closeout-dependent checks
honestly fail. This dry run is negative evidence, not a defect.

Ledgers unchanged by this PR: KX-P03-S01-T01 stays NEXT, later P03 tasks stay
PLANNED, SG-000031 stays GRAIN with 0 records.

Pending after this PR merges with post-merge CI 6/6: final native
verify_execution (all 20 evidence and 8 acceptance genuinely satisfiable),
immutable record under `.specgrain/evidence/SG-000031/`, then a proof PR
carrying the record, the proof section, and the single-task ledger transition.

Status: `IMPLEMENTED_UNPROVEN`.

## Native SG-000031 verification - PROVEN

Pinned native verification used the repository-pinned SpecGrain CLI source
(TheHalfMoon/SpecGrain commit `5de7d6499bb0a9e3a191fc0934399cf099d1980a`,
archive SHA-256 `da6a8c006c3a0154e76e2bf08bcae58bb86e04f06dd7a03115275b466489eef9`)
via the local scratch script `.specgrain/tmp/sg31_prove.py` (NOT committed) and
loaded the tracked WorkPacket from `docs/evidence/SG-000031-work-packet.json`.

It independently observed the shell implementation range
`0d9805e97f973955b5d9966dd73176d5b7d488fe...4a617661fab16961b70305334005dd5bd73d83fa`
as exactly thirteen paths under `apps/desktop/`, all inside the frozen
SG-000031 eleven-path authorized surface. No manifest, lockfile, KRP,
protocol, provider, or ledger paths change across the range (probe-verified on
linux and macos). The combined source diff is `+1322/-0` with zero npm
dependencies.

Native verify_execution result:

- implementation revision: `git:f51f04c3b369d7839aab56eae27efef704f1a638`;
- packet digest: `sha256:4b7dad47c9434c265cee589eca35a4f06723b99f21db0461ce677f380d78ee2d`;
- spec revision: `sha256:6709656ac1f172bfcb3f3d5445ea5b76ac937ed75f1e7ec01901200f42a375d2`;
- context-plan digest: `sha256:d196775b4a0ca522e651e173c5722cd5c95c95e69c385247d662000950e6909f`;
- execution result digest: `sha256:d958adbb79942163c5bce1e8fffad55d58d8f304842a8511b3cd56c4a3b780a3`;
- immutable evidence record: `sha256:2860e1a9ab8e0595ea57cbbd6e45ff480e9f926be8dfc75ba6d7f7e489244dbe`;
- verified=true;
- issues=[];
- acceptance checks: 8/8 PASS;
- required evidence checks: 20/20 PASS.

The generated evidence bytes are persisted at:

`.specgrain/evidence/SG-000031/2860e1a9ab8e0595ea57cbbd6e45ff480e9f926be8dfc75ba6d7f7e489244dbe.json`

The generated file is 8,491 bytes with file SHA-256:

`sha256:fb18317707b6f69ac4eb0b68cc5e5c3dedf3c25f072cd4673a3898614d91fbd3`

Prove status after append:

- `prove SG-000031` reports verified true with record count 1;
- `prove SG-000030` reports verified true with record count 1 (preserved);
- `check .` reports valid.

A pre-closeout dry run of the same script honestly reported verified=false
with exactly the 5 closeout-dependent checks failing (acceptance 6/8, evidence
17/20, bindings clean) and appended no record; the final run above ran only
after closeout PR #219 merged with post-merge CI 6/6.

Status: `KX-P03-S01-T01 PROVEN` with this proof PR merge; only KX-P03-S01-T02 is NEXT.
