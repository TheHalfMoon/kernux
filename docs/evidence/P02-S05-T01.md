# Evidence — KX-P02-S05-T01

## Task and current closure state

`KX-P02-S05-T01 — Implement policy evaluator and bounded grants/expiry/delegation/deny reasons.`

Primary SpecGrain:

`SG-000023 — KX-P02-S05-T01 — Implement fail-closed policy evaluation and bounded Grant lifecycle`

Risk: `R3`.

The implementation chain is merged and its final implementation revision is qualified on `main`.

The task remains canonically `NEXT`.

No native SG-000023 evidence record is claimed by this document. No later P02 task is authorized by this document.

Final qualified implementation revision:

`e8c97f277289195a82b00d2b3b2ca1dfea32ffb8`

Final implementation post-merge workflow:

`35518951856` — six jobs SUCCESS.

## Frozen SG-000023 identities

- spec revision: `sha256:bd6fff0a55cd96efe1937e5e170e9fb48672a54af3175967600e845af7a4bbef`;
- original WorkPacket identity: `sha256:f8b664eac0be012ae41d727f52a0ac0f5f120fc3ac9230029b34f13f35a61265`;
- context-plan identity: `sha256:ec3e79f0ae07ee828d09b932ad95fe94d82107e077f7e7600e53d87636095348`;
- original context accounting: `18,368 / 20,000` tokens;
- acceptance checks: 17;
- required evidence checks: 24.

The original WorkPacket digest was frozen before implementation and carried through the bounded implementation chain.

The exact original WorkPacket bytes and exact original ContextSource records are not present in the inspected canonical repository/GitHub surfaces. The authorized macOS remote channel is currently quota-blocked, so no claim is made that an historical local temporary packet or context file still exists.

A digest alone is not sufficient native verification authority. A replacement packet must not be regenerated from inferred post-implementation context and called the original packet.

Issue #145 tracks durable original-WorkPacket retention and recovery hardening.

## Qualified implementation chain

| Slice | Exact head | Merge |
| --- | --- | --- |
| PR #127 — SG-000023 refinement | `cdca9edd8d789d81d93cc948605f48f8137bac38` | `8f24f575dddba8fcaf6b8c5f5106092739891399` |
| PR #128 — policy action/resource core | `7cde07642e69cdeb96b942dc2b6b1a05cda57f3f` | `ccbbafe1aa1e6f1940f0745e0aa7fec6f5e9bcf5` |
| PR #129 — consequence/constraint semantics | `9ac644231e59dbc968c496e956756ea11881db69` | `1adb66e6aec25ef714679179592557b602c5e0cb` |
| PR #130 — request/Grant envelopes | `ed4a11bb3d4ce33fe0a82d27051c2a299f3c9168` | `8faa33fc3d2a8034ca89ad67afe3a7ba54003689` |
| PR #131 — schema-v2 migration | `71f982cc7ae54d05a6af148f3ab145a3c7e26956` | `32f7b347f19fc9d45e1b71aaa0a398531538b4c6` |
| PR #132 — validated Grant ledger | `b64d795b1a0edd19b23b73584ef477e76a56e731` | `e090507c8b5e3df2acdd66e1c18051738cbbf1a4` |
| PR #133 — admission/revocation accounting | `471421729bd11895ab73405c65049ce89cfdb25c` | `a710429c4d29762682940123747b61520ba3c61b` |
| PR #134 — budget-consumption boundary | `594409c9b386ff8288f0eec8a5886bd58cda47aa` | `5647df076a1a2865dd6f5efce5b9fa3a4d330cc5` |
| PR #135 — bounded delegation issuance | `45ec9198baec662517ffe7d5b0f0a8f6cd328c97` | `9406a02aa0dab540ae8a8761d5a1c93d10c90eea` |
| PR #136 — ancestor Grant budgets | `e897533e31e8270d2cac4813eabfadbaf2c43065` | `610a793add0f7c49e3879ddcfb1d22edfcb74324` |
| PR #137 — atomic policy audit | `4fa1d4df30adb8e5a74dee2fd97ad0ba1617e3b8` | `6a724e58018c16bcca20b8cfd14c9631b4cd5b30` |
| PR #138 — mandatory authority intersection | `63800bfe1f942c8041d7eb8d8fc9688136e8722c` | `e412ef26f0a669dbb8e0505f180848f7af1e67bc` |
| PR #139 — audited Grant composition | `70315e94d31cb5068aa323dd49492936d9f245a7` | `ecca499f6ce4a06f596c9ce1f5f2434825c51d94` |
| PR #140 — kernuxd pre-side-effect authorization | `cffb8b9d52dd80e0d98cae986cb808dda0ca8328` | `e8c97f277289195a82b00d2b3b2ca1dfea32ffb8` |

Every dependent implementation slice preserved its own exact failure history and did not relabel negative, cancelled, review-only, or obsolete-head results as PASS.

The final PR #140 exact head passed:

- workflow `35510428174`: six jobs SUCCESS;
- Rust formatting, Clippy, and tests: PASS;
- native Windows kernuxd/policy/store/identity Clippy and tests: PASS;
- JavaScript / TypeScript: PASS;
- SpecGrain repository-state check: PASS;
- Provenance: PASS;
- Diffcipline R1: PASS;
- exact-head manual review: no code findings;
- unresolved review threads: zero.

Final PR #140 local qualification recorded:

- local macOS focused corpus: PASS;
- local macOS full `pnpm check`: PASS;
- local Diffcipline v1.0.0 R3: PASS;
- Alibaba OpenCodeReview v1.12.7 Delegation Mode: PASS;
- OpenCodeReview rule resolution: PASS;
- host-agent semantic/security accounting: PASS;
- complete manual excluded-file accounting: PASS;
- OCR rule evidence SHA-256: `aacfc3f8225590c7ad9388db5a6d6f9e06ded92f40cce57a7d76fcad9fffbd2e`.

PR #140 then merged normally and `main@e8c97f277289195a82b00d2b3b2ca1dfea32ffb8` passed post-merge workflow `35518951856` with all six jobs SUCCESS.

## Full implementation range

The complete implementation range after the qualified SG-000023 refinement merge is:

`8f24f575dddba8fcaf6b8c5f5106092739891399..e8c97f277289195a82b00d2b3b2ca1dfea32ffb8`

GitHub comparison reports 15 changed paths:

1. `.github/workflows/ci.yml`
2. `Cargo.lock`
3. `Cargo.toml`
4. `crates/kernux-policy/Cargo.toml`
5. `crates/kernux-policy/src/decision.rs`
6. `crates/kernux-policy/src/envelope.rs`
7. `crates/kernux-policy/src/lib.rs`
8. `crates/kernux-policy/src/semantics.rs`
9. `crates/kernux-store/Cargo.toml`
10. `crates/kernux-store/src/event.rs`
11. `crates/kernux-store/src/grant.rs`
12. `crates/kernux-store/src/lib.rs`
13. `crates/kernuxd/Cargo.toml`
14. `crates/kernuxd/src/authorization.rs`
15. `crates/kernuxd/src/lib.rs`

## Native SG-000023 change-surface gap

The frozen SG-000023 change surface authorizes the policy crate as a directory prefix and explicitly names the relevant workspace/store/daemon paths.

Independent comparison of the complete observed implementation range against the pinned SpecGrain path semantics identifies three observed implementation paths that are not covered by the frozen SG-000023 change surface:

- `crates/kernux-store/Cargo.toml`;
- `crates/kernux-store/src/event.rs`;
- `crates/kernuxd/src/authorization.rs`.

The pinned verifier authorizes exact paths or literal directory prefixes. It must not be forced to reinterpret the Grain after implementation.

Therefore SG-000023 must not receive a fabricated passing native evidence record, and SG-000023 must not be edited retroactively to make the historical implementation appear scoped.

This is structurally analogous to the preserved SG-000005 native proof gap. Kernux previously resolved that class of problem by leaving the failed primary Grain visible and using a separate corrective Grain, SG-000006, to explicitly re-adopt and independently verify the affected current surfaces.

## Required corrective path

Before KX-P02-S05-T01 can become `PROVEN`, a separate corrective Grain must be authored through the pinned native SpecGrain CLI and must follow the fail-closed corrective contract tracked in Issue #148.

The corrective Grain must:

1. preserve the SG-000023 packet-recovery and change-surface gaps as negative evidence;
2. explicitly authorize its own CLI-emitted SpecGrain file, two exact corrective packet-evidence JSON paths, and the three current source surfaces:
   - `crates/kernux-store/Cargo.toml`;
   - `crates/kernux-store/src/event.rs`;
   - `crates/kernuxd/src/authorization.rs`;
3. use no retroactive SG-000023 mutation and no wildcard authority for the corrective execution surfaces;
4. use a new original corrective WorkPacket generated on clean canonical `main` after corrective refinement merge and post-merge qualification, but before any corrective repository mutation;
5. execute a dedicated packet-persistence slice first, containing only the exact ContextSource records and exact CLI-exported WorkPacket bytes;
6. require that packet-persistence slice to pass local/full/R3/OpenCodeReview/exact-head review and six-job CI gates, merge normally, and pass exact post-merge qualification before any source reconciliation begins;
7. only after packet persistence is canonical and byte-identical replay remains proven, execute a separate source-reconciliation slice touching exactly the three re-adopted source paths with one bounded non-functional reconciliation comment each unless fresh evidence proves an actual defect;
8. preserve dependency/package/version/source identity, Event sequencing/ownership/redaction/transaction semantics, and daemon trusted-authority/exact-request/atomic-audit/non-forgeability semantics;
9. require the source-reconciliation slice to pass focused/full local macOS evidence, Diffcipline R3, Alibaba OpenCodeReview accounting, exact-head six-job CI/review/zero-thread, normal merge, and exact post-merge qualification;
10. independently observe the combined corrective execution range as exactly the two packet-evidence files plus the three re-adopted source paths;
11. run pinned native verification only from the original corrective WorkPacket, exact final corrective merge revision, exact five observed paths, and independently rechecked acceptance/evidence records;
12. append native corrective proof only after verification returns `verified=true` with zero issues;
13. keep generated native evidence, `docs/evidence/P02-S05-T01.md`, `specs/CURRENT.md`, and `specs/tasks.md` as post-native closeout/canonical-transition surfaces rather than corrective execution authority;
14. prove the KX-P02-S05-T01 macro outcome only by the combined qualified SG-000023 implementation evidence plus the corrective native proof and subsequent protected closeout.

The corrective Grain itself must be created through supported SpecGrain authoring commands. Hand-editing `.specgrain` state is forbidden by repository governance.

Issue #148 is the current detailed corrective contract. Its final v5 section supersedes earlier exploratory runbooks and dry-run identities.

## Current blocker and fail-closed state

The connected macOS host remains online but its Remote Desktop Commander monthly call quota is exhausted. The service explicitly instructed callers not to retry or reconnect until usage resets.

That blocks:

- inspection of any historical untracked SG-000023 packet/context files on that host;
- supported local SpecGrain CLI authoring on that host;
- fresh local macOS qualification for a corrective candidate.

No attempt is made to bypass that quota or substitute a different host while claiming macOS evidence.

Current canonical state remains:

- `KX-P02-S05-T01` — `NEXT`;
- `KX-P02-S05-T02` — `PLANNED`;
- `KX-P02-S06-T01` — `PLANNED`;
- `KX-P02-S06-T02` — `PLANNED`;
- P02 progress — `5 / 9`;
- P02 phase exit — unproven.

No later P02 implementation is authorized.

## Recovery and completion boundary

This evidence file is a negative-proof/readiness record, not a completion claim.

Do not:

- regenerate an inferred SG-000023 WorkPacket and label it original;
- hand-author a native SG-000023 evidence record;
- edit SG-000023 retroactively to include the three missing paths;
- mark KX-P02-S05-T01 `PROVEN`;
- advance KX-P02-S05-T02;
- weaken local macOS, Diffcipline, OpenCodeReview, exact-head, post-merge, or native SpecGrain gates.

The next valid repository mutation is supported CLI authoring of the corrective Grain when an authorized execution environment for the pinned SpecGrain CLI is available.

## Corrective SG-000024 native proof — superseding status

The earlier blocker section above is retained as historical negative evidence. It no longer describes the live corrective frontier.

The final v5 corrective contract from Issue #148 has now executed through native proof without rewriting SG-000023.

### Corrective refinement

PR #149 introduced the corrective Grain exclusively through the repository-pinned SpecGrain CLI.

- corrective Grain: `SG-000024`;
- corrective refinement merge: `17fe5b52d7d68b04e97bd65bc0c6be4637141225`;
- semantic Spec revision: `sha256:bad445ed3c11f6b5ff18b1c4e93266aff6f66c64c9c531322986b380a8b7b49f`;
- exact CLI-emitted Spec blob: `9c7a858f2028ee7401f69e74551032ad0713f875`;
- exact-head qualification and protected merge/post-merge gates passed before packet generation.

SG-000023 remains unchanged and natively unverified. Its historical recovery/change-surface gap remains negative evidence.

### Original corrective packet persistence

The production corrective packet was generated on clean canonical main after PR #149 merged and before any corrective implementation mutation.

Frozen identities:

- ContextPlan: `sha256:3bfc0ff9366f6a2e33ad95ec30e10d20f08a0732939242efc5f6aafe20d9961f`;
- WorkPacket: `sha256:826805690aecaa38641ebda10cdf304b94c2bb5362f61d349f45d8f8476e5851`;
- tracked ContextSource file SHA-256: `sha256:58b87cbedd80a00380f890b3e5da1f6d6d16be555da12165fc6b4b126309bc4e`;
- tracked WorkPacket file SHA-256: `sha256:35afa67ba1f15260f0e7507009ee002c44ca65fef505ee10cf1f1b5dc16e13ef`.

PR #150 persisted only the two authorized packet evidence files and merged as:

`35e59be711f0550639bd82d6cfa79ff24f8f4163`

Native macOS qualification, full repository baseline, Diffcipline R3, Alibaba OpenCodeReview accounting, exact-head six-job CI/review, ordinary merge, exact post-merge six-job qualification, and byte-identical packet replay all passed before source reconciliation began.

### Three-path source reconciliation

PR #151 touched exactly:

- `crates/kernux-store/Cargo.toml`;
- `crates/kernux-store/src/event.rs`;
- `crates/kernuxd/src/authorization.rs`.

Each path received exactly one non-functional corrective scope-reconciliation comment and no semantic source change.

Exact source-reconciliation head:

`916f2486aee62c9e263acd559db01d5acac94a80`

Ordinary merge:

`40517b3537f84f02197fa22f0f699adc998bd6c8`

Qualification evidence includes:

- exact-head six-job CI `35564699911` — SUCCESS;
- corrected native macOS qualification `35575855639` — SUCCESS;
- focused policy/store/daemon regression evidence — PASS;
- full `pnpm check` — PASS;
- Diffcipline R3 — PASS, exactly 3 files and +3/-0;
- Alibaba OpenCodeReview v1.12.7 Delegation Mode — 3/3 reviewable, 0 excluded;
- exact-head semantic/security/governance review — no finding;
- unresolved review threads — 0;
- post-merge six-job CI `35576060583` — SUCCESS.

The earlier native macOS probe `35564469515` remains preserved as FAIL. It failed before project qualification because the probe used Bash `mapfile`, which is unavailable in the macOS default Bash. The candidate was not relabeled PASS and the failed run was not rerun-to-green. A forward-only Bash-3.2-compatible probe revision produced the subsequent successful run `35575855639`.

### Native SpecGrain verification

Pinned native verification run `35576532262` checked out exactly:

`40517b3537f84f02197fa22f0f699adc998bd6c8`

It verified the repository pin:

`TheHalfMoon/SpecGrain@5de7d6499bb0a9e3a191fc0934399cf099d1980a`

with archive SHA-256:

`da6a8c006c3a0154e76e2bf08bcae58bb86e04f06dd7a03115275b466489eef9`

The run independently replayed the canonical ContextSource records through the pinned packet command and reproduced the tracked WorkPacket byte-for-byte.

The independently observed corrective execution range:

`17fe5b52d7d68b04e97bd65bc0c6be4637141225..40517b3537f84f02197fa22f0f699adc998bd6c8`

contains exactly five implementation-authority paths:

1. `crates/kernux-store/Cargo.toml`;
2. `crates/kernux-store/src/event.rs`;
3. `crates/kernuxd/src/authorization.rs`;
4. `docs/evidence/SG-000024-context-sources.json`;
5. `docs/evidence/SG-000024-work-packet.json`.

Native `verify_execution` result:

- implementation revision: `git:40517b3537f84f02197fa22f0f699adc998bd6c8`;
- packet digest: `sha256:826805690aecaa38641ebda10cdf304b94c2bb5362f61d349f45d8f8476e5851`;
- execution result digest: `sha256:55af1e7eb8cbab392ae5fde16b76b930adb211e16cc097f142479717dfa6327f`;
- immutable evidence record: `sha256:f6340663290f6a609777bc1c2d10269f85e5107aa5aa4f385a06ab67b41b452e`;
- `verified=true`;
- `issues=[]`;
- acceptance checks: 10/10 PASS;
- required evidence checks: 14/14 PASS.

The generated evidence bytes are persisted at:

`.specgrain/evidence/SG-000024/f6340663290f6a609777bc1c2d10269f85e5107aa5aa4f385a06ab67b41b452e.json`

The generated file is 11,583 bytes with file SHA-256:

`sha256:da684229eca25ee18a266d92d6efb2db38ed256c27fddc72540ee45fb0ff3666`

`specgrain prove SG-000024 . --json` reports:

- `verified=true`;
- `record_count=1`;
- latest record digest `sha256:f6340663290f6a609777bc1c2d10269f85e5107aa5aa4f385a06ab67b41b452e`.

### Remaining canonical boundary

This proof-bearing closeout does not itself advance the canonical task ledger.

Until this closeout candidate passes its own exact-head qualification/review, ordinary protected merge, and exact post-merge qualification:

- `KX-P02-S05-T01` remains `NEXT`;
- `KX-P02-S05-T02` remains `PLANNED`;
- S06 remains `PLANNED`;
- P02 progress remains `5 / 9`;
- P02 phase exit remains unproven.

Only after this closeout is canonical and post-merge-qualified may a separate canonical transition advance KX-P02-S05-T01 and, if live dependency truth still permits it, expose KX-P02-S05-T02 as the next task.
