# Evidence — KX-P03-S02-T01 (implementation, UNPROVEN)

Task: `KX-P03-S02-T01 — Establish Kernux design tokens, primitives, accessibility and design-system lint rules.`

Grain: `SG-000033`, state `GRAIN`, risk `R1`. Status: `IMPLEMENTED_UNPROVEN`.
No task-state, phase-exit, release, or `PROJECT_COMPLETE` advancement is claimed by this record.

## Canonical implementation range

Pre-implementation packet base: `0e743a533111942b091f4ced4cc3100fea330538` (SG-000033 refinement/packet persistence lineage).
Canonical final implementation main: `ad1a848c67c5f6592e6af8f6132f119df57ee847`.
Canonical final implementation tree: `56b9c2fe46bbd74c02613e513d637d2d2453965d`.
Implementation evidence PR #238 merged normally at `a0dbe92fc1eacb3cbd349c216dce9a9b2caa9ba2`, tree `e46f17f4bc8a3df3f171bd3fd2c9dc756bb3ceba`; post-merge CI `36037008797` was SUCCESS.

Implementation slices, all ordinary merges with exact-head and post-merge CI:

- PR-A #232, token authority: candidate `a8d1de4b917fadf396ae52dd9485125f87f2791c`, merge `c89b8fe643d192434fbf2de403a241272b29f09c`, exact-head CI `36011778487`, post-merge CI `36012093060`.
- PR-B #233, semantic primitives: candidate `9e50709acb23cb69e25926e461bb9ced52fc9f1a`, merge `e2d39e8169f1fd517c1281c12d5456c3833e9aed`, exact-head CI `36015172491`, post-merge CI `36015456287`.
- PR-C1 #234, conformance foundation: candidate `63b388311aee95a3c7e0fae4d1b30bfb89ad6145`, merge `2f6d67ba5c610f1ab8c1b686082a0d1f97b75dcd`, exact-head CI `36032203041`, post-merge CI `36032424712`.
- PR-C2 #235, static token/contrast and renderer authority: candidates `dda8bc3be9001fb16e6c6a80a903720461d1ac51` and `be919497be7017a01b69aa394ffc6c1d5061c262`, merge `cc2244ef47c9a303980a6f7cfa302ab79671249d`, exact-head CI `36033563359`, post-merge CI `36033803287`.
- PR-C3 #236, primitive/style conformance: candidate `83ea8958228d2d1250d1fc8ac8b29e6039e0fded`, merge `5489114b0516ac9dae96faede12944467fb0e484`, exact-head CI `36034929697`, post-merge CI `36035156099`.
- PR-D #237, renderer accessibility style contract: candidate `334d68b7c921bd2b315ebb868c0029ef98eb2c3f`, merge `ad1a848c67c5f6592e6af8f6132f119df57ee847`, exact-head CI `36035926441`, post-merge CI `36036173392`.

The combined implementation range is inside the SG-000033 authorized surface. It adds no npm dependency and does not change `pnpm-lock.yaml`.

## Qualification observed

At the final canonical implementation tree, the repository-owned checks pass:

- `node tools/ui-system/check.mjs`: PASS.
- `node --test tools/ui-system/check.test.mjs`: 10/10 PASS.
- `pnpm run test:ui-system`: 15/15 PASS, including renderer focus-visible, reduced-motion, visually-hidden, token, contrast, primitive, and hostile-input coverage.
- `pnpm run check:js`: PASS.
- `pnpm run check:workspace`: PASS.
- `pnpm run check:protocol`: PASS, including generated contracts, 10-case conformance, 10-payload compatibility, and 20-policy/13-wire adversarial checks.
- `pnpm run specgrain:check`: PASS; 33 specs, 0 refining, 0 blocked.
- Strict focused TypeScript (`apps/desktop/test/tsconfig.tests.json`): PASS.
- `oxfmt --check` and `oxlint` over the governed implementation surface: PASS.
- Canonical PR exact-head and post-merge CI for every slice above: 6/6 SUCCESS, including Windows kernuxd, Linux JavaScript/TypeScript/Rust jobs, SpecGrain, Diffcipline R1, and Provenance.

Local Rust build/test qualification remains unavailable because the installed Windows target lacks MSVC `link.exe`; CI is the authority for unchanged Rust qualification. This is preserved negative evidence, not waived.

## Native desktop qualification

Probe run `36239099789` qualified the exact canonical evidence source `a0dbe92fc1eacb3cbd349c216dce9a9b2caa9ba2` independently on `ubuntu-latest`, `macos-14`, and `windows-latest`. The probe branch contains only the disposable workflow; every matrix leg checked out the pinned canonical source SHA before qualification, so the workflow itself is outside the SG-000033 implementation surface.

All three native legs completed SUCCESS and independently established:

- `pnpm@12.4.2` with Node 24;
- `pnpm install --frozen-lockfile`, including the repository supply-chain lockfile policy: PASS;
- `pnpm run test:ui-system`: 15/15 PASS, 5 suites, 0 failures;
- canonical token authority and hostile token lookup checks: PASS;
- WCAG-AA semantic color-pair checks and malformed-color rejection: PASS;
- semantic primitive accessibility, disabled/busy/error/status semantics, and non-color-only status communication: PASS;
- focus-visible, reduced-motion, visually-hidden, scalable typography and non-negative motion contract: PASS;
- `pnpm run check:workspace`: PASS;
- KRP generation, conformance, compatibility, and adversarial checks: PASS;
- `pnpm run specgrain:check`: PASS, 33 specs and zero readiness blocks;
- root TypeScript typecheck: PASS;
- `oxfmt --check`: PASS;
- `oxlint`: 0 warnings / 0 errors;
- frozen WorkPacket digest: `sha256:f6d73032728b8f1fe28dc086393639ed1240da32cfff15451f5a8ec82d43f646` PASS;
- final exact-SHA and clean-worktree assertions: PASS.

Native platform details observed in the successful run:

- Windows: Microsoft Windows Server 2025 `10.0.26100`, runner image `windows-2025-vs2026` version `20260922.246.2`, `win32/x64`, Node `v24.21.0`; full SG-000033 desktop qualification PASS.
- macOS: macOS `14.8.9` (`23J631`), `macos-14-arm64`, `darwin/arm64`; full SG-000033 desktop qualification PASS.
- Linux: `ubuntu-latest`; full SG-000033 desktop qualification PASS.

### Native-probe negative evidence preserved

Two earlier probe attempts failed closed on Windows before any SG-000033 test executed:

- `36238922659`: Windows failed during `actions/setup-node` package-manager cache discovery because the `pnpm/action-setup` shim layout was invalid on the current Windows 2025 runner image. Linux and macOS passed. This was an environment/bootstrap failure, not a product failure.
- `36238981926`: after removing setup-node caching, Windows reached dependency installation but the same `pnpm/action-setup` global-layout shim remained broken (`...global\\v11...\\pnpm` not recognized). Linux and macOS passed. No SG-000033 Windows test ran on this candidate.
- The probe was repaired structurally by removing `pnpm/action-setup` and installing pinned `pnpm@12.4.2` directly after Node 24. Run `36239099789` then passed the unchanged qualification suite on all three platforms. No test, assertion, or gate was weakened.

## Review and accounting evidence

- Jev CLI `0.3.2`, TypeSafe provider, model `jev-1.13.0`, exact slice diffs: no authority/dependency/cost or implementation-defect finding; recorded probabilities were PR-C1 `0.37/0.08`, PR-C2 final `0.33/0.04`, PR-C3 `0.53/0.11`, and PR-D `0.29/0.09` for the targeted defect question and negative direction. No blocking finding was returned.
- Alibaba OpenCodeReview `1.12.9` Delegation Mode accounting only, no semantic OCR claim: PR-C1 4 total/4 reviewable/0 excluded; PR-C2 4/4/0; PR-C3 4/4/0; PR-D 5 reviewable/1 excluded default test path. Accounting does not constitute semantic review approval.
- Independent Cline reviewer invocation was unavailable due authorization errors. This is tooling negative evidence, not approval.

## Current evidence status

The following SG-000033 evidence is now established by the canonical implementation chain plus native probe `36239099789`:

- token authority, contrast, semantic primitive, accessibility-state, focus/reduced-motion, design-conformance, renderer-authority-preservation and hostile-input evidence;
- durable packet persistence and frozen packet digest;
- full baseline qualification;
- Linux desktop qualification;
- Windows desktop qualification;
- macOS desktop qualification;
- implementation PR and post-merge CI evidence;
- Jev review evidence;
- Alibaba OpenCodeReview accounting evidence.

## Unproven gates

The following required SG-000033 evidence remains honestly unproven and prevents PROVEN status:

- native SpecGrain `verify_execution` / `prove` with zero issues and an immutable `.specgrain/evidence/SG-000033` record;
- protected closeout PR and its post-merge CI;
- final closeout semantic review / zero blocking review threads.

No ledger transition, native evidence record, protected closeout, or `PROJECT_COMPLETE` claim is made here. The next lawful action is protected closeout preparation and SpecGrain dry verification, not later P03 feature implementation.

## Protected closeout — pre-proof candidate

Canonical pre-closeout main is `70ec02989b738e05621798e7b420d2e4cd6cc77f`, the ordinary merge of PR #239. Its post-merge CI run `36239490302` completed SUCCESS. No implementation file changed after the already-qualified SG-000033 implementation/evidence chain.

A fresh independent dry verification was executed by probe run `36243087264` against exact canonical source `70ec02989b738e05621798e7b420d2e4cd6cc77f`. The unchanged qualification suite completed SUCCESS on Linux, macOS-14, and Windows, including 15/15 design-system tests, workspace/protocol/SpecGrain/typecheck/format/lint checks, the frozen packet digest, and clean exact-SHA assertions.

The pinned SpecGrain dry `verify_execution` deliberately did not append evidence. It bound the frozen WorkPacket to the 15 implementation/spec/packet paths and returned exactly the expected closeout-dependent negative result:

- `verified=false`;
- acceptance: `7/9` PASS;
- required evidence: `19/22` PASS;
- issues: exactly `5`;
- result digest: `sha256:4ee50d787e326d7c238075cdf1ffd405c7a0822585c661030897993498a989e5`;
- failed acceptance only: `Canonical closeout advances only KX-P03-S02-T01 scope` and `Native SpecGrain verification binds the WorkPacket to observed evidence with zero issues`;
- failed evidence only: `protected-closeout-pr-pass`, `closeout-post-merge-ci-pass`, and `native-specgrain-verification-pass`.

No scope mismatch, packet mismatch, spec-revision mismatch, missing check, unscoped path, or executor-status issue was reported. This is expected negative evidence: the protected closeout and its post-merge CI cannot truthfully exist before this closeout merges, and the immutable native proof record must not be appended before those conditions are satisfied.

The canonical ledgers remain unchanged by this pre-proof closeout candidate: `KX-P03-S02-T01` is not yet PROVEN, later P03 work is not authorized by this document, and no project-completion claim is made.

Pending after this closeout PR obtains exact-head qualification, semantic review with zero blocking findings, an ordinary merge, and successful post-merge CI: rerun native SpecGrain verification with all 9 acceptance checks and all 22 evidence checks satisfied, append the immutable SG-000033 evidence record, then carry that record plus the single-task ledger transition in a dedicated proof PR.

Status remains `IMPLEMENTED_UNPROVEN` until that proof chain completes.
