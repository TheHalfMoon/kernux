# Evidence — KX-P00-S01-T02

## Task

`KX-P00-S01-T02 — Add baseline CI for Rust/TS format, lint, static checks, unit placeholders and cache discipline.`

Risk: `R1`

## Qualified implementation

Final implementation head:

`dba4cb455302bf33fdda6aa268e9df472eeb6b3c`

Merged to `main` through PR #5 at:

`646fda9f784ce2368f534a56d8a454ae27b87a35`

## Proven change

T02 established:

- pinned Node 24 / pnpm 12.4.2 CI execution;
- committed `pnpm-lock.yaml` and `Cargo.lock`;
- `pnpm install --frozen-lockfile`;
- pnpm cache keyed by the committed lockfile;
- Oxfmt formatting checks;
- Oxlint lint checks;
- TypeScript 7 strict type checking;
- Node built-in workspace smoke tests;
- Rust 1.98.1 setup;
- rustfmt checks;
- Clippy with warnings denied;
- Rust smoke tests;
- pinned GitHub Action revisions;
- bounded CI timeouts and superseded-run cancellation.

No Kernux product code or founding-donor source was introduced.

## Qualification evidence

Final PR-head workflow run:

`35287273907`

Observed:

- JavaScript / TypeScript: SUCCESS;
- Rust: SUCCESS.

Post-merge `main` workflow run:

`35287358701`

Observed:

- JavaScript / TypeScript: SUCCESS;
- Rust: SUCCESS.

At final PR qualification:

- PR #5 was mergeable;
- open review threads: 0;
- CodeRabbit status: SUCCESS.

The committed `pnpm-lock.yaml` matched the GitHub Actions-generated bootstrap artifact byte-for-byte with SHA-256:

`b91f67680351f3bdb8185ed580df7557d67281ce2cfab43ec831d6a40b5df610`

The temporary bootstrap artifact path was removed before final qualification. Final CI uses only committed lockfiles and frozen dependency installation.

## Local verification

Before the final lockfile/cache commit, the exact candidate was exercised locally with:

```text
pnpm install --frozen-lockfile
pnpm check:workspace
pnpm check:js
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets
git diff --check
```

Observed JavaScript/TypeScript smoke tests: 1 passed, 0 failed.

Observed Rust smoke tests: 1 passed, 0 failed.

## Acceptance conclusion

**PROVEN** for the bounded T02 outcome.

This evidence establishes baseline repository CI and dependency/cache discipline. It does not establish SpecGrain, Diffcipline, donor provenance enforcement, security qualification, product-runtime behavior, or release readiness.

## Recovery

Recovery is a normal revert of PR #5 / merge commit `646fda9f784ce2368f534a56d8a454ae27b87a35`. No user data or product runtime state was created.
