# Evidence — KX-P01-S05-T02

## Task

`KX-P01-S05-T02 — Define compatibility/versioning rules and unknown/additive-field behavior.`

Primary SpecGrain:

`SG-000014 — KX-P01-S05-T02 — Freeze KRP v1 compatibility, version negotiation, and additive-field rules`

Corrective SpecGrain:

`SG-000015 — KX-P01-S05-T02 corrective — Authorize schema-digest propagation to shared fixture derivatives`

Risk: `R2`.

## Outcome

KRP v1 now has one canonical compatibility policy at `docs/canonical/KRP_COMPATIBILITY.md`.

The policy freezes:

- `krp/1` as the exact protocol compatibility family;
- capability version `1` as the exact current capability-version token;
- schema SHA-256 as provenance identity, not an independently negotiated wire version;
- closed-object decoding for the current wire contract;
- fail-closed handling for unknown fields, unknown closed enum meanings, unsupported protocol/capability versions, and missing required fields;
- exact capability admission by action + capability version, with no unknown-action fallback;
- feature-negotiated additive optional evolution with sender suppression for unsupported peers;
- optional-field absence as absence only, never fabricated authorization, execution success, side-effect certainty, or negotiated feature support;
- protocol mismatch as an admission/interpretation failure that cannot rewrite runtime contact, execution, cancellation, or side-effect truth;
- unsupported or unnegotiated Event types as non-projecting and non-authorizing.

Authoritative schema SHA-256:

`7c8ec2e44777a35c73414da488394ef482a7db9510ba1bda7419929c3bbf03a9`

Generated shared core fixture digest:

`41570fc8bc1d9f88bdd9fe9e770b74cb4e600fa85e4ee50d305bfabb81dff454`

The final focused matrix proves `10 payload`, `1 truth`, `2 event-projection`, `3 capability-admission`, and `3 feature-gate` cases.

The full adversarial protocol corpus remains owned by `KX-P01-S05-T03`.

## Authority chain

- PR #66 refined SG-000014; merge `aefc90d753af6d0b2111976bcc38d2d675d7f352`; post-merge run `35416156937` passed all five protected jobs.
- PR #67 refined corrective SG-000015; merge `0eca5e6436c96af7fc168ac723fd47d09ecf95da`; post-merge run `35417157846` passed all five protected jobs.

SG-000015 authorizes deterministic schema-digest propagation only to:

- `protocol/fixtures/v1/core.json`;
- `packages/contracts/src/conformance.ts`.

It does not authorize semantic fixture changes or S05-T03 adversarial scenarios.

## Implementation chain

PR #68 — `feat: bind KRP v1 versions and strict decoding`

- exact head `3a972070f913c9ef6279f326b8bb664ddf88714f`;
- exact-head CI `35417387938` — five protected jobs PASS;
- merge `713112dcf6bd07d9b6150ff098c997841e647c59`;
- post-merge CI `35417459665` — five protected jobs PASS.

PR #69 — `test: enforce KRP compatibility matrix`

- exact head `f24965613519f943111539889bd694caf3e5a805`;
- exact-head CI `35417712069` — five protected jobs PASS;
- merge `e2dc2e73123ba4d36e1e872d756305d4c8e8df49`;
- post-merge CI `35417838175` — five protected jobs PASS.

PR #70 — `docs: freeze KRP compatibility policy`

- exact head `d934c7b777d05b1782cbffdc2c52ce16e97d4077`;
- exact-head CI `35418272661` — five protected jobs PASS;
- merge `babc07970a08a52013510dd41dae46f029211458`;
- post-merge CI `35418314487` — five protected jobs PASS.

PR #71 — `test: close KRP compatibility proof gaps`

- exact head `ab256ab7fa676d4e0c86f7378be533114b7aafb0`;
- exact-head CI `35418868252` — five protected jobs PASS;
- merge `5f805fc014da24a17c1fe330c45c284384deb400`;
- post-merge CI `35418918294` — five protected jobs PASS.

PR #71 added focused proof that optional parent-Grant metadata cannot alter explicit bounded authority, optional termination cause cannot fabricate successful execution from `unverifiable`, unknown closed enum values reject, and capability admission requires exact action + version matching.

A deliberate permissive capability-admission expectation was rejected by the checker as negative evidence.

## Independent local verification

Final qualified main implementation revision:

`5f805fc014da24a17c1fe330c45c284384deb400`

Independent checks passed:

- `pnpm check`;
- `git diff --check`;
- generator `--check`;
- shared Node conformance over all eight core fixtures;
- focused Rust compatibility tests;
- shared Rust fixture round trip;
- focused compatibility matrix;
- provider-neutral core scan;
- schema/version binding checks;
- core fixture semantic identity relative to the pre-change fixture;
- generated schema/core fixture digest binding.

Generator drift was deliberately introduced in an isolated worktree and the generator check rejected it.

Diffcipline R2 independently passed for all four implementation slices:

- PR #68: 7 files, `+127/-22`;
- PR #69: 4 files, `+441/-5`;
- PR #70: 6 files, `+327/-3`;
- PR #71: 3 files, `+138/-1`.

## Native SpecGrain verification — SG-000014

- spec revision: `sha256:a833153c9946e90cffc77202779a474e343bac197826e01c964b610682a49763`;
- WorkPacket: `sha256:bb14f760b0d579bf8e3353f7b1d3f3bc3bf707d7d2e8313777ad4548e90cebe7`;
- context-plan digest: `sha256:7423ba825e87eb8d8c2106583bccbbc41e9750137730573e69e730566aa3e297`;
- implementation revision: `git:5f805fc014da24a17c1fe330c45c284384deb400`;
- verification: `verified=true`;
- issues: `[]`;
- result digest: `sha256:5adf70f2570bb960f23b39d98721c46caca6dd4f4f90bf9483467b684c495441`;
- evidence record: `sha256:a022dc5a6c2c52bf1dfcd988a29384d6eadd2d0c23553162ed4fe2f7ee0613b4`.

The record was appended through the pinned native SpecGrain API.

## Native SpecGrain verification — SG-000015

- spec revision: `sha256:fab5a58540c28cf7ff8e6059e7b16c28855318f4d594c4f0cfeb19fbc9eccad8`;
- WorkPacket: `sha256:dfd2eb11836d264a2989a80057f3c90b2b02d004f8712eafb8a807b32e38c9b5`;
- context-plan digest: `sha256:fb8d3d333a646930b7343f7dc2c8afbcea6ac5c5eeae79d77d20950193bb9764`;
- implementation revision: `git:713112dcf6bd07d9b6150ff098c997841e647c59`;
- verification: `verified=true`;
- issues: `[]`;
- result digest: `sha256:aab28173f3701d3a84879b81e93863b11fb3ab7af856bfbd0f6fc16f8f68eb0e`;
- evidence record: `sha256:82b6cc51af7b85bcd416bfcf44d3c3f2e4e0b930b8612e89ef162192f8c1203a`.

The record was appended through the pinned native SpecGrain API.

## Scope accounting

SG-000014 observed implementation paths are exactly the generated Rust and TypeScript contract outputs, Rust focused compatibility tests, canonical compatibility/protocol/runtime/ADR documentation, package protocol gate, focused compatibility fixture/checker, authoritative schema, and deterministic generator.

SG-000015 observed implementation paths are exactly `packages/contracts/src/conformance.ts` and `protocol/fixtures/v1/core.json`.

No unscoped implementation path is claimed by either native proof.

## Governance verification

The protected branch continues to require JavaScript / TypeScript, Rust, SpecGrain, Diffcipline R1, and Provenance.

No required check was removed, bypassed, or weakened. No force-push, rebase, or shared-history rewrite was used.

## Acceptance conclusion

**PROVEN** for `KX-P01-S05-T02`.

The evidence establishes exact KRP v1 protocol/capability version binding, strict closed decoding, fail-closed unknown-value behavior, optional-absence semantics, exact capability admission, feature-negotiated additive evolution, runtime-truth preservation under incompatibility, and non-projecting unsupported Event semantics.

This does not prove the full adversarial/negative protocol corpus or the P01 exit gate.

## Next frontier

`KX-P01-S05-T03 — Build canonical protocol fixture corpus including disconnect, duplicate operation, malformed payload and version mismatch.`

Its generated-contract and compatibility dependencies are now PROVEN.

