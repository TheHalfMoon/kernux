# Evidence — KX-P02-S02-T01

## Task

`KX-P02-S02-T01 — Implement installation/device identity and key creation/rotation/recovery abstraction.`

SpecGrain:

`SG-000020 — KX-P02-S02-T01 — Establish local installation identity and key lifecycle`

Risk: `R3`.

## Current closure state

The bounded SG-000020 implementation is merged and qualified on `main`, but the task remains canonically `NEXT` until native SG-000020 verification is appended to the protected closeout PR, that proof-bearing head passes fresh exact-head qualification and review, the PR merges through the ordinary protected path, the resulting `main` revision passes post-merge qualification, and a separate canonical transition records the next frontier.

This pre-proof closeout does **not** authorize `KX-P02-S03-T01` or any later P02 work.

Final qualified implementation revision:

`8f7a43361a0a0d44202e336358e64a47d7686cfc`

Final implementation post-merge workflow:

`35441914338` — six jobs SUCCESS on the exact revision above, including the five protected contexts plus the additive native `Windows kernuxd` evidence job.

## SpecGrain binding

The governing Grain remains exactly bound to:

- spec revision: `sha256:062adf2889f3971093369d935e7d5d52eb1072c01b522851519d6527f5b63e3b`;
- context-plan digest: `sha256:d82af7ffbf3854138ed5c98784d3afd3370fac89f2522a2c82660405669a57e7`;
- WorkPacket digest: `sha256:aad1ff66dceb5e7f6333a3e97d7640163518d3c4324dd1ec130b86e866cc2e8a`;
- context sources: 17 required sources / 8992 tokens within the 18000-token budget;
- acceptance criteria: 13;
- required evidence IDs: 18;
- authorized change surface: 10 paths.

The pinned SpecGrain API reloaded the original WorkPacket successfully on final implementation `main`. `SpecNode.revision_digest` equals the packet `spec_revision`, and the packet digest remains exact. No WorkPacket was regenerated from post-implementation source bytes.

The first context-plan attempt correctly failed closed at 51,419 required tokens against the 18,000-token policy budget. The budget was not raised. Context was narrowed to exact required line ranges/API metadata and the successful 8,992-token packet above was then produced.

## Grain refinement

PR #99 — `docs: refine local identity grain`

- exact head: `126fec85103870e20385211cc855d0c438f3299a`;
- exact-head workflow: `35436371306` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOT1new` — no findings;
- merge: `a1395b5df3dc918be383abd1da0052f75e10f2e6`;
- post-merge workflow: `35436442167` — six jobs SUCCESS.

No SG-000020 implementation began before the refinement merged and fresh `main` qualification succeeded.

## Dependency admission

PR #100 — `build: admit local identity dependencies`

- exact head: `6597ca7e29f2070b13f548fb806f47d271f64e1b`;
- exact-head workflow: `35436690932` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOT524w` — no findings;
- merge: `1799c136025fed42b00c09ca462df1325374acb8`;
- post-merge workflow: `35437582982` — six jobs SUCCESS.

Exact admitted direct dependencies:

- `ed25519-dalek = 3.0.0`, `default-features = false`, direct feature `zeroize` only;
- `getrandom = 0.4.3`, `default-features = false`;
- `zeroize = 1.9.0`, `default-features = false`.

The dependency validator initially failed closed before the governed exception-evidence path existed. That failed attempt remains FAIL. After exact license/feature review and evidence creation, `python3 tools/dependencies/validate.py check` passed with nine direct external dependencies.

The dependency slice contained only the workspace/crate shell, exact dependency/lock/registry changes, CI extension, and evidence. It did not contain identity semantics.

## Local identity core

PR #101 — `feat: add local identity core`

- exact head: `6886dde9becb1734223c54d34394955ee7a7b590`;
- exact-head workflow: `35437795774` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOT6s8w` — no findings;
- merge: `30e296107d7b45efbe0bd06d22629e0cde32ac7e`;
- post-merge workflow: `35437865644` — six jobs SUCCESS.

The core established:

- `InstallationId` as an opaque non-secret exact 16-byte value;
- `InstanceId` as an opaque non-secret exact 16-byte value;
- strict 32-lowercase-hex canonical text for both IDs;
- rejection of uppercase, non-hex, whitespace, wrong length, and alternate encodings;
- production ID generation using `getrandom::fill` OS entropy;
- positive `KeyGeneration`, initial value 1, zero rejection, and checked non-wrapping increment;
- Ed25519 `SigningKey` creation from a fresh exact 32-byte OS-random seed;
- immediate zeroization of the temporary seed after key construction;
- public descriptor containing only InstallationId, generation, and the exact Ed25519 verifying key;
- strict 64-lowercase-hex public verifying-key representation and parsing;
- no public private-key/keypair export, generic signing API, serde secret surface, or persistence API.

A guessed negative verifying-key vector was found to be valid by `ed25519-dalek`; that test failed and was not relabeled. A real canonical-hex invalid point fixture was then discovered using the exact library API and the corrected test passed. An overbroad manual source assertion also confused public verifying-key bytes with secret-key export; it remained a failed assertion and was replaced by a semantic public-API check.

## Key rotation

PR #102 — `feat: rotate local identity keys`

- exact head: `854f776288fc1b76f91a605719968711556f69a1`;
- exact-head workflow: `35437989949` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOT8H1Q` — no findings;
- merge: `e1578c5e3eb1eee48576e0c9155f4fced36889b3`;
- post-merge workflow: `35438065373` — six jobs SUCCESS.

Rotation:

- checks generation overflow before entropy use or identity mutation;
- preserves InstallationId exactly;
- increments generation exactly once;
- creates a fresh independent Ed25519 key using the admitted OS-random path;
- returns explicit previous/current **public** lineage descriptors;
- replaces the active signing key rather than retaining the retired private key;
- proves the new key signs/verifies while the retired public key rejects the new-key signature;
- does not imply remote revocation, policy authority, or durable storage.

The pinned `ed25519-dalek` zeroize feature supplies secret-key drop zeroization; no private-key copy is retained by the Kernux rotation surface.

## Key recovery

PR #103 — `feat: recover local identity keys`

- exact head: `a3a6edbaa412857c0e7e3e88262de9d4e3b2d020`;
- exact-head workflow: `35438300862` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOT_itw` — no findings;
- merge: `dfab00b905b08df843a45bcb98bac122c8d52b0b`;
- post-merge workflow: `35438377231` — six jobs SUCCESS.

Recovery:

- uses `RecoverySeed` as an exact 32-byte secret wrapper;
- `RecoverySeed` is not `Clone` or `Copy` and has no `Debug`, `Display`, serde, or raw-secret accessor;
- implements explicit `Zeroize` and drop-time zeroization;
- uses exact domain `kernux.identity.recovery/v1\0`;
- binds InstallationId, positive key generation, and verifying key in the recovery reference;
- reconstructs an Ed25519 key only from caller-supplied recovery material;
- requires the derived public key to equal the expected public descriptor;
- validates the bound recovery reference with strict Ed25519 verification;
- rejects wrong recovery material and tampering of InstallationId, generation, verifying key, or recovery signature with generic non-secret `RecoveryMismatch`;
- does not persist or source recovery material from a credential store.

Deterministic recovery inputs exist only in executable tests. No test seed or private-key material is copied into this evidence packet.

## Identity session binding

PR #104 — `feat: bind local identity sessions`

- exact head: `46b31110ce9b95f0c057ee17ad0e3949e6d302ef`;
- exact-head workflow: `35441840324` — six jobs SUCCESS;
- exact-head review: `PRR_kwDOUevqks8AAAABOUMi8Q` — no findings;
- merge: `8f7a43361a0a0d44202e336358e64a47d7686cfc`;
- post-merge workflow: `35441914338` — six jobs SUCCESS.

Session binding:

- uses exact domain/version separator `kernux.identity.session/v1\0`;
- binds current InstallationId;
- binds current positive key generation;
- binds current Ed25519 verifying key;
- binds exact 16-byte InstanceId;
- binds caller-supplied exact 32-byte `SessionChallenge`;
- signs only the field-complete domain-separated binding contract;
- validates with strict Ed25519 verification;
- rejects tampering of InstallationId, generation, verifying key, InstanceId, challenge, signature, and domain/version;
- exposes signature bytes only as public proof material;
- introduces no public generic signing method or private-key export.

Two manual review assertions initially failed because they matched source text too broadly or depended on formatting. They remained non-successful assertions and were replaced by semantic checks of the actual public API and strict verification call.

## Cross-platform executable evidence

Final-main local macOS qualification:

`cargo test --locked -p kernux-identity --all-targets` — 18/18 PASS.

Final-main Ubuntu `Rust` job in workflow `35441914338` executes:

- `kernux-identity` unit tests: 8/8 PASS;
- `kernux-identity` integration tests: 10/10 PASS.

Final-main native Windows `Windows kernuxd` job in workflow `35441914338` executes:

- `kernux-identity` unit tests: 8/8 PASS;
- `kernux-identity` integration tests: 10/10 PASS.

The Windows evidence is native `windows-latest` runtime execution. Cross-compilation is not substituted for native Windows evidence.

## Protocol and transport regression

The final SG-000020 implementation range is:

`a1395b5df3dc918be383abd1da0052f75e10f2e6..8f7a43361a0a0d44202e336358e64a47d7686cfc`

Exactly eight paths changed:

- `.github/workflows/ci.yml`;
- `Cargo.lock`;
- `Cargo.toml`;
- `crates/kernux-identity/Cargo.toml`;
- `crates/kernux-identity/src/lib.rs`;
- `crates/kernux-identity/tests/core.rs`;
- `docs/evidence/P02-S02-T01-DEPENDENCY-GATE.md`;
- `third_party/dependencies/approved.json`.

All are inside SG-000020's authorized change surface.

Independent final-main checks prove:

- `git diff --check` over the SG-000020 implementation range — PASS;
- dependency validator — PASS;
- KRP generated contracts — PASS;
- KRP shared fixture conformance — PASS, 10 cases, schema digest `904eb86ee9028aca002586187ade53eefea1a51339bff9904182e95d88751b94`;
- KRP compatibility matrix — PASS;
- KRP adversarial conformance — PASS;
- T02 authentication/lifecycle/local-transport suites remain PASS;
- full `pnpm check` — PASS;
- SpecGrain repository check — PASS.

No KRP schema/generated contract path changed in the SG-000020 implementation range. T02 launch-nonce authentication and local transport semantics remain unchanged.

## Security and scope boundary

Independent review confirms SG-000020 remains limited to:

- non-secret local InstallationId and InstanceId primitives;
- Ed25519 local key creation;
- checked local key generation lineage;
- in-memory key rotation;
- caller-supplied bounded recovery;
- domain-separated local identity session binding;
- dependency/CI support required for those primitives.

It does not implement or claim:

- SQLite, filesystem-backed identity state, WAL, migrations, or restart durability;
- macOS Keychain, Windows Credential Manager/DPAPI, Linux Secret Service/libsecret, TPM, Secure Enclave, HSM, or another credential store;
- capability policy, Grant issuance/enforcement, permission profiles, secret brokerage, or security-audit persistence;
- privileged filesystem/process/PTY operations;
- artifact CAS;
- sandbox, browser, or computer control;
- remote device enrollment, mTLS, cloud/org/user identity, pairing, certificate authority, or remote trust;
- KRP schema changes or T02 transport-auth changes;
- P02 phase completion.

No private-key or recovery-seed value is recorded in this evidence packet.

## Review discipline

Alibaba OpenCodeReview is the review/accounting framework for SG-000020. CodeRabbit, Qodo, and Cubic are not qualification evidence.

Bounded slice accounting:

- PR #99 refinement: 1/1 reviewable, 0 excluded;
- PR #100 dependency admission: 5 reviewable / 2 excluded; `Cargo.lock` and Markdown evidence manually reviewed completely;
- PR #101 identity core: 2/2 reviewable, 0 excluded;
- PR #102 rotation: 2/2 reviewable, 0 excluded;
- PR #103 recovery: 2/2 reviewable, 0 excluded;
- PR #104 session binding: 2/2 reviewable, 0 excluded.

Each implementation slice also received complete manual semantic/security review and an exact-head GitHub review with no findings and zero unresolved review threads. No direct LLM OpenCodeReview success is claimed because no real configured OCR LLM result is used as qualification evidence.

## Failure accounting

Failures and invalid assumptions remain failures rather than being rewritten as successful evidence:

- the first SG-000020 context plan exceeded the token budget and failed closed;
- one OpenCodeReview temp-file write failed when the host filesystem was full;
- one full `pnpm check` failed with host `ENOSPC` during Rust metadata creation;
- an assumed invalid Ed25519 public-key test vector was accepted by the exact library and the test failed;
- two manual source assertions produced false positives because their text matching was overbroad/format-sensitive.

Only subsequent fresh, corrected runs are used as PASS evidence. Reproducible build/Cargo caches were the only material removed to recover from the host disk-pressure event; repository evidence, pinned governance tools, WorkPacket/context files, and source state were preserved.

## Independent final-main qualification

On `main@8f7a43361a0a0d44202e336358e64a47d7686cfc`:

- `git diff --check` over the implementation range — PASS;
- `cargo test --locked -p kernux-identity --all-targets` — 18/18 PASS locally on macOS;
- dependency validator — PASS;
- full `pnpm check` — PASS;
- Ubuntu identity tests — 18/18 PASS in workflow `35441914338`;
- native Windows identity tests — 18/18 PASS in workflow `35441914338`;
- all five protected CI contexts — SUCCESS;
- additive `Windows kernuxd` — SUCCESS;
- KRP protocol surface unchanged — PASS;
- exact eight-path implementation range inside the authorized surface — PASS.

## Protected closeout candidate

This branch is the real protected pre-proof closeout candidate for SG-000020.

At this pre-proof head:

- `KX-P02-S02-T01` remains canonically `NEXT`;
- `KX-P02-S03-T01` and all later P02 tasks remain `PLANNED`;
- P02 phase exit remains unproven;
- native SG-000020 evidence has not yet been appended;
- exact-head protected CI plus additive `Windows kernuxd` must pass before native verification may cite `protected-closeout-pr-pass`.

After exact-head qualification, the pinned native SpecGrain API may append the immutable SG-000020 verification record to this same PR. The proof-bearing head may record `NATIVE_VERIFIED_PENDING_PROTECTED_CLOSEOUT_MERGE`, but T01 must remain canonically `NEXT` until ordinary protected merge and final post-merge qualification succeed.

## Next frontier

Protected exact-head qualification of this pre-proof closeout candidate, followed by native SG-000020 verification.

No later P02 implementation is authorized before the proof-bearing closeout merges, post-merge qualification succeeds, and a separate canonical transition establishes the next frontier.
