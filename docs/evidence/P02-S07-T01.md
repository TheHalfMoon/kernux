# Evidence — KX-P02-S07-T01 (implementation, UNPROVEN)

Task: `KX-P02-S07-T01 — Define and implement kernel egress-class/destination policy primitives for NONE, DIRECT_DESTINATION, CONNECTED_ACCOUNT, EXTERNAL_MODEL, EXTERNAL_TOOL, REMOTE_RUNTIME, UPDATE and TELEMETRY; unknown sensitive egress fails closed.`
Grain: `SG-000029`, state `GRAIN`, risk `R3` (high).
Status: `IMPLEMENTED_UNPROVEN`. No advancement claimed. P02 stays `9/10`, exit unproven.
Ledgers (`specs/CURRENT.md`, `specs/tasks.md`) are intentionally untouched by this record.

## Canonical implementation range

- Pre-implementation base (canonical main): `a55832a5e5f6b2cf2abd6ac72c756c8ab2a01235`
- PR-A (#201) head: `7e3f87a220057972ea3a2b8541bc8af5f6411e22`
- PR-A merge: `a9f697c933c583133fd02e575a801531991c2b2c` (2026-09-23T20:48:35Z, ordinary merge)
- PR-B (#203) head: `27736f76d68745151fb19c761838eaf90538e576`
- PR-B merge: `912bbf7af6f12d03ba94ed8faf43a5aa65934595` (2026-09-24T07:41:51Z, ordinary merge)
- PR-C (#204) head: `789331a14ad7202338499bc0790f2aebe141a593`
  (corpus head `c49c08e` plus one additive fixture-fix commit; never force-pushed)
- PR-C merge: `8d9cb45338b7baeb93e7212419524f48aaf26906` (2026-09-24T07:55:54Z, ordinary merge)
- Combined range `a55832a...8d9cb45`: exactly 2 files, `+1569/-0`
  (`crates/kernux-policy/src/egress.rs` +1564/-0, `crates/kernux-policy/src/lib.rs` +5/-0).
  Per-slice numstat (+485, +555/-6, +535) sums higher because PR-B replaced 6
  PR-A module-doc lines; the combined range is authoritative.
- No Cargo manifest drift, no lockfile drift, no `protocol/` drift, no ledger
  change across the whole range (verified empty by probe surface steps on both platforms).
- `docs/evidence/P02-S07-T01.md` is this file (IMPLEMENTED_UNPROVEN; advances nothing).

## Packet identity (frozen)

- WorkPacket digest: `sha256:fedbd524f487e3acbd7defc979dfd32a28be1217ac74b47d6d47a762892137ca`
- Context-plan digest: `sha256:6b1328622e66ca24c8022587c4892f703bbfd470c30e4c66cec279728cdbc560`
- Spec revision: `sha256:8680129138f08eb434e729a28136d3d6902881eaee91929c92e60b97c61d9bc8`
- SpecGrain pin: `TheHalfMoon/SpecGrain` commit `5de7d6499bb0a9e3a191fc0934399cf099d1980a`
  (archive SHA-256 `da6a8c006c3a0154e76e2bf08bcae58bb86e04f06dd7a03115275b466489eef9`)
- WorkPacket file SHA-256: `3699d36ea9a30ebdfd75e2c0b9ca05107811b1692076132da9fb5d4571adba37`
  (10141 bytes; context sources 7206 bytes, 17 sources).
- Byte-identical WorkPacket replay (`cmp` PASS) on probe Linux and macOS-14 for
  all three slice heads. Local Windows replay reproduces identical JSON content;
  the only byte difference is stdout CRLF vs LF, a platform artifact of console
  capture, not a packet mismatch. The Grain and packet were not modified.
- `prove SG-000029`: `verified=false`, `record_count=0` (correct: native
  verification is a later dependent step, no record hand-authored).
- `prove SG-000028`: `verified=true`, `record_count=1` (prior proof preserved).

## PR-A qualification (head 7e3f87a)

- Scope: 2 files (`crates/kernux-policy/src/egress.rs` +480, `crates/kernux-policy/src/lib.rs` +5).
  Closed `EgressClass` vocabulary with exact parsing, stable strings,
  `requires_destination`, validated `EgressHost`/`EgressAccount`/`EgressRuntimeId`,
  `EgressDestination`, and exact class/destination structural binding. No Grant
  evaluation, no authority minting, no provider coupling, no I/O.
- Exact-head CI run `35915267257`: 6/6 SUCCESS (Rust, JS/TS, SpecGrain,
  Diffcipline R1, Provenance, Windows kernuxd). Preserved, never re-run on an unchanged head.
- Probe run `35917714275` (workflow `pr201-egress-a-qualification`):
  Linux and macOS jobs SUCCESS, every step green.
- Diffcipline R3 with `run-verification=true`, action
  `TheHalfMoon/Diffcipline@5cb1c77340b75649f6168e0e8f66479ea047ea96` (v1.0.0):
  base `a55832a`, 2 files, `+485/-0`, dependencies unchanged, lockfiles
  unchanged, untracked 0, scope PASS, verification PASS (`pnpm check`),
  final verdict PASS — on Linux and macOS-14 independently.
- Full baseline `pnpm check` PASS both platforms; focused `kernux-policy`
  regression plus secret/store/daemon suites PASS both platforms.
- Genuine Jev (`jev 0.3.2`, provider TypeSafe, model `jev-1.13.0`,
  base `a55832a`, head `7e3f87a`): 8/8 answer no — authority-widening 0.04,
  permissive-unknown 0.02, destination-confusion 0.04, cloud-fallback 0.02,
  provider-coupling 0.04, secret-leak 0.08, krp-drift 0.13, scope-overclaim 0.03.
  No blocking findings. Disposition clean.
- Alibaba OpenCodeReview `1.12.7` (`85cecfe`, windows/amd64 binary SHA-256
  `7c40f72363c66bd49aa16ddde48ddb458eed6d8492c3a0c35a94d6a9bac1bb64` verified),
  Delegation Mode accounting only: merge_base `a55832a`, total 2 / reviewable 2 /
  excluded 0, `+485/-0`, `excluded_files: []`. Same accounting on probe Linux
  and macOS-14. No model-backed review claimed.
- Native macOS: `macos-14-arm64`, ProductVersion `14.8.9`, BuildVersion `23J631`, arm64.
- Windows: exact-head CI `Windows kernuxd` SUCCESS (local `cargo` blocked by
  missing `link.exe`; CI is the Rust authority per precedent).
- Semantic review of exact head: eight exact classes only; case-sensitive closed
  parsing; unknown rejected; `NONE` destination-free; every external class bound
  to its exact destination shape; account/runtime cannot masquerade as host;
  no provider meaning; no grant bypass; no authority creation; no network I/O;
  no cloud fallback; no secret leakage; no KRP changes. No defects.
- Reviews: 0 formal. Threads: 0 blocking (0 PR comments; only bot skip notices).
- Post-merge CI run `35918418274` on `a9f697c`: 6/6 SUCCESS.

## PR-B qualification (head 27736f7)

- Scope: 1 file (`crates/kernux-policy/src/egress.rs`), `+555/-6` (the 6
  deletions replace the PR-A module-doc scope note; no PR-A semantic change).
  Pure deterministic grant-constraining evaluator: `EgressRequest`,
  `AuthoritativeEgressConstraint`, `EgressGrantBinding` (re-runs the canonical
  Grant matcher; unforgeable from a bare decision), `EgressDenyReason` (six
  stable kebab-case codes), and `evaluate_egress_constraint` (operation, class,
  destination, resource-host/runtime, network-allowlist, and sensitivity checks;
  unknown sensitivity fails closed). No I/O, no authority minting.
- Exact-head CI run `35920766134`: 6/6 SUCCESS.
- Probe run `35920791174` (workflow `pr203-egress-b-final4-qualification`,
  TARGET pinned to `27736f7`): Linux (ubuntu-24.04) and macOS-14 jobs SUCCESS,
  every step green, including R3 Verdict PASS + Verification PASS, replay `cmp`
  PASS, `pnpm check`, focused and regression suites, and OCR accounting
  (1/1/0, `+555/-0`) on both platforms.
- Genuine Jev (same CLI/provider/model, base `a9f697c`, head `27736f7`):
  8/8 answer no — authority-minting 0.06, permissive-evaluation 0.02,
  substitution 0.09, cloud-fallback 0.03, provider-coupling 0.04,
  secret-leak 0.08, krp-drift 0.12, scope-overclaim 0.05.
  No blocking findings. Disposition clean.
- Alibaba OpenCodeReview `1.12.7` Delegation Mode accounting only:
  merge_base `a9f697c`, total 1 / reviewable 1 / excluded 0, `+555/-0`,
  `excluded_files: []`, on probe Linux and macOS-14.
- Native macOS-14 arm64 (14.8.9, 23J631) PASS; Windows via exact-head CI PASS.
- Semantic review: evaluation only RESTRICTS existing authority (Grant binding
  first, egress narrows second); operation/class/destination/account/runtime
  exact matching; resource-host/runtime cross-checks against the canonical
  Grant resource; network-allowlist enforcement; stable redaction-safe denials;
  grant IDs are non-secret handles consistent with audit projections. No defects.
- Reviews: 0 formal. Threads: 0 (0 PR comments).
- Post-merge CI run `35971021047` on `912bbf7`: 6/6 SUCCESS.

## PR-C qualification (head 789331a)

- Scope: 1 file (`crates/kernux-policy/src/egress.rs`), `+535/-0`, test-only.
  Nine adversarial tests (synthetic data only): unknown-input rejection;
  exhaustive 8x4 missing-destination matrix (8 valid, 24 rejected);
  cross-class substitution at the same host (`DIRECT_DESTINATION` vs
  `EXTERNAL_MODEL`, `EXTERNAL_TOOL` vs `UPDATE`, `TELEMETRY` vs others,
  account vs tool, `NONE` vs model); grant-resource substitution (host,
  runtime-A vs runtime-B, network allowlist allow+deny); authorized-operation
  divergence; relabeling-exhaustion proof (16 class/sensitivity combinations,
  exactly 1 Eligible — no cloud fallback); exact Debug-surface locking;
  denial-to-audit-code projection (6 unique kebab-case codes, UUID grant handles).
- Exact-head CI run `35971871519`: 6/6 SUCCESS.
- Probe run `35972003724` (workflow `pr204-egress-c-qualification`, TARGET
  pinned to `789331a`): Linux and macOS-14 jobs SUCCESS, every step green,
  including R3 Verdict PASS + Verification PASS, replay `cmp` PASS, `pnpm check`,
  focused `kernux-policy` plus secret/store/daemon suites, and OCR accounting
  (1/1/0, `+535/-0`) on both platforms.
- Genuine Jev (same CLI/provider/model, base `912bbf7`, head `789331a`):
  8/8 answer no — authority 0.07, fail-closed 0.07, class-confusion 0.14,
  no-fallback 0.06, provider/secrets 0.08, krp/drift 0.07, corpus-quality 0.38,
  scope 0.12. The 0.38 flags intentional exact-Debug-string brittleness that
  locks the surface against future secret leakage; adjudicated non-blocking,
  still no. No blocking findings. Disposition clean.
- Alibaba OpenCodeReview `1.12.7` Delegation Mode accounting only:
  merge_base `912bbf7`, total 1 / reviewable 1 / excluded 0, `+535/-0`,
  `excluded_files: []`, on local windows/amd64 plus probe Linux and macOS-14.
- Native macOS-14 arm64 (14.8.9, 23J631) PASS; Windows via exact-head CI PASS.
- Semantic review: corpus asserts evaluation outcomes only; helpers mirror the
  proven Grant/capability shapes; runtime tests use matrix-approved
  `computer.observe` over `kernux://runtime/` resources; the 255-byte fixture
  deterministically exceeds the 253-byte bound (fixed forward, see negative
  evidence). No defects.
- Reviews: 0 formal. Threads: 0 (0 PR comments).
- Post-merge CI run `35972325065` on `8d9cb45`: 6/6 SUCCESS.

## Security invariants (held across all three slices)

- Egress evaluation only RESTRICTS existing authority; it never mints or widens
  authority. Missing/wrong Grant, class, destination, account, runtime,
  operation, or incomplete sensitivity always DENY.
- No silent cloud fallback: local/`NONE` failure remains failure; the
  relabeling-exhaustion test proves no class/sensitivity relabeling of a denied
  request can yield Eligible.
- Class separation holds structurally and at evaluation:
  `DIRECT_DESTINATION != EXTERNAL_MODEL`,
  `CONNECTED_ACCOUNT != arbitrary EXTERNAL_TOOL`,
  `UPDATE != EXTERNAL_TOOL`, `TELEMETRY != arbitrary destination`,
  `NONE != network authority`, `REMOTE_RUNTIME != local execution`
  (exact runtime-identity binding).
- No plaintext secrets in decision objects, Debug/Display, errors, or denial
  reasons (exact-rendering locks plus forbidden-substring guards).
- No KRP schema change (v1 frozen), no new secret reveal authority, no
  secret-provider changes, no daemon changes, no ledger advancement.
- Decisions project to the existing redaction-safe audit vocabulary (stable
  kebab-case codes plus UUID grant handles) without a new authority path;
  SG-000028 is not rewritten.

## Negative evidence (preserved, not waived)

- PR-C fixture defect on first head `c49c08e`: a 127-byte two-label hostname
  (`a`*63 + `.` + `b`*63) is legitimately VALID, so
  `unknown_inputs_fail_closed` failed at `egress.rs:1231` in exact-head CI
  `35971586095` (Rust + R1 jobs) and identically on the Windows job and probe
  `35971670743`. Fixed structurally with a 255-byte four-label fixture
  (deterministically over the 253-byte bound, 1 line to 7 lines); no
  suppression, no gate weakened. Fix-forward head `789331a` green everywhere.
- CI-side Jev gate attempt without credentials: probe `35921203543`
  (`TYPESAFE_API_KEY_UNAVAILABLE`, exit 3). Genuine Jev runs locally where the
  key exists; nothing was weakened or skipped.
- Local Windows limitations: `link.exe` absent (no local cargo test/clippy);
  local pnpm 11.x vs required 12.x; `.cache/specgrain` source dir has an
  orphaned ACL (SpecGrain runs via a clean extraction to `%TEMP%` instead);
  PowerShell `>` redirect appends CRLF (packet replay compared LF-normalized).
  All affected gates ran on authorized runners, never waived.
- Jev argv-splitting and exec-cap lessons from prior tasks retained: all
  recorded scores come from complete, well-formed single-question runs.
- Ambient qodo billing-blocked, CodeRabbit auto-skip, and cubic reviewer-only
  states are limitations, not approvals.

## Outstanding before PROVEN (not claimed here)

1. Native `verify_execution` with the pinned SpecGrain verifier binding the
   WorkPacket to this observed evidence (8/8 acceptance, 22/22 required
   evidence, zero issues), producing the immutable record.
2. Separate protected closeout PR (`.specgrain/evidence/SG-000029/`, this
   record finalized, `specs/CURRENT.md`, `specs/tasks.md`) with its own
   exact-head CI + R3 + Jev + OpenCodeReview + platform + semantic + zero-thread
   qualification, lawful merge, and post-merge CI. Only then may
   `KX-P02-S07-T01` transition NEXT to PROVEN; P02 exit stays unproven.

Status: `IMPLEMENTED_UNPROVEN`. `KX-P02-S07-T01` stays NEXT. P02 stays `9/10`.

## Protected closeout (pre-proof, closeout PR)

Closeout claim: SG-000029 implementation is canonical (PR-A #201 merge
`a9f697c`, PR-B #203 merge `912bbf7`, PR-C #204 merge `8d9cb45`, evidence #205
merge `ed2d381`, each with post-merge CI 6/6 SUCCESS: `35918418274`,
`35971021047`, `35972325065`, `35972988287`). Combined range
`a55832a...8d9cb45` holds exactly `crates/kernux-policy/src/egress.rs` and
`crates/kernux-policy/src/lib.rs` (`+1569/-0`) inside the nine-path authorized
surface.

Pre-closeout native verification (pinned SpecGrain source
`5de7d6499bb0a9e3a191fc0934399cf099d1980a`, script
`.specgrain/tmp/sg29_prove.py`, NOT committed) reports verified=false with
exactly 5 issues and nothing else: `protected-closeout-pr-pass`,
`closeout-post-merge-ci-pass`, `native-specgrain-verification-pass`, and
acceptance checks 2 and 7. Acceptance is 6/8, evidence 19/22; packet, spec,
result, scope, and surface bindings are clean. Dry-run result digest:
`sha256:d062907271e8b392f17600eac86ffd71b4d15070cb017fbcf7126b4e6d769223`.
No record was appended: a passing record cannot lawfully exist before this
closeout merges with green post-merge CI, so the closeout-dependent checks
honestly fail. This dry run is negative evidence, not a defect.

Ledgers unchanged by this PR: KX-P02-S07-T01 stays NEXT, P02 stays 9/10,
P02 exit stays unproven, SG-000029 stays GRAIN with 0 records.

Pending after this PR merges with post-merge CI 6/6: final native
verify_execution (all 22 evidence and 8 acceptance genuinely satisfiable),
immutable record under `.specgrain/evidence/SG-000029/`, then a proof PR
carrying the record, the proof section, and the single-task ledger transition.

Status: `IMPLEMENTED_UNPROVEN`.
