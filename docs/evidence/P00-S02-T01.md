# Evidence — KX-P00-S02-T01

## Task

`KX-P00-S02-T01 — Initialize repository-local SpecGrain and document deterministic authoring/refinement workflow.`

Risk: `R1`

## Qualified implementation

Final implementation head:

`18673181fc25b86e2e0d65db8be387000c6a580f`

Merged to `main` through PR #7 at:

`752b5f9b977a31c54743d92ff6f9063428eb3976`

## Pinned SpecGrain authority

Kernux pins:

- repository: `TheHalfMoon/SpecGrain`;
- commit: `5de7d6499bb0a9e3a191fc0934399cf099d1980a`;
- archive SHA-256: `da6a8c006c3a0154e76e2bf08bcae58bb86e04f06dd7a03115275b466489eef9`;
- license: MIT;
- minimum Python: 3.11 for repository-governance tooling only.

The repository-local runner downloads only the exact pinned archive over verified HTTPS, validates the tracked SHA-256, rejects unsafe archive members/symlinks, bounds download/extraction size, and executes the verified source from ignored cache. It does not vendor SpecGrain source into Kernux.

## Repository-local state

The merged task establishes:

- `.specgrain/project.json`;
- `.specgrain/policies/default.json` with `readiness_mode=report`;
- `.specgrain/specs/SG-000001.json`;
- `tools/specgrain/pin.json`;
- portable Python and Node launchers;
- `pnpm specgrain` / `pnpm specgrain:check`;
- a dedicated SpecGrain GitHub Actions job;
- governance documentation defining authority boundaries and deterministic authoring/refinement.

Runtime-only SpecGrain state under `.specgrain/tmp/` is ignored.

## Initial Grain proof

The first bounded successor task was created through the live pinned SpecGrain CLI as:

`SG-000001 — KX-P00-S03-T01 — Activate Diffcipline proof policy and CI gate`

Observed lifecycle:

`DRAFT -> SHAPED -> REFINING -> GRAIN`

Revision after shaping and final promotion:

`sha256:f86ede65c949dda5053ea103e3bd1d46da19af9e945e84c328e8b036d4b7e167`

Observed readiness:

- `specgrain check .` reported the REFINING leaf Grain-ready;
- explicit `grain` promotion succeeded;
- `specgrain next .` projected `SG-000001` as eligible in wave 1.

This eligibility did not override `specs/CURRENT.md`; canonical macro-frontier authority remained separate until this evidence reconciliation.

## Local verification

Before final push, the exact candidate was exercised locally with:

```text
python3 -m py_compile tools/specgrain/run.py
node --check tools/specgrain/run.mjs
python3 tools/specgrain/run.py check .
node tools/specgrain/run.mjs check .
pnpm install --frozen-lockfile
pnpm check:workspace
pnpm check:js
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets
git diff --check
```

Observed:

- SpecGrain store validation: PASS;
- Node launcher path: PASS;
- frozen pnpm install: PASS;
- Oxfmt/Oxlint/TypeScript: PASS;
- Node smoke tests: 1 passed / 0 failed;
- rustfmt/Clippy: PASS;
- Rust smoke tests: 1 passed / 0 failed;
- diff whitespace validation: PASS.

A deliberate tamper test appended bytes to the cached SpecGrain archive. The runner rejected it with exit code 2 because the observed SHA-256 no longer matched the tracked pin. Restoring the exact archive returned the store check to PASS.

On macOS, a local Python CA-bundle failure was also observed. The runner preserved TLS verification and successfully used system `curl` as a bounded HTTPS-only fallback; certificate verification was not disabled.

## Exact-head CI qualification

Final PR-head workflow run:

`35288293282`

Observed:

- JavaScript / TypeScript: SUCCESS;
- Rust: SUCCESS;
- SpecGrain: SUCCESS.

At final PR qualification:

- PR #7 was mergeable;
- CodeRabbit status: SUCCESS;
- open review threads: 0.

Post-merge `main` workflow run:

`35289526820`

Observed on `main@752b5f9b977a31c54743d92ff6f9063428eb3976`:

- JavaScript / TypeScript: SUCCESS;
- Rust: SUCCESS;
- SpecGrain: SUCCESS.

## Acceptance conclusion

**PROVEN** for the bounded SpecGrain-adoption outcome.

The evidence establishes repository-local deterministic SpecGrain readiness/refinement tooling and exact-source validation. It does not establish Diffcipline proof enforcement, donor provenance enforcement, product implementation, security qualification, or release readiness.

## Recovery

Recovery is a normal revert of PR #7 / merge commit `752b5f9b977a31c54743d92ff6f9063428eb3976`. Canonical product/user runtime data was not created by this task.
