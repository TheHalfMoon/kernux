# Evidence — KX-P02-S01-T01

## Task

`KX-P02-S01-T01 — Create kernuxd lifecycle, private UDS/named-pipe transport, health/version and graceful shutdown.`

SpecGrain:

`SG-000018 — KX-P02-S01-T01 — Create kernuxd lifecycle, private local transport, health/version probe, and graceful shutdown`

Risk: `R3`.

## Current closure state

The implementation is merged and qualified on `main`, but this task remains `NEXT` until the native SG-000018 proof is appended to the protected closeout PR and that proof-bearing head merges through the ordinary protected path.

This pre-proof closeout does **not** authorize KX-P02-S01-T02 or any later P02 work.

Final qualified implementation revision:

`e7645d5e950c9f959f5bbe1c3c5e3ad36e339f42`

Final implementation post-merge CI:

`35428742009` — six jobs SUCCESS, including the five protected contexts plus the additive `Windows kernuxd` evidence job.

## SpecGrain binding

The refined Grain is bound to:

- spec revision: `sha256:9264a4dd586340d788ec0d5b06b1ef1d4833bc29b5dd29cf00d519d4fd56aa55`;
- WorkPacket: `sha256:8ee3e890d95f077afde71956045076ff8c0442265697090ae20cbca6d73f776b`;
- context-plan digest: `sha256:6e9f9ae2cec2e86874cc4e93fdcd9246bd4288f49ed1d63db1f9b4f427b4b3aa`;
- acceptance criteria: 12;
- required evidence IDs: 17.

PR #84 — `docs: refine kernuxd lifecycle grain`

- exact head: `d924a8c6fbb28f4ecd526736ada6be5ca0cb517b`;
- exact-head CI: `35424966299` — SUCCESS;
- merge: `dbbf4e170cdd0b2c4913dbcb276e7af0fa8a5bc8`;
- post-merge CI: `35425026145` — SUCCESS.

No implementation began before the refinement merge and fresh-main qualification.

## Dependency admission

PR #85 — `build: admit kernuxd local IPC dependencies`

- exact head: `ea0375e335e0f3b424af3e5ddef28971d55a1d93`;
- exact-head CI: `35425385008` — SUCCESS;
- merge: `c81dafebdac8cee27d10d440cbeea40e1ef4ff0d`;
- post-merge CI: `35425432914` — SUCCESS.

The admitted direct runtime dependencies are exact and bounded:

- `interprocess = { version = "=2.4.4", default-features = false }`;
- `serde_json = "=1.0.151"`.

The dependency evidence packet is:

`docs/evidence/P02-S01-T01-DEPENDENCY-GATE.md`

Observed and reviewed license expressions:

- `interprocess 2.4.4`: `0BSD OR Apache-2.0`;
- `serde_json 1.0.151`: `MIT OR Apache-2.0`.

The repository dependency validator passes on the final implementation revision:

`Dependency admission: PASS (6 direct external dependencies)`.

The first repository-local dependency validation attempt during implementation failed closed because the required exception evidence path did not yet exist. That failure is preserved in the dependency-gate packet and was not relabeled.

## Generated KRP probe contract

PR #86 — `feat: add generated kernuxd probe contracts`

- exact head: `05481016a5541b3468594155d5dee996b70c4f76`;
- exact-head CI: `35425635587` — SUCCESS;
- merge: `15e170f85dd2006b1bbe668ce2e0c543bf7c61e7`;
- post-merge CI: `35425681770` — SUCCESS.

The authoritative KRP schema and deterministic generator provide:

- `DaemonProbeKind`;
- `DaemonLifecycleState`;
- `DaemonHealthState`;
- `DaemonProbeRequest`;
- `DaemonProbeResponse`.

Final schema SHA-256:

`904eb86ee9028aca002586187ade53eefea1a51339bff9904182e95d88751b94`

Final shared core fixture conformance:

`KRP shared fixture conformance: PASS (10 cases, sha256:904eb86ee9028aca002586187ade53eefea1a51339bff9904182e95d88751b94)`

The probe contract binds exact `krp/1`, request identity, lifecycle health, daemon package version, implementation revision, and authoritative schema provenance. Unknown fields and unsupported `krp/2` payloads reject.

Existing KRP compatibility and adversarial semantics remain qualified and unchanged.

## kernuxd lifecycle

PR #87 — `feat: add kernuxd lifecycle foundation`

- exact head: `9684a3c99f5761e31b66861c0aa73b9b79296a70`;
- exact-head CI: `35425883624` — SUCCESS;
- merge: `50e1e04d813bb5f211a453a47d6c8b79d8e4f57a`;
- post-merge CI: `35425929316` — SUCCESS.

The lifecycle is explicit:

`Starting -> Serving -> ShuttingDown -> Stopped`

Independent final-main lifecycle execution:

`cargo test --locked -p kernuxd --test lifecycle` — 5/5 PASS.

The owner-side shutdown handle remains in-process. No shutdown command is exposed over unauthenticated IPC. Probe health is derived honestly from lifecycle state and does not imply caller authorization or privileged-operation readiness.

## Local transport core

PR #88 — `feat: add kernuxd local probe transport`

- exact head: `0f07f919293b1397611036d07023de04557fb2e7`;
- exact-head CI: `35426800474` — SUCCESS;
- merge: `7e9059718d0e76b9bf4082d1c7beaface81c10b3`;
- post-merge CI: `35426845321` — SUCCESS.

The transport is local-only and synchronous:

- Unix: filesystem Unix-domain socket under a verified private runtime directory;
- Windows: local namespaced pipe token;
- fixed bounded frame size;
- one probe request per connection;
- finite I/O deadline behavior;
- generated request/response wire types;
- typed fail-closed framing errors;
- active second-bind rejection;
- safe Unix stale-socket recovery only after endpoint verification;
- owner-controlled graceful shutdown;
- no TCP, UDP, HTTP, Tokio, or remote transport.

A combined unpublished scratch candidate contained transport plus Unix tests at +869 lines. Diffcipline R3 correctly rejected that combined range because it exceeded the +600-line maximum. The rejected scratch commit was never pushed and the gate was not weakened. The work was split into bounded PRs.

## Unix runtime and adversarial evidence

PR #89 — `test: add kernuxd Unix transport adversarial corpus`

Corrected exact head:

`2ec4264e5db129f3b6c1afd6603f0ba91a7f18a2`

- corrected exact-head CI: `35427100017` — SUCCESS;
- merge: `0d7461bdf5e247d3a15701e0453b4538e5e6753b`;
- post-merge CI: `35427145457` — SUCCESS.

Independent final-main Unix execution:

`cargo test --locked -p kernuxd --test transport_unix` — 7/7 PASS.

The executable Unix corpus proves:

- startup -> serving -> health/version probe -> owner shutdown -> stopped;
- endpoint cleanup and clean restart;
- active second-bind conflict without displacement;
- verified stale socket recovery;
- unsafe non-socket endpoint rejection;
- non-private runtime directory rejection;
- runtime-directory symlink rejection;
- malformed JSON rejection;
- oversized-frame rejection;
- unknown-field rejection;
- unsupported `krp/2` rejection;
- idle-client timeout without daemon death;
- one probe response per connection;
- explicit absence of network-listener primitives.

### Preserved PR #89 failure

Initial head:

`61c17f5b3a226c7ed6784d2e35a47b3b7a6cd78f`

Workflow:

`35426979066` — FAILURE.

The one-probe-close test assumed Linux and macOS would expose identical close behavior after the client pipelined a second request. Ubuntu returned connection reset where macOS returned clean EOF. The test was corrected without weakening the invariant: it now proves the first response belongs to the first request and accepts platform-valid close outcomes while treating any second response as a hard failure.

The failed workflow remains failed evidence and is not reused as success evidence.

## Windows named-pipe runtime evidence

PR #90 — `test: add Windows kernuxd runtime evidence`

Corrected exact head:

`0eb5f2c5e89543b105b39b9c8d15e46fa4388e43`

- corrected exact-head CI: `35428586300` — six jobs SUCCESS;
- merge: `e7645d5e950c9f959f5bbe1c3c5e3ad36e339f42`;
- post-merge CI: `35428742009` — six jobs SUCCESS.

The additive `Windows kernuxd` job runs natively on `windows-latest` and passed both:

- `cargo clippy --locked -p kernuxd --all-targets -- -D warnings`;
- `cargo test --locked -p kernuxd --all-targets`.

The Windows runtime corpus proves:

- local named-pipe probe/shutdown/restart;
- second-bind conflict and clean rebind after listener release;
- rejection of remote-host and path-form pipe names;
- bounded owner shutdown with an idle named-pipe client.

Windows uses nonblocking synchronous named-pipe streams with explicit finite read/write deadlines because `interprocess 2.4.4` does not expose socket-style synchronous named-pipe I/O timeouts. Unix preserves kernel receive/send timeout behavior.

### Preserved PR #90 failure

Initial exact head:

`dd71e48ede895de6f6f37b72ae865873409ff100`

Workflow:

`35428388019` — FAILURE.

Windows Clippy passed, but runtime tests were 3 passed / 1 failed. The failing test was:

`local_named_pipe_probe_shutdown_and_restart_are_deterministic`

Observed error:

`IncompleteFrame`

The Windows nonblocking pipe path could expose zero-byte no-progress behavior that the first implementation interpreted as EOF. The correction treats Windows zero-byte progress as pollable no-progress until the finite deadline while preserving Unix EOF/WriteZero semantics. The idle-client test was also strengthened so an immediate close is a failure rather than a false bounded-shutdown success.

The failed workflow remains failed evidence and is not reused as success evidence.

## Final implementation CI

The five protected status contexts remain:

- JavaScript / TypeScript;
- Rust;
- SpecGrain;
- Diffcipline R1;
- Provenance.

The additive evidence job is:

- Windows kernuxd.

PR #90 exact-head run `35428586300` passed all six jobs.

Final implementation post-merge run `35428742009` passed all six jobs on `main@e7645d5e950c9f959f5bbe1c3c5e3ad36e339f42`.

Branch protection remained enabled and the original five required contexts remained unchanged. The Windows job is additional evidence and is not a substitute for a protected context.

## Review discipline

Alibaba OpenCodeReview is the review framework for this task. CodeRabbit is not used as evidence.

Across the implementation slices:

- OCR Delegation Mode was used to account for reviewable files and applicable rule groups;
- excluded files, when present, were manually reviewed;
- Rust/security rules were applied to the transport and runtime-test slices;
- GitHub Actions rules were applied to the Windows workflow change;
- direct `ocr review` was attempted during the transport work but no LLM endpoint/token/model is configured on the device, so no direct LLM OpenCodeReview success is claimed.

PR #90 exact-head review record was anchored to `0eb5f2c5e89543b105b39b9c8d15e46fa4388e43` after fresh 6/6 CI. No unresolved review threads remained before merge.

## Independent final-main verification

On `main@e7645d5e950c9f959f5bbe1c3c5e3ad36e339f42`:

- `git diff --check dbbf4e170cdd0b2c4913dbcb276e7af0fa8a5bc8..e7645d5e950c9f959f5bbe1c3c5e3ad36e339f42` — PASS;
- `cargo test --locked -p kernuxd --test lifecycle` — 5/5 PASS;
- `cargo test --locked -p kernuxd --test transport_unix` — 7/7 PASS;
- `python3 tools/dependencies/validate.py check` — PASS;
- exact dependency pin/default-feature assertions — PASS;
- no-network-listener static assertions over `kernuxd` transport/binary — PASS;
- later-P02 scope-exclusion assertions — PASS;
- six workflow-name assertions — PASS;
- `pnpm check` — PASS;
- SpecGrain check — PASS;
- generated KRP contract drift/conformance — PASS;
- compatibility matrix — PASS;
- adversarial matrix — PASS;
- Rust formatting/Clippy/workspace tests — PASS.

One closeout-time local invocation of the dependency validator omitted the required `check` subcommand and exited 2 after printing CLI usage. The corrected canonical command above passed immediately. This was a command-invocation error, not a product or dependency-admission failure, and it is not counted as success evidence.

## Observed implementation paths

The independently observed implementation range is:

`dbbf4e170cdd0b2c4913dbcb276e7af0fa8a5bc8..e7645d5e950c9f959f5bbe1c3c5e3ad36e339f42`

Exactly 18 paths changed:

- `.github/workflows/ci.yml`;
- `Cargo.lock`;
- `Cargo.toml`;
- `crates/kernux-contracts/src/generated.rs`;
- `crates/kernux-contracts/tests/fixtures.rs`;
- `crates/kernuxd/Cargo.toml`;
- `crates/kernuxd/src/lib.rs`;
- `crates/kernuxd/src/main.rs`;
- `crates/kernuxd/src/transport.rs`;
- `crates/kernuxd/tests/lifecycle.rs`;
- `crates/kernuxd/tests/transport_unix.rs`;
- `crates/kernuxd/tests/transport_windows.rs`;
- `docs/evidence/P02-S01-T01-DEPENDENCY-GATE.md`;
- `packages/contracts/src/conformance.ts`;
- `packages/contracts/src/generated.ts`;
- `protocol/fixtures/v1/core.json`;
- `protocol/schema/krp.v1.schema.json`;
- `third_party/dependencies/approved.json`.

Every observed path is inside the SG-000018 WorkPacket authorized change surface.

## Scope boundary

This task does not implement or claim:

- launch nonce;
- peer identity binding;
- caller authentication or authorization;
- rejection of unauthorized local callers;
- installation/device identity or keys;
- SQLite or any durable metadata store;
- artifact CAS;
- policy evaluation or Grant enforcement;
- permission profiles;
- secret brokering;
- security-audit persistence;
- privileged filesystem/process/PTY operations;
- sandbox lifecycle;
- browser/computer control;
- remote/cloud transport;
- TCP, UDP, HTTP, or other network listeners;
- P02 phase completion.

KX-P02-S01-T02 retains exclusive ownership of caller/session binding.

## Protected closeout candidate

This branch is the real protected closeout candidate for SG-000018.

At this pre-proof head:

- `KX-P02-S01-T01` remains `NEXT`;
- `KX-P02-S01-T02` remains `PLANNED`;
- later P02 tasks remain `PLANNED`;
- P02 phase exit remains unproven;
- native SG-000018 evidence has not yet been appended;
- exact-head protected CI plus the additive Windows job must pass before native verification may cite `protected-closeout-pr-pass`.

After exact-head qualification, the pinned native SpecGrain API may append the immutable SG-000018 verification record to this same PR. That proof-bearing head may update `specs/CURRENT.md` and `specs/tasks.md` for the authorized transition, but the transition is not canonical until ordinary protected merge succeeds.

The proof-bearing head must qualify again before merge. The resulting `main` revision must then pass post-merge qualification.

## Next frontier

Protected exact-head qualification of this pre-proof closeout candidate, then native SG-000018 verification.

No KX-P02-S01-T02 implementation is authorized before the proof-bearing closeout merges and final post-merge qualification succeeds.
