# Evidence — KX-P01-S04-T01

## Task

`KX-P01-S04-T01 — Define event envelope, lineage, artifact references, evidence binding and redaction metadata.`

SpecGrain:

`SG-000011 — KX-P01-S04-T01 — Freeze Event envelope, lineage, artifact, evidence, and redaction semantics`

Risk: `R2`.

## Outcome

Kernux now has one provider-neutral conceptual Event/Evidence contract for:

- immutable Event and Evidence identity;
- typed owner streams;
- explicit contiguous `stream_seq`;
- exact `previous_event_id` continuity;
- correlation to exact entity revisions, RequestId, OperationId, Grant, Runtime, and provenance;
- acyclic causal/supersession/fork lineage;
- exact Artifact references with SHA-256 when bytes qualify a claim;
- evidence source classes and independent-gate rules;
- explicit redaction/exclusion/transformation metadata;
- deterministic projection rebuild and fail-closed integrity behavior.

No persistent event store, generated wire schema, CAS implementation, export archive, compatibility policy, or runtime event transport was introduced.

## Grain identity

Refinement PR #45:

`docs: refine event evidence grain`

Refinement head:

`b24f3fa07edfba4a6ee1ebd4544aece1a630f872`

Refinement merge:

`1d9429e630b17bdf884a2b5bfe48727d29bbd49c`

Spec revision:

`sha256:4f25d40eb0c9a55c3a16bbd4addb0c63e17b3ec04ae0219ed1cbbcabc0d7d091`

WorkPacket:

`sha256:39e970e84c5af391dcca47ae60a36f4817b278cd369f20e7a09a12c00434ac6a`

Context-plan digest:

`sha256:fa79c2e7bc82bd945c32c5f006a875d46c1e66097246395fefbe0ffa826ae890`

Context:

- 12 required sources;
- 18,322 source bytes;
- 4,584 / 10,000 estimated context tokens.

Refinement proof:

- SpecGrain: PASS;
- `pnpm check`: PASS;
- `git diff --check`: PASS;
- Diffcipline R2: PASS;
- +110 / -0;
- scope violations: 0;
- PR CI `35339301942`: five required jobs SUCCESS;
- post-merge CI `35339377573`: five required jobs SUCCESS.

## Implementation

PR #46:

`docs: define event evidence model`

Head:

`7c465058c1e77526b0270ff9d19e29469bd8cd9f`

Merge:

`28515e290f09b1ee164d5749a3d0e0d9510a64e6`

Exact implementation:

- seven files;
- +591 / -2;
- Diffcipline R2: PASS;
- focused/adversarial checker: PASS.

Primary contract:

`docs/canonical/EVENT_AND_EVIDENCE_MODEL.md`

Accepted decision:

`ADR-0041 — Durable truth is append-only Event streams with independently bound Evidence`

Cross-linked canonical surfaces:

- `docs/canonical/ARCHITECTURE.md`;
- `docs/canonical/IDENTITY_AND_REVISION_MODEL.md`;
- `docs/canonical/RUNTIME_OPERATION_MODEL.md`;
- `docs/canonical/QUALITY_AND_EVIDENCE.md`;
- `docs/canonical/DATA_LIFECYCLE_AND_PORTABILITY.md`;
- `docs/canonical/ARCHITECTURE_DECISIONS.md`.

PR CI:

`35341058400` — five required jobs SUCCESS.

Post-merge CI:

`35341135584` — five required jobs SUCCESS.

## Adversarial proof

Focused checks established:

- sequence starts at 1;
- zero/gap/duplicate/wrong-predecessor streams reject;
- conflicting Event identities reject;
- UUID/timestamps cannot override canonical sequence;
- causal self/transitive cycles reject;
- Artifact digest mismatch rejects byte qualification;
- `agent_report` cannot satisfy machine/human independent gates;
- transformed redaction creates a new Artifact identity/digest;
- unknown Event type cannot silently mutate a privileged projection.

## Native SpecGrain verification

Implementation range:

`1d9429e630b17bdf884a2b5bfe48727d29bbd49c..28515e290f09b1ee164d5749a3d0e0d9510a64e6`

Verification:

`verified=true`

Issues:

`[]`

Result digest:

`sha256:954a787117f106dde4dbbae85f77820f0b31da54d9d969eeffbd2e9d6a457447`

Evidence record:

`sha256:7915f9afebb37a6b5261e1a07568d2f38da950b28dd2f81d18b2853cc5a518b7`

`specgrain prove SG-000011 .` reports `verified=true`.

## Acceptance conclusion

**PROVEN** for `KX-P01-S04-T01`.

This does not prove generated Rust/TypeScript contracts, compatibility behavior, fixture corpus, or the P01 phase exit.

## Next frontier

`KX-P01-S05-T01 — Choose one schema source and generate Rust + TypeScript contracts with round-trip/conformance fixtures.`

Its prior P01 semantic dependencies are now PROVEN.
