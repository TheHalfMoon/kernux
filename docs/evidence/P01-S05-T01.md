# Evidence — KX-P01-S05-T01

## Task

`KX-P01-S05-T01 — Choose one schema source and generate Rust + TS contracts with round-trip/conformance fixtures.`

Primary SpecGrain:

`SG-000012 — KX-P01-S05-T01 — Generate provider-neutral Rust and TypeScript contracts from one canonical schema`

Governance prerequisite SpecGrain:

`SG-000013 — KX-P01-S05-T01 prerequisite — Replace unmergeable dependency REVIEW with enforced dependency admission`

Risk: `R2`.

## Outcome

Kernux now has one provider-neutral KRP v1 schema source at:

`protocol/schema/krp.v1.schema.json`

Schema SHA-256:

`0ec273c387cf8f344b178c66ae046f75af88945dc428f4a16601156f4e538500`

The repository deterministically generates Rust and TypeScript contracts from those exact schema bytes. Generated outputs declare the authoritative source and exact schema digest; generated Rust is emitted rustfmt-stable by the generator itself.

The shared conformance source is:

`protocol/fixtures/v1/core.json`

It contains eight canonical cases covering CapabilityRequest, Grant, RuntimeContactObservation, OperationStart, OperationObservation, RuntimeError, Event, and Evidence.

The same tracked fixture bytes are consumed by:

- Node/schema conformance in `tools/protocol/conformance.mjs`;
- generated TypeScript compile-time fixture assertions;
- Rust `serde` / `serde_json` fixture tests;
- semantic JSON round-trip equality checks.

The normal repository baseline now checks generator drift plus shared fixture conformance.

Compatibility/version-negotiation and unknown/additive-field behavior remain explicitly owned by `KX-P01-S05-T02`. The full adversarial protocol corpus remains explicitly owned by `KX-P01-S05-T03`.

## Primary implementation chain

- PR #49 — `feat: select KRP schema source` — merge `6a733533507be1f5a4d844827dbc04158c1aa1eb`;
- PR #50 — `feat: add deterministic KRP generator` — merge `cc2f0293d77884bc1668e2d6d3b780f8cb312fb6`;
- PR #51 — `feat: generate TypeScript KRP contracts` — merge `70b4dcaf57cb9608a49f685c86444dbdad976b89`;
- PR #52 — `fix: align KRP capability runtime schema` — merge `2602833bd1396c8ed23b5810176cb366e5646f64`;
- PR #53 — `fix: align KRP event evidence schema` — merge `54c223e8db4ca49c367f3d5677a626fa156443a4`;
- PR #60 — `build: admit Rust contract dependencies` — merge `34d2bea46e01ac35c2f4efb0d1c01736348b599e`;
- PR #61 — `feat: generate Rust KRP contracts` — merge `44e8ec21f7a3b402cc78a9a6c8e7cb176dc39b46`;
- PR #62 — `test: add shared KRP conformance fixtures` — merge `3f1bce2992b9a7b96467fdb3baf9925f68917ea9`;
- PR #63 — `test: prove cross-language KRP conformance` — merge `a1ee7a16339302152324155357c6f96d1314fd6c`.

The final implementation revision bound by SG-000012 is:

`git:a1ee7a16339302152324155357c6f96d1314fd6c`

PR #63 exact-head CI run `35413911244` passed all five protected required jobs.

PR #63 post-merge CI run `35414046602` passed all five protected required jobs.

## Dependency-governance prerequisite

PR #54 demonstrated the original governance deadlock and was deliberately closed unmerged. Its failing Diffcipline REVIEW state remains preserved as negative evidence.

The replacement prerequisite path was bounded across PRs #55–#60. It introduced the required dependency-admission validator and transferred executable dependency judgment into the already-required Provenance gate before Diffcipline dependency/lockfile observations changed from REVIEW to ALLOW.

The first admitted external Rust dependencies are:

- `serde = 1.0.229`;
- `serde_json = 1.0.151`.

The dependency gate evidence is recorded in:

`docs/evidence/P01-S05-DEPENDENCY-GATE.md`

SG-000013 implementation revision:

`git:d309f7f4328507415d80037645f6d595ed78d369`

## Native SpecGrain verification — SG-000012

Spec revision:

`sha256:e70b76e1f20a1663cefea55a435c280bbf8643254ff1f1ac726add76731ebafa`

WorkPacket:

`sha256:398a09aa4d2f520ce8ee087156097180427a2e71c518423b9ec85025240f284d`

Context-plan digest:

`sha256:2dc490285498d4e593039cd155a586b12a03d12970dd19b1200def4c9f89dc8f`

Verification:

`verified=true`

Issues:

`[]`

Result digest:

`sha256:2a3454db5d22e014e4c8b2f1166e281729d740934c4df2469b52a4b6ef3ba6a7`

Evidence record:

`sha256:6d3caf7c146995e76228ec4d10ede1b2b1e440a8873b40079313d7d427d475e4`

The record was appended through the pinned native SpecGrain API and binds the original recovered WorkPacket.

## Native SpecGrain verification — SG-000013

Spec revision:

`sha256:ad439f9bf15b318df7dbab23fab29d5e3f979345b76b15904c0a4b862572e53f`

WorkPacket:

`sha256:24e46f3454f199cde77e33f131a083f7cfef8e5bc6ecd0dabcc581b85f4bf510`

Context-plan digest:

`sha256:606e9a4e0bb02720f944e7bde2f90634dc7797a4b0b6b1d79bfd2d4343d1dde7`

Verification:

`verified=true`

Issues:

`[]`

Result digest:

`sha256:df44da2978819cddf90b15c25332e1d7ebe9ba4ee0fb6e6802ce8234a70cf324`

Evidence record:

`sha256:41d657f718ec18d70bca33d5c73e5decff5a51e7f6ad46066f8509b2883db800`

PR #64, `evidence: append S05 native proofs`, merged at `f4ce48bf5258c2106764d8cfbe6b769b879f5ce3`.

Its exact-head CI run `35414325360` passed all five protected required jobs.

Its post-merge CI run `35414497010` passed all five protected required jobs.

## Governance verification

Live branch protection after the native-proof merge requires exactly:

- JavaScript / TypeScript;
- Rust;
- SpecGrain;
- Diffcipline R1;
- Provenance.

Required checks are strict, `enforce_admins=true`, required conversation resolution is enabled, force pushes are disabled, and branch deletion is disabled.

No required check was removed or bypassed.

## Acceptance conclusion

**PROVEN** for `KX-P01-S05-T01`.

The evidence establishes one authoritative JSON Schema source, deterministic generated Rust and TypeScript contracts, shared cross-language conformance fixtures, exact schema-digest binding, Rust/TypeScript semantic round trips, fail-closed generated-output drift detection, provider-neutral core contracts, and the governed dependency prerequisite required to support the Rust implementation.

This does not prove compatibility/version-negotiation policy, unknown/additive-field behavior, the full adversarial protocol corpus, or the P01 phase exit.

## Next frontier

`KX-P01-S05-T02 — Define compatibility/versioning rules and unknown/additive-field behavior.`

Its generated-contract dependency is now PROVEN.
